#![forbid(unsafe_code)]

//! Watchdog process, mutual heartbeat with dnsqb-service (SPEC.md §7).
//! Deferred to Фаза 3 (SPEC.md "Фазований план") — crate scaffolded now so
//! the workspace shape doesn't change later, no implementation yet.

fn main() {
    todo!("Фаза 3: dnsqb-watcher not implemented yet (SPEC.md §7)");
}
