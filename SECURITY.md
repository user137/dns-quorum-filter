# Security

Threat model summary, hard constraints, and dependency-vetting table. Full reasoning behind each
item lives in `SPEC.md` — this file tracks the current state, `SPEC.md` explains why.

## Threat model (summary — see SPEC.md §"Наскрізні вимоги", §2, §8.1 for full reasoning)

- **Local DoH cert compromise** — the largest single attack surface in the project. Mitigated by
  using a single self-signed leaf certificate on `127.0.0.1` (never a local CA): a compromised
  private key spoofs localhost only, not arbitrary domains. See SPEC.md §2.
- **Tauri IPC boundary (webview → Rust backend)** — treated as untrusted input (XSS in the UI,
  compromised render, or dev error can all produce an arbitrary IPC call). Covered by four test
  categories, not one "smoke" test: correctness, exploit (path traversal, allowlist bypass,
  oversized payload), misuse (contradictory input, malformed manually-edited override file),
  fuzz (no `unwrap()`/`panic!` on arbitrary byte input at the boundary). See SPEC.md §8.1.
- **Upstream DoH providers and DNS wire format** — parsed via `hickory-dns`, not a hand-rolled
  parser, specifically because this is historically the highest-CVE-density code class in C-based
  DNS software.
- **GeoIP / top-sites data feeds** — binary blobs that influence blocking decisions; atomic
  replacement is gated on TLS + checksum verification before swapping in a new file (SPEC.md §3.5).
- **Browser silent DoH-fallback** — open, not fully closed: if `dnsqb-service` errors or times out,
  the browser may silently fall back to system DNS, bypassing quorum entirely with no built-in
  mitigation in the current design (SPEC.md, Відкриті питання, п.10).

## Hard constraints (see SPEC.md §"Наскрізні вимоги" for the full list)

- Listens only on `127.0.0.1` — never `0.0.0.0`, no network listener beyond loopback.
- `#![forbid(unsafe_code)]` in every crate where possible; any `unsafe` needs an explicit
  justification comment.
- Domain names live only in the in-memory ring buffer by default; disk persistence of the query log
  is opt-in and must be encrypted via platform secure storage. Diagnostic/service logs never contain
  domain names.
- Elevated privileges only once, for the one-time trust-store install/uninstall step — the main
  process runs as a normal user, never with standing elevated privileges.
- CI must run `cargo audit` (known CVEs) and `cargo deny` (license/duplicate checks) as required
  steps, not optional ones.
- Reproducible builds / signed release binaries — the app installs a trusted certificate into the
  system, so binary trust is as security-critical as the certificate itself.

## Dependency vetting

When a crate is added, add a row here **before merging**, not after:

| Crate | Maintainer | Reproducible build? | Independent audit / reputation | CVE history | Notes |
|---|---|---|---|---|---|
| `hickory-proto` 0.26.1 | `hickory-dns` project (formerly `trust-dns`); maintainers include Dirkjan Ochtman (also rustls, RustSec core team), divergentdave, marcus0x62 | Standard `cargo`/`crates.io` build, no vendored binaries | First formal security audit funded/performed by Ferrous Systems; production use includes Let's Encrypt's recursive resolver and (per public reporting) Google Pixel | Past DNSSEC-validation CVEs (e.g. CVE-2025-25188, unbounded-loop/memory-exhaustion class), fixed in later releases; `cargo audit` run clean against 0.26.1 at time of adding (2026-08-25) — re-verify on every `cargo audit` CI run, don't treat this row as a standing guarantee | Chosen per SPEC.md "Технічний стек" specifically to avoid hand-rolling a DNS wire-format parser (highest-CVE-density code class in C-based DNS software); only the `-proto` sub-crate is used in Крок 0 (wire-format types for conformance-test fixtures), not the full resolver |
| `tokio` 1.53.1 | `tokio-rs` org, founding maintainer Carl Lerche; foundational async runtime for a large fraction of the Rust async ecosystem, backed by multiple large-org sponsors | Standard `cargo`/`crates.io` build, no vendored binaries | De facto standard async runtime; formal `security@tokio.rs` disclosure process, advisories coordinated through GitHub/RustSec | Historical advisories cluster in `io-util`/channel APIs and legacy 0.1-era sibling crates (`tokio-reactor`, `tokio-timer`, both unmaintained, not depended on here); `cargo audit` run clean against 1.53.1 at time of adding (2026-08-25, T-20–T-26 batch) — re-verify on every CI run | Async runtime for concurrent upstream queries (SPEC.md "Технічний стек"); features limited to `rt-multi-thread`, `macros`, `net`, `time` — no `full` |
| `reqwest` 0.13.4 | Sean McArthur (`seanmonstar`); widely used, foundational Rust HTTP client | Standard `cargo`/`crates.io` build, no vendored binaries; TLS backend (`rustls`) itself audited independently (see rustls project) | Most widely depended-on Rust HTTP client; `rustls`-backed builds avoid the system OpenSSL dependency the "Технічний стек" section explicitly rules out | `cargo audit` run clean against 0.13.4 and its `rustls`/`aws-lc-rs` TLS stack at time of adding (2026-08-25) — re-verify on every CI run | `default-features = false`, features `rustls` + `http2` only — no `native-tls`, no `blocking`. Pulled `aws-lc-rs`/`aws-lc-sys` as the `rustls` crypto backend and `webpki-root-certs`/`rustls-platform-verifier` for trust-store validation, each vetted via `cargo deny`'s license check (`deny.toml`), not this table, since none is a direct dependency this project chose |
| `futures-util` 0.3.34 | `rust-lang` org (`futures-rs` project); foundational async-combinator crate, widely depended on across the ecosystem (also pulled transitively by `reqwest`/`hyper`) | Standard `cargo`/`crates.io` build, no vendored binaries | Long-standing, heavily reviewed crate; no independent formal audit found, but extremely high reverse-dependency count makes it de-facto scrutinized | Two historical advisories, both fixed years before this version and irrelevant to the API surface used here: RUSTSEC-2020-0059 (`MutexGuard::map` Send/Sync unsoundness) and RUSTSEC-2020-0062 (unsound `Sync` on `FuturesUnordered`, fixed 0.3.5) — neither `Mutex` type is used by this project. `cargo audit`/`cargo deny check` run clean against 0.3.34 at time of adding (2026-08-25, T-27–T-31 batch) | `default-features = false`, feature `std` only — pulled specifically for `FuturesUnordered`/`StreamExt`, not the full `futures` crate (SPEC.md §3.6's early-return/cancellation, T-30) |
| `tracing` 0.1.44 | `tokio-rs` org; same maintainer group as `tokio` | Standard `cargo`/`crates.io` build, no vendored binaries | Facade/macro crate for the `tokio`-ecosystem's de-facto standard structured-logging framework; shares `tokio`'s `security@tokio.rs` disclosure process | No advisories against the `tracing` facade crate itself; RUSTSEC-2025-0055 (ANSI-escape log injection) is against `tracing-subscriber`, which this project does **not** depend on yet (no subscriber wired — T-29 only emits events, `main.rs` is still a stub) — re-check this row when a subscriber is added (real-listener batch). `cargo audit` run clean at time of adding (2026-08-25) | Diagnostic-only logging (SPEC.md, Наскрізні вимоги — no domain names in service logs); `error_kind()` in `quorum.rs` deliberately avoids logging `UpstreamError`'s own `Display` for the `Http` variant, since `reqwest::Error`'s message embeds the failed request URL (which encodes the query, i.e. the domain) |
| `moka` 0.12.16 | `moka-rs` org, maintainer `tatsuya6502`; production use includes crates.io's own API service (cache in front of PostgreSQL, ~85% hit rate on the download endpoint) | Standard `cargo`/`crates.io` build, no vendored binaries | Not part of a large sponsored foundation like `tokio-rs`/`rust-lang`, but has real production adoption beyond its own test suite; design is a Rust port of Java's well-established Caffeine cache | No advisory filed against `moka` itself in the RustSec database. Changelog shows the project proactively bumping transitive deps ahead of advisories against *them* (`crossbeam-epoch` for CVE-2022-23639, `crossbeam-channel` to 0.5.15 for RUSTSEC-2025-0024) — a maintenance-health signal, not a vulnerability in `moka`. `cargo audit`/`cargo deny check` run clean against 0.12.16 and its full transitive tree at time of adding (2026-08-25, T-32/T-34/T-36 batch); `cargo deny check` reported no new license-allowlist gap | `default-features = false`, feature `future` only (SPEC.md §4: async-compatible concurrent cache, per-entry TTL via the `Expiry` trait, T-32). Not yet wired to the request pipeline — see `cache.rs`'s module doc comment and TASKS.md T-39 |
| `serde` 1.0.229 + `serde_json` 1.0.151 | `serde-rs` org, maintainer David Tolnay (also `syn`/`thiserror`, prolific/high-reputation Rust ecosystem maintainer); the de-facto standard Rust (de)serialization framework | Standard `cargo`/`crates.io` build, no vendored binaries | Extremely high reverse-dependency count (already pulled in transitively via `reqwest`/`hickory-proto` before this batch made it a direct dependency); no independent formal audit found, but among the most heavily reviewed crates in the ecosystem | No advisories against either crate in the RustSec database. `cargo audit`/`cargo deny check` run clean against 1.0.229/1.0.151 at time of adding (2026-08-26, T-37 batch) | `serde` with `derive` only. Direct dependency now for the override-list file's on-disk JSON shape (`overrides.rs`, T-37) — not spent solely there: the same crate is needed regardless for T-53's Tauri IPC DTO layer (`diagrams/ui-dto-model.md`), so introducing it now is for its long-term purpose, not a one-off parser |
| `tempfile` 3.27.0 (dev-only) | `Stebalien` (Steven Allen); long-standing, widely used Rust temp-file/dir crate | Standard `cargo`/`crates.io` build, no vendored binaries | High reverse-dependency count; not part of a large sponsored foundation but long track record | No advisories against this crate in the RustSec database. `cargo audit`/`cargo deny check` run clean at time of adding (2026-08-26, T-37 batch) | **`[dev-dependencies]` only — never compiled into a shipped binary.** Used solely in `overrides.rs`'s unit tests for `OverrideLists::load`'s file-reading path (RAII cleanup via `NamedTempFile`'s `Drop`, not manual `fs::remove_file`) |
| `parking_lot` 0.12.5 (+ `parking_lot_core` 0.9.12, `lock_api` 0.4.14) | Amanieu d'Antras; long-standing, widely used synchronization-primitives crate (also maintains `hashbrown`, in `rustc`'s own toolchain) | Standard `cargo`/`crates.io` build, no vendored binaries | High reverse-dependency count across the ecosystem, including `rustc` itself in some configurations; no independent formal audit found but long track record and heavy real-world scrutiny | No advisories against `parking_lot`/`parking_lot_core`/`lock_api` in the RustSec database. `cargo audit`/`cargo deny check` run clean against 0.12.5 at time of adding (2026-08-26, T-42/T-43 batch); no new license-allowlist gap (MIT/Apache-2.0, already allowed) | SPEC.md §6.1's explicit choice for `query_log.rs`'s ring buffer — a `std`-lock-free, non-`tokio` `RwLock` for an in-memory structure with no `.await` inside any critical section (a `tokio::sync::RwLock` would add unneeded overhead for a lock never held across an await point) |
| `rcgen` 0.14.9 | `rustls` org (same org as the `rustls` TLS crate already a transitive dependency via `reqwest`); actively maintained, used by `rustls`'/`quinn`'s own test suites and widely as a self-signed-cert generator | Standard `cargo`/`crates.io` build, no vendored binaries | No independent formal audit found specifically for `rcgen`, but high scrutiny by association — it's the de-facto standard Rust crate for exactly this use case (test/local self-signed certs) | No advisories against `rcgen` in the RustSec database. `cargo audit`/`cargo deny check` run clean against 0.14.9 at time of adding (2026-08-26, T-48 batch); no new license-allowlist gap (`MIT OR Apache-2.0`, already allowed) | SPEC.md §2's self-signed leaf certificate for the local `DoH` listener (`cert::generate_self_signed_cert`, T-48). `default-features = false, features = ["aws_lc_rs"]` — **not** the default `ring` feature — deliberately matches `reqwest`/`rustls`'s already-chosen `aws-lc-rs` crypto backend, avoiding a second directly-chosen crypto implementation in the binary (`ring` can still appear transitively via an Android-only `rustls-platform-verifier-android` dependency, which is not this project's choice and not active on the Windows target). **`zeroize` feature checked, not enabled**: read the 0.14.9 source directly — the feature only implements manual `zeroize::Zeroize` for `KeyPair` (`.zeroize()` must be called explicitly), not `ZeroizeOnDrop`, so enabling it alone would not add any automatic Drop-based protection; real on-drop zeroization would need wrapping the key in `zeroize::Zeroizing<KeyPair>` (a second, separate dependency) at whatever point the key gets a real owner/lifetime — deliberately deferred to **T-50** (private-key storage), which is where that lifecycle is actually decided, not silently skipped. **`IsCa::ExplicitNoCa` used, not the `NoCa` default** — the default omits the `BasicConstraints` extension entirely rather than encoding `cA=FALSE`, which would make this project's "leaf, never a CA" property (this file's own top-listed threat) unverifiable from the cert's actual bytes; caught by advisor review of the diff, not by the first-written tests, which passed either way |
| `x509-parser` 0.18.1 (dev-only) | `rusticata` org, maintainer Pierre Chifflier; long-standing pure-Rust X.509 parser used across the `rusticata`/network-forensics tooling ecosystem | Standard `cargo`/`crates.io` build, no vendored binaries | No independent formal audit found; moderate reverse-dependency count, but a focused, single-purpose parser rather than a large attack surface | No advisories against `x509-parser` in the RustSec database. `cargo audit`/`cargo deny check` run clean against 0.18.1 at time of adding (2026-08-26, T-48 batch); no new license-allowlist gap (`MIT OR Apache-2.0`, already allowed) | **`[dev-dependencies]` only — never compiled into a shipped binary.** Used solely in `cert.rs`'s unit tests to empirically decode the DER `rcgen` produces and assert on the actual typed `SubjectAlternativeName`/`BasicConstraints`/`KeyUsage`/validity fields, rather than trusting `rcgen`'s behavior from its docs alone (same "verify empirically" discipline as this codebase's other gotchas, e.g. the `Name::from_utf8` wildcard-label case) |

Planned crates and the reasoning for choosing each are listed in `SPEC.md` §"Технічний стек" and
`CLAUDE.md` §"Planned stack" — that's a design-time rationale, not a substitute for this vetting
table once a specific version is actually pinned.
