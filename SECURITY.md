# Security

Threat model summary, hard constraints, and dependency-vetting table. Full reasoning behind each
item lives in `SPEC.md` — this file tracks the current state, `SPEC.md` explains why.

## Threat model (summary — see SPEC.md §"Наскрізні вимоги", §2, §8.1 for full reasoning)

- **Local DoH cert compromise** — the largest single attack surface in the project. Mitigated by
  using a single self-signed leaf certificate on `127.0.0.1` (never a local CA): a compromised
  private key spoofs localhost only, not arbitrary domains. See SPEC.md §2. **Current storage
  posture (T-67, 2026-08-31):** the private key is held in the OS secret store — Windows
  Credential Manager via the `keyring` crate (`key_store.rs`), keyed on an entry name derived
  from the app-data directory (`dns-quorum-filter` / `doh-tls-private-key:<sha1(dir)[..8]>`) so a
  scratch/isolated instance never collides with the real one. Only the public `cert.pem` is on
  disk. A pre-T-67 install's plaintext `key.pem` is picked up once by
  `cert::migrate_legacy_key_into_store` (decode → store → zero-and-unlink the file), keeping the
  existing trusted cert. keyring's `windows-native-keyring-store` contains the Win32 FFI, so
  `dnsqb-service`'s crate-wide `#![forbid(unsafe_code)]` stays intact. The two-phase `icacls.exe`
  ACL helpers went out with the last plaintext secret file when **T-163** moved the MaxMind
  credentials into the OS store too — nothing this crate writes to disk is secret any more.
  macOS Keychain / Linux Secret Service are abstracted by `keyring` but unverified here
  (no build/test access) — **T-71**.
- **MaxMind account id + license key (T-80 / T-163).** The opt-in MaxMind GeoLite2 advanced mode's
  credentials are held in the OS secret store (`key_store::maxmind_credentials_entry`,
  `maxmind-credentials:<sha1(dir)[..8]>`) as a small JSON blob — the same `keyring` primitive as
  the TLS key, resolved once at startup and refreshed at runtime (T-163). A pre-T-163 install's
  plaintext `%LOCALAPPDATA%\dns-quorum-filter\geoip_maxmind.toml` (a dedicated file, never a table
  in `resolver_config.toml` — see `geoip_credentials.rs`'s module doc for why) is migrated once by
  `geoip_credentials::migrate_legacy_credentials_file` (parse → store → zero-and-unlink), then the
  file is gone. Leak surface while in memory / in transit is closed by: `LicenseKey`'s redacting
  `Debug` + no `Display` / no `Serialize`; the download using an `Authorization: Basic` header,
  never a URL query parameter; `reqwest` 0.13 stripping `Authorization` on the cross-host redirect
  to Cloudflare R2 (verified in the vendored `redirect.rs`); and every MaxMind `reqwest::Error`
  being mapped to a coarse label, never logged via `Display`.
- **~~Tauri IPC boundary (webview → Rust backend)~~ — historical, the channel no longer exists
  (T-149, SPEC.md "Відкриті питання" п.13, DECISIONS.md).** `crates/dnsqb-ui` is deleted; no
  process embeds a webview or exposes Rust functions to JS via `#[tauri::command]` anymore. The
  underlying principle (explicit allowlisted surface, no raw internal structs exposed, four test
  categories not one smoke test — SPEC.md §8.1) carries forward to the admin HTTP channel below,
  not lost with the removal.
- **Admin HTTP channel (browser/tray → `dnsqb-service`, loopback, T-52/T-149)** — the exposed route
  set is a structural allowlist, not just documentation: `dispatch::ROUTES` is the actual table
  `serve()` dispatches from (checked before the handler-selection `match`), so a path/method pair
  not listed there can never reach a handler no matter what arm a future `match` edit adds
  (T-59, verified empirically — an unlisted arm added to the `match` without a `ROUTES` entry stays
  unreachable; `serve_matches_the_documented_admin_route_allowlist` fails if `ROUTES` itself drifts
  from its own hand-written expected copy). Every mutating
  route (`POST /admin/config`, `POST /admin/reset`, `POST /admin/shutdown`) requires
  `Content-Type: application/json` (`dispatch::content_type_is_json`) as its whole CSRF defense:
  not a CORS-simple content type, so a cross-origin write forces a preflight this service never
  answers. DNS-rebinding is closed independently by the leaf cert's narrow SAN set
  (`127.0.0.1`/`::1`/`localhost` only, T-48), not by this gate. `POST /admin/reset` (T-149) reloads
  both on-disk TOML files and clears the cache + query log — a malformed file on disk fails closed
  (500, live state untouched), never a partial apply. **`POST /admin/shutdown` (T-149) is the
  highest blast-radius route on this whole channel** — it terminates the entire `dnsqb-service`
  process (a graceful drain via `hyper_util::server::graceful::GracefulShutdown`, never
  `std::process::exit`), meaning DNS resolution for the whole machine goes silently unfiltered
  (the browser falls back to system DNS, SPEC.md "Відкриті питання" п.10) until a human manually
  restarts the service — `dnsqb-watcher` (Фаза 3) is the only thing that would ever auto-recover
  this, and it doesn't exist yet. Reachable only from this same loopback-only, CSRF-gated,
  cert-pinned channel; the one shipped caller (`dnsqb-tray`'s "Зупинити фільтрацію" menu item)
  gates it behind a native confirm dialog naming this exact consequence before ever sending the
  request. The embedded web UI (`GET /admin/ui`, T-149) ships `Content-Security-Policy: default-src
  'self'; frame-ancestors 'none'` — the latter specifically to keep the page from becoming
  iframe-able/clickjackable once T-49 installs the cert and the current incidental
  untrusted-cert protection against framing goes away.
- **Upstream DoH providers and DNS wire format** — parsed via `hickory-dns`, not a hand-rolled
  parser, specifically because this is historically the highest-CVE-density code class in C-based
  DNS software.
- **GeoIP / top-sites data feeds** — binary blobs that influence blocking decisions; atomic
  replacement is gated on TLS + checksum verification before swapping in a new file (SPEC.md §3.5).
  T-80's MaxMind advanced mode fetches a Basic-auth'd `.tar.gz` from `download.maxmind.com` and
  extracts the `.mmdb` member in memory (`tar` crate, read-only, bounded) before the same atomic
  swap; a `.tar.gz.sha256` sidecar is verified opportunistically (present mismatch → hard fail,
  absent → the TLS + gzip-CRC + structural-parse fallback).
- **Browser silent DoH-fallback** — open, not fully closed: if `dnsqb-service` errors or times out,
  the browser may silently fall back to system DNS, bypassing quorum entirely with no built-in
  mitigation in the current design (SPEC.md, Відкриті питання, п.10).

## Hard constraints (see SPEC.md §"Наскрізні вимоги" for the full list)

- Listens only on `127.0.0.1` — never `0.0.0.0`, no network listener beyond loopback.
- `#![forbid(unsafe_code)]` in every crate where possible; any `unsafe` needs an explicit
  justification comment.
- Domain names live only in the in-memory ring buffer by default; disk persistence of the query log
  is opt-in and must be encrypted via platform secure storage. Diagnostic/service logs never contain
  domain names.
- Elevated privileges only once, for the one-time trust-store install/uninstall step — the main
  process runs as a normal user, never with standing elevated privileges.
- CI must run `cargo audit` (known CVEs) and `cargo deny` (license/duplicate checks) as required
  steps, not optional ones. A CodeQL SAST scan (`.github/workflows/codeql.yml`, language `rust`,
  `build-mode: none`) runs on every push/PR — its alerts are triaged and fixed in the same pass,
  not left open because the other gates are green (T-101).
- Reproducible builds / signed release binaries — the app installs a trusted certificate into the
  system, so binary trust is as security-critical as the certificate itself. Built (Батч 3.7,
  T-100/T-102/T-103): pinned toolchain + tracked `Cargo.lock` + `--locked` everywhere +
  `/Brepro` + `codegen-units = 1`, verified by a blocking CI job that clean-builds twice in
  different paths and compares SHA-256 (proven on the pinned toolchain + `windows-latest` runner
  image; a different Windows SDK could change `link.exe` output — untested). A third party on the
  same toolchain/SDK can rebuild a tag's source and confirm the unsigned binaries byte-for-byte. **CI signing is `test-signed` with an ephemeral certificate —
  not trusted; production trust comes only from Microsoft Store re-signing the MSIX at
  publication** (a real cert can be supplied as the `CODESIGN_PFX` secret to sign strictly
  instead). The `v*`-tag release job re-proves reproducibility before it publishes a **draft**
  release; a human publishes it.
- MSIX packaging (Батч 3.8, T-156) — `runFullTrust`, not an AppContainer sandbox: the app already
  needs ordinary Win32 access outside any package (spawning siblings, `CurrentUser\Root`,
  Credential Manager), and grants no capability beyond that (no hidden MITM/proxy — this is a DoH
  filter the browser is explicitly pointed at). The `.msix` signature must match `<Identity
  Publisher>` exactly (`packaging/pack-msix.ps1` derives both from one `-Publisher` value, never
  two literals). **The sideload trust certificate is a *different* certificate from the one the
  running app installs for `127.0.0.1` DoH traffic** — trusting one does not trust the other, and
  the two must never be conflated in operator-facing instructions. Confirmed empirically
  (2026-09-04): `Cert:\CurrentUser\TrustedPeople` is *not* sufficient for `Add-AppxPackage`
  (`0x800B0109`) — sideloading needs `Cert:\LocalMachine\Root` or `\LocalMachine\TrustedPeople`,
  both requiring an elevated session, which is itself a real (if one-time, install-only) elevation
  cost this project's "no persistent elevated privileges" principle doesn't otherwise carry.
- **T-70 residual risk, MSIX-specific**: MSIX has no uninstall-time code hook at all — the OS just
  deletes the package's files, nothing runs afterward. `local_state::remove_all` (tray "Повністю
  видалити" / `/admin/ui`'s danger-zone card) is therefore an **in-app, user-triggered** action
  that must run *before* the package is removed, not an automatic cleanup step. If a user removes
  the app from Windows Settings without running it first, the trusted certificate and Credential
  Manager secrets are left behind — the same class of bug as any other left-behind trusted cert,
  but not structurally preventable under MSIX's model. Stated, not silently assumed away.

## Dependency vetting

When a crate is added, add a row here **before merging**, not after:

| Crate | Maintainer | Reproducible build? | Independent audit / reputation | CVE history | Notes |
|---|---|---|---|---|---|
| `hickory-proto` 0.26.1 | `hickory-dns` project (formerly `trust-dns`); maintainers include Dirkjan Ochtman (also rustls, RustSec core team), divergentdave, marcus0x62 | Standard `cargo`/`crates.io` build, no vendored binaries | First formal security audit funded/performed by Ferrous Systems; production use includes Let's Encrypt's recursive resolver and (per public reporting) Google Pixel | Past DNSSEC-validation CVEs (e.g. CVE-2025-25188, unbounded-loop/memory-exhaustion class), fixed in later releases; `cargo audit` run clean against 0.26.1 at time of adding (2026-08-25) — re-verify on every `cargo audit` CI run, don't treat this row as a standing guarantee | Chosen per SPEC.md "Технічний стек" specifically to avoid hand-rolling a DNS wire-format parser (highest-CVE-density code class in C-based DNS software); only the `-proto` sub-crate is used in Крок 0 (wire-format types for conformance-test fixtures), not the full resolver |
| `tokio` 1.53.1 | `tokio-rs` org, founding maintainer Carl Lerche; foundational async runtime for a large fraction of the Rust async ecosystem, backed by multiple large-org sponsors | Standard `cargo`/`crates.io` build, no vendored binaries | De facto standard async runtime; formal `security@tokio.rs` disclosure process, advisories coordinated through GitHub/RustSec | Historical advisories cluster in `io-util`/channel APIs and legacy 0.1-era sibling crates (`tokio-reactor`, `tokio-timer`, both unmaintained, not depended on here); `cargo audit` run clean against 1.53.1 at time of adding (2026-08-25, T-20–T-26 batch) — re-verify on every CI run | Async runtime for concurrent upstream queries (SPEC.md "Технічний стек"); `dnsqb-service` declares `rt-multi-thread`, `macros`, `net`, `sync`, `time` — `sync` added T-149 (`tokio::sync::watch`, `/admin/shutdown`'s graceful-shutdown signal). `cargo tree -f "{p} {f}" -p tokio` confirms the resolved workspace feature set as `bytes,default,fs,io-util,libc,macros,mio,net,rt,rt-multi-thread,socket2,sync,test-util,time,tokio-macros,windows-sys` (2026-08-27) — the extras beyond this crate's own declaration come from unification with other workspace members'/dev-dependencies' own requests (e.g. `test-util` from this crate's own `[dev-dependencies]`), not from `sync` itself; still no `full` |
| `reqwest` 0.13.4 | Sean McArthur (`seanmonstar`); widely used, foundational Rust HTTP client | Standard `cargo`/`crates.io` build, no vendored binaries; TLS backend (`rustls`) itself audited independently (see rustls project) | Most widely depended-on Rust HTTP client; `rustls`-backed builds avoid the system OpenSSL dependency the "Технічний стек" section explicitly rules out | `cargo audit` run clean against 0.13.4 and its `rustls`/`aws-lc-rs` TLS stack at time of adding (2026-08-25) — re-verify on every CI run | `default-features = false`, features `rustls` + `http2` only — no `native-tls`, no `blocking`. Pulled `aws-lc-rs`/`aws-lc-sys` as the `rustls` crypto backend and `webpki-root-certs`/`rustls-platform-verifier` for trust-store validation, each vetted via `cargo deny`'s license check (`deny.toml`), not this table, since none is a direct dependency this project chose. `json` added since (T-52, `admin::AdminClient`) and `stream` added T-75 (`Response::bytes_stream()` — `geoip_updater::fetch_bounded`'s chunk-by-chunk size-bounded download; pulls in `tokio-util`/`wasm-streams`, the latter `cfg`'d to `wasm32` and outside this project's `deny.toml` `[graph] targets` restriction) — this row's own feature list left un-rewritten at those points, noted here instead per "never silently deprecate" |
| `futures-util` 0.3.34 | `rust-lang` org (`futures-rs` project); foundational async-combinator crate, widely depended on across the ecosystem (also pulled transitively by `reqwest`/`hyper`) | Standard `cargo`/`crates.io` build, no vendored binaries | Long-standing, heavily reviewed crate; no independent formal audit found, but extremely high reverse-dependency count makes it de-facto scrutinized | Two historical advisories, both fixed years before this version and irrelevant to the API surface used here: RUSTSEC-2020-0059 (`MutexGuard::map` Send/Sync unsoundness) and RUSTSEC-2020-0062 (unsound `Sync` on `FuturesUnordered`, fixed 0.3.5) — neither `Mutex` type is used by this project. `cargo audit`/`cargo deny check` run clean against 0.3.34 at time of adding (2026-08-25, T-27–T-31 batch) | `default-features = false`, feature `std` only — pulled specifically for `FuturesUnordered`/`StreamExt`, not the full `futures` crate (SPEC.md §3.6's early-return/cancellation, T-30) |
| `tracing` 0.1.44 | `tokio-rs` org; same maintainer group as `tokio` | Standard `cargo`/`crates.io` build, no vendored binaries | Facade/macro crate for the `tokio`-ecosystem's de-facto standard structured-logging framework; shares `tokio`'s `security@tokio.rs` disclosure process | No advisories against the `tracing` facade crate itself. RUSTSEC-2025-0055 (ANSI-escape log injection) is against `tracing-subscriber`, which is now a direct dependency (T-143, see its own row below) — re-checked as promised: `cargo audit` runs clean against `tracing-subscriber` 0.3.23 (the advisory's affected range doesn't include this version). `cargo audit` run clean at time of adding (2026-08-25) | Diagnostic-only logging (SPEC.md, Наскрізні вимоги — no domain names in service logs); `error_kind()` in `quorum.rs` deliberately avoids logging `UpstreamError`'s own `Display` for the `Http` variant, since `reqwest::Error`'s message embeds the failed request URL (which encodes the query, i.e. the domain). Before `main.rs` wired a real subscriber (T-143), every existing `tracing::` call site was grepped and confirmed to interpolate no domain name — `cache.rs`'s one site is a fixed string, `quorum.rs`'s sites use the `Provider` enum and the already-coarse `error_kind()` label, never raw error text |
| `moka` 0.12.16 | `moka-rs` org, maintainer `tatsuya6502`; production use includes crates.io's own API service (cache in front of PostgreSQL, ~85% hit rate on the download endpoint) | Standard `cargo`/`crates.io` build, no vendored binaries | Not part of a large sponsored foundation like `tokio-rs`/`rust-lang`, but has real production adoption beyond its own test suite; design is a Rust port of Java's well-established Caffeine cache | No advisory filed against `moka` itself in the RustSec database. Changelog shows the project proactively bumping transitive deps ahead of advisories against *them* (`crossbeam-epoch` for CVE-2022-23639, `crossbeam-channel` to 0.5.15 for RUSTSEC-2025-0024) — a maintenance-health signal, not a vulnerability in `moka`. `cargo audit`/`cargo deny check` run clean against 0.12.16 and its full transitive tree at time of adding (2026-08-25, T-32/T-34/T-36 batch); `cargo deny check` reported no new license-allowlist gap | `default-features = false`, feature `future` only (SPEC.md §4: async-compatible concurrent cache, per-entry TTL via the `Expiry` trait, T-32). Not yet wired to the request pipeline — see `cache.rs`'s module doc comment and TASKS.md T-39 |
| `serde` 1.0.229 + `serde_json` 1.0.151 | `serde-rs` org, maintainer David Tolnay (also `syn`/`thiserror`, prolific/high-reputation Rust ecosystem maintainer); the de-facto standard Rust (de)serialization framework | Standard `cargo`/`crates.io` build, no vendored binaries | Extremely high reverse-dependency count (already pulled in transitively via `reqwest`/`hickory-proto` before this batch made it a direct dependency); no independent formal audit found, but among the most heavily reviewed crates in the ecosystem | No advisories against either crate in the RustSec database. `cargo audit`/`cargo deny check` run clean against 1.0.229/1.0.151 at time of adding (2026-08-26, T-37 batch) | `serde` with `derive` only. Direct dependency now for the override-list file's on-disk JSON shape (`overrides.rs`, T-37) — not spent solely there: the same crate is needed regardless for T-53's admin-channel DTO layer (`diagrams/ui-dto-model.md`; the channel itself moved from Tauri IPC to plain HTTP JSON at T-149, the DTO-typing need is unchanged), so introducing it now is for its long-term purpose, not a one-off parser |
| `tempfile` 3.27.0 (dev-only) | `Stebalien` (Steven Allen); long-standing, widely used Rust temp-file/dir crate | Standard `cargo`/`crates.io` build, no vendored binaries | High reverse-dependency count; not part of a large sponsored foundation but long track record | No advisories against this crate in the RustSec database. `cargo audit`/`cargo deny check` run clean at time of adding (2026-08-26, T-37 batch) | **`[dev-dependencies]` only — never compiled into a shipped binary.** Used solely in `overrides.rs`'s unit tests for `OverrideLists::load`'s file-reading path (RAII cleanup via `NamedTempFile`'s `Drop`, not manual `fs::remove_file`) |
| `parking_lot` 0.12.5 (+ `parking_lot_core` 0.9.12, `lock_api` 0.4.14) | Amanieu d'Antras; long-standing, widely used synchronization-primitives crate (also maintains `hashbrown`, in `rustc`'s own toolchain) | Standard `cargo`/`crates.io` build, no vendored binaries | High reverse-dependency count across the ecosystem, including `rustc` itself in some configurations; no independent formal audit found but long track record and heavy real-world scrutiny | No advisories against `parking_lot`/`parking_lot_core`/`lock_api` in the RustSec database. `cargo audit`/`cargo deny check` run clean against 0.12.5 at time of adding (2026-08-26, T-42/T-43 batch); no new license-allowlist gap (MIT/Apache-2.0, already allowed) | SPEC.md §6.1's explicit choice for `query_log.rs`'s ring buffer — a `std`-lock-free, non-`tokio` `RwLock` for an in-memory structure with no `.await` inside any critical section (a `tokio::sync::RwLock` would add unneeded overhead for a lock never held across an await point) |
| `rcgen` 0.14.9 | `rustls` org (same org as the `rustls` TLS crate already a transitive dependency via `reqwest`); actively maintained, used by `rustls`'/`quinn`'s own test suites and widely as a self-signed-cert generator | Standard `cargo`/`crates.io` build, no vendored binaries | No independent formal audit found specifically for `rcgen`, but high scrutiny by association — it's the de-facto standard Rust crate for exactly this use case (test/local self-signed certs) | No advisories against `rcgen` in the RustSec database. `cargo audit`/`cargo deny check` run clean against 0.14.9 at time of adding (2026-08-26, T-48 batch) and again with the `pem`/`zeroize` features added (2026-08-26, T-50 batch); no new license-allowlist gap either time (`MIT OR Apache-2.0`, already allowed) | SPEC.md §2's self-signed leaf certificate for the local `DoH` listener (`cert::generate_self_signed_cert`, T-48). `default-features = false, features = ["aws_lc_rs", "pem", "zeroize"]` — **not** the default `ring` feature — deliberately matches `reqwest`/`rustls`'s already-chosen `aws-lc-rs` crypto backend, avoiding a second directly-chosen crypto implementation in the binary (`ring` can still appear transitively via an Android-only `rustls-platform-verifier-android` dependency, which is not this project's choice and not active on the Windows target). **`pem`/`zeroize` added at T-50** (private-key disk persistence — `cert::write_cert_and_key_to_app_data`): `pem` gates `Certificate::pem()`/`KeyPair::serialize_pem()`, the actual PEM-encoding calls this task needs; `zeroize` gates `impl zeroize::Zeroize for KeyPair`, confirmed by reading the 0.14.9 source to wipe only the `KeyPair`'s internal DER bytes on an explicit `.zeroize()` call (no `ZeroizeOnDrop`) — the PEM `String` this project derives from that key is a separate value the feature does not cover, hence the project's own direct `zeroize` dependency (see its own row below) to wrap that text in `Zeroizing<String>`. **`IsCa::ExplicitNoCa` used, not the `NoCa` default** — the default omits the `BasicConstraints` extension entirely rather than encoding `cA=FALSE`, which would make this project's "leaf, never a CA" property (this file's own top-listed threat) unverifiable from the cert's actual bytes; caught by advisor review of the diff, not by the first-written tests, which passed either way |
| `x509-parser` 0.18.1 (dev-only) | `rusticata` org, maintainer Pierre Chifflier; long-standing pure-Rust X.509 parser used across the `rusticata`/network-forensics tooling ecosystem | Standard `cargo`/`crates.io` build, no vendored binaries | No independent formal audit found; moderate reverse-dependency count, but a focused, single-purpose parser rather than a large attack surface | No advisories against `x509-parser` in the RustSec database. `cargo audit`/`cargo deny check` run clean against 0.18.1 at time of adding (2026-08-26, T-48 batch); no new license-allowlist gap (`MIT OR Apache-2.0`, already allowed) | **`[dev-dependencies]` only — never compiled into a shipped binary.** Used solely in `cert.rs`'s unit tests to empirically decode the DER `rcgen` produces and assert on the actual typed `SubjectAlternativeName`/`BasicConstraints`/`KeyUsage`/validity fields, rather than trusting `rcgen`'s behavior from its docs alone (same "verify empirically" discipline as this codebase's other gotchas, e.g. the `Name::from_utf8` wildcard-label case); T-50 also uses its `pem` module (`parse_x509_pem`) to prove `Certificate::pem()`'s output decodes to the same DER as `cert.der()` |
| `zeroize` 1.9.0 | RustCrypto org; foundational memory-hygiene crate across the RustCrypto ecosystem, also a dependency of `rcgen` itself (previously transitive-only, now direct) | Standard `cargo`/`crates.io` build, no vendored binaries | Widely depended on across the Rust crypto ecosystem (RustCrypto's own primitives, `rcgen`, many others); no independent formal audit found specifically for this crate, but very high scrutiny by sheer reverse-dependency count | No advisories against `zeroize` in the RustSec database. `cargo audit`/`cargo deny check` run clean against 1.9.0 at time of adding (2026-08-26, T-50 batch); no new license-allowlist gap (`MIT OR Apache-2.0`, already allowed) | Default features only (`alloc`, no `std`/`derive`/`serde`/`simd`). Wraps in-memory copies of private-key bytes: `Zeroizing<Vec<u8>>` around the PKCS#8 DER in `cert::write_cert_and_key_to_app_data` (was PEM `String` at T-50; changed to DER at T-67 when the key stopped being written as a file) and around the migrated legacy `key.pem` bytes in `cert::migrate_legacy_key_into_store`; also `key_store::load_secret` returns `Zeroizing<Vec<u8>>` (both the TLS key and, since T-163, the MaxMind-credentials blob), and `.zeroize()` is called on the `KeyPair` after hand-off. **Best-effort in-memory hygiene, not a guarantee** — by the time these run the bytes have already reached the OS (page cache, possibly swap, and for the on-disk `cert.pem` the file itself); stated in the module doc, no test asserts "memory was wiped" (that would only prove `rcgen`/`zeroize`'s own implementation) |
| `rustls` 0.23.43 | `rustls` org; already a transitive dependency via `reqwest`'s TLS backend, now also a direct one | Standard `cargo`/`crates.io` build, no vendored binaries | Widely regarded as the leading memory-safe TLS implementation for Rust; independent audits published by the `rustls` project (Cure53, others) | No advisories against `rustls` 0.23.43 in the RustSec database. `cargo audit`/`cargo deny check` run clean at time of adding (2026-08-26, T-142 batch); no new license-allowlist gap (`Apache-2.0 OR ISC OR MIT`, `ISC` already allowed for this same TLS stack) | `default-features = false, features = ["aws_lc_rs", "std", "tls12"]` — confirmed via `cargo tree -f "{p} {f}" -p rustls` that this exactly matches the feature set `reqwest` already activates (no `ring`, no `logging`, no `prefer-post-quantum` added to the build). `tls::load_or_generate_server_config` (T-142) uses it to build the local `DoH` listener's `rustls::ServerConfig` from the persisted self-signed leaf cert (T-48/T-50) — every config is built via `builder_with_provider(aws_lc_rs::default_provider())`, never the plain `ServerConfig::builder()`, so this project's own choice of crypto provider is never left to ambient crate-feature auto-detection (see `tls.rs`'s own module doc comment) |
| `hyper` 1.11.0 | `hyperium` org, maintainer Sean McArthur (also `reqwest`); the foundational low-level Rust HTTP implementation, already a transitive dependency via `reqwest`'s client stack, now also a direct one | Standard `cargo`/`crates.io` build, no vendored binaries | Extremely high reverse-dependency count; widely regarded as the reference-quality Rust HTTP implementation, `tokio`-ecosystem-adjacent | No advisories against `hyper` 1.11.0 in the RustSec database. `cargo audit`/`cargo deny check` run clean at time of adding (2026-08-26, T-143 batch); no new license-allowlist gap (`MIT`, already allowed) | `dispatch::serve`/`main.rs`'s TCP accept loop and `DoH` request/response handling (T-143). Feature set now includes `server` on top of the `client, http1, http2` `reqwest` already activates — confirmed via `cargo tree -f "{p} {f}" -p hyper` that no other feature changed |
| `hyper-util` 0.1.20 | `hyperium` org; utility/glue crate for `hyper` 1.x (connection I/O adapters, the `auto` HTTP/1-vs-2 server builder), already a transitive dependency via `reqwest`, now also a direct one | Standard `cargo`/`crates.io` build, no vendored binaries | Maintained by the same org/team as `hyper` itself; used across the `hyper` 1.x ecosystem as the standard glue layer | No advisories against `hyper-util` 0.1.20 in the RustSec database. `cargo audit`/`cargo deny check` run clean at time of adding (2026-08-26, T-143 batch) and again with `server-graceful` added (2026-08-27, T-149 batch); no new license-allowlist gap either time (`MIT`, already allowed) | `default-features = false, features = ["http1", "http2", "server", "server-auto", "server-graceful", "tokio"]` — adds the server-side features on top of the `client, client-legacy, client-proxy, http1, http2, tokio` set `reqwest` already activates; `server-graceful` added T-149 for `hyper_util::server::graceful::GracefulShutdown` (confirmed via `cargo tree -f "{p} {f}" -p hyper-util`, re-run 2026-08-27 — no other feature changed). `hyper_util::server::conn::auto::Builder` negotiates HTTP/1.1 vs HTTP/2 per connection so the local `DoH` listener doesn't have to assume every client always selects h2 via ALPN against a self-signed leaf |
| `tokio-rustls` 0.26.4 | `rustls` org; the standard `tokio` integration for `rustls`, already a transitive dependency via `reqwest`, now also a direct one | Standard `cargo`/`crates.io` build, no vendored binaries | Same org/maintainers as `rustls` itself; the de-facto standard way to use `rustls` from `tokio` | No advisories against `tokio-rustls` 0.26.4 in the RustSec database. `cargo audit`/`cargo deny check` run clean at time of adding (2026-08-26, T-143 batch); no new license-allowlist gap (`MIT OR Apache-2.0`, already allowed) | `default-features = false, features = ["tls12"]` — `aws-lc-rs` resolves in automatically via this workspace's existing `rustls` feature choice (confirmed via `cargo tree -f "{p} {f}" -p tokio-rustls`, same discipline as every other `rustls`-family addition in this project). Terminates TLS on each accepted connection before handing it to `hyper-util`'s `auto` server builder (`main.rs`, T-143) |
| `http` 1.5.0 | `hyperium` org; the shared `Request`/`Response`/`StatusCode`/`Method` types `hyper` 1.x itself is built on, already a transitive dependency, now also a direct one | Standard `cargo`/`crates.io` build, no vendored binaries | Foundational, extremely high reverse-dependency count across the entire Rust HTTP ecosystem | No advisories against `http` 1.5.0 in the RustSec database. `cargo audit`/`cargo deny check` run clean at time of adding (2026-08-26, T-143 batch); no new license-allowlist gap (`MIT`, already allowed) | Default features. `dispatch.rs` names `http::StatusCode`/`Method`/`Request`/`Response`/`header` directly rather than reaching through a `hyper::` re-export, since `serve` (T-143) is generic over the request body type and this is the crate those types actually live in |
| `http-body-util` 0.1.5 | `hyperium` org; `hyper` 1.x's official body-handling utility crate (`Full`, `Empty`, `Limited`, `BodyExt`), already a transitive dependency, now also a direct one | Standard `cargo`/`crates.io` build, no vendored binaries | Maintained by the same org/team as `hyper`; the standard companion crate for `hyper` 1.x body handling | No advisories against `http-body-util` 0.1.5 in the RustSec database. `cargo audit`/`cargo deny check` run clean at time of adding (2026-08-26, T-143 batch); no new license-allowlist gap (`MIT`, already allowed) | Default features. `Full<Bytes>` builds every `DoH` HTTP response; `Limited::new(body, MAX_MESSAGE_SIZE)` wraps every POST request body **before** `.collect()` (`dispatch::serve`, T-143) — the size bound has to sit there, not after collection, or it checks the length of something already fully allocated instead of actually bounding the allocation (SPEC.md §8.1 — advisor review of the plan caught the first draft doing this the wrong way round) |
| `bytes` 1.12.1 | `tokio-rs` org; the standard reference-counted byte-buffer crate across the `tokio`/`hyper` ecosystem, already a transitive dependency, now also a direct one | Standard `cargo`/`crates.io` build, no vendored binaries | Foundational, extremely high reverse-dependency count | No advisories against `bytes` 1.12.1 in the RustSec database. `cargo audit`/`cargo deny check` run clean at time of adding (2026-08-26, T-143 batch); no new license-allowlist gap (`MIT`, already allowed) | Default features. `dispatch.rs` builds every response body as `Full::new(Bytes::from(...))`; named directly rather than via `hyper::body::Bytes`'s re-export for clarity |
| `tracing-subscriber` 0.3.23 | `tokio-rs` org; same maintainer group as `tokio`/`tracing` | Standard `cargo`/`crates.io` build, no vendored binaries | De-facto standard `tracing` subscriber implementation across the `tokio` ecosystem | RUSTSEC-2025-0055 (ANSI-escape log injection via unsanitized log content) exists against this crate's affected version range, but not 0.3.23 — `cargo audit` runs clean against it (re-checked specifically for this advisory, not just a blanket clean run; see the `tracing` row above, which named this exact check as owed when a subscriber was eventually added). `cargo audit`/`cargo deny check` run clean overall at time of adding (2026-08-26, T-143 batch); no new license-allowlist gap (`MIT`, already allowed) | Default features, `tracing_subscriber::fmt::init()` called once in `main.rs` (T-143) — the first real subscriber this project has ever wired, making every existing `tracing::` call site's output live for the first time. Every existing call site was grepped and confirmed not to interpolate a domain name before this was added (see the `tracing` row's note) |
| `toml` 1.1.4 (+ `toml_parser`, `toml_writer`, `toml_datetime`, `serde_spanned`, `winnow`) | `toml-rs` org; the de-facto standard Rust TOML parser, used across the Cargo ecosystem itself (`Cargo.toml` parsing) | Standard `cargo`/`crates.io` build, no vendored binaries | Extremely high reverse-dependency count (Cargo's own manifest parser uses this same crate family); no independent formal audit found, but very high scrutiny by sheer ubiquity | No advisories against `toml`/`winnow`/the `toml_*` split crates in the RustSec database. `cargo audit`/`cargo deny check` run clean at time of adding (2026-08-26, T-145 batch); no new license-allowlist gap (all `MIT OR Apache-2.0` except `winnow`, plain `MIT` — both already allowed) | Default features (`display`, `parse`, `serde`, `std`). Replaces `serde_json` as the on-disk format for `config.rs`'s `ResolverConfig`/`overrides.rs`'s `OverrideLists` (T-145, DECISIONS.md — comment support for hand-edited config, `ssh_config`/`my.cnf`-style). `overrides.rs`'s parse-error variant deliberately holds no `toml::de::Error` payload — its `Display`/`Debug` render an annotated snippet of the offending input line, confirmed empirically, and `overrides.toml` is nothing but domain names (SPEC.md "Наскрізні вимоги"); `config.rs`'s `resolver_config.toml` carries no domains, so its error variant keeps the payload |

| `tray-icon` 0.21.3 (+ `muda` 0.17.2, its menu dependency, consumed via `tray_icon::menu::*`'s re-export — no direct `muda` dependency) | `tauri-apps` org — the same publisher this project previously vetted for `tauri`/`tauri-build` (that row removed T-149, `crates/dnsqb-ui` deleted, DECISIONS.md) | Standard `cargo`/`crates.io` build, no vendored binaries | `tauri-apps` runs its own security advisory process; `tray-icon`/`muda` are the tray/menu primitives Tauri's own desktop tray feature is built on, so real production usage beyond this project | No advisories against `tray-icon`/`muda` 0.21.3/0.17.2 in the RustSec database. `cargo deny check` run clean against the full tree at time of adding (2026-08-27, T-149 batch, Крок 0 probe) — confirmed via a throwaway scratch build+run before committing to this pair, same "verify empirically before relying on it" discipline as every other unfamiliar-crate-behavior claim in this project. **No new `deny.toml` entries needed** — every crate in the tree resolves to an already-allowed license (`MIT OR Apache-2.0` for both) | `dnsqb-tray`'s tray icon + right-click menu (T-149, replacing `dnsqb-ui`'s Tauri window). `muda`'s global `MenuEvent` channel isn't wired into `tao`'s own event delivery on Windows — drained on a short fixed poll tick inside `tao`'s event loop instead (`main.rs`'s own module doc comment) |
| `tao` 0.31.1 | `tauri-apps` org — same publisher as `tray-icon`/`tauri` above; a maintained fork of the `winit` windowing/event-loop crate | Standard `cargo`/`crates.io` build, no vendored binaries | Same org/advisory process as `tauri`; used as `tauri`'s own underlying windowing layer, so real production usage | No advisories against `tao` 0.31.1 in the RustSec database. `cargo deny check` run clean at time of adding (2026-08-27, T-149 batch); no new license-allowlist gap (`Apache-2.0`, already allowed) | Owns `dnsqb-tray`'s main thread — `EventLoop::run` never returns, so the admin-channel status poll lives on a separate OS thread with its own `tokio` runtime instead (`status.rs`), never blocking `tao`'s own message pump |
| `rfd` 0.15.4 | `PolyMeilex` (Poly); Rust File Dialog — a thin safe wrapper over each OS's native dialog APIs, no bundled UI toolkit of its own | Standard `cargo`/`crates.io` build, no vendored binaries; `default-features = false` on this project's declaration, so the Linux-only XDG-portal/`ashpd`/`async-std` backend isn't even pulled into the dependency tree on this Windows-only target | Widely used for exactly this "native file/message dialog" niche across the Rust desktop-app ecosystem; no independent formal audit found, but a narrow, single-purpose wrapper crate, not a large attack surface | No advisories against `rfd` 0.15.4 in the RustSec database. `cargo deny check` run clean at time of adding (2026-08-27, T-149 batch); no new license-allowlist gap (`MIT`, already allowed) | The one native confirm dialog on this project's highest-blast-radius action — "Зупинити фільтрацію" (`dnsqb-tray`'s `confirm_stop_filtering`) uses `rfd::MessageDialog`'s synchronous `MessageButtons::YesNo` API to name the shutdown consequence before `POST /admin/shutdown` is ever sent; also the (network-free) "Про програму" about dialog |
| `proptest` 1.11.0 (dev-only) | `proptest-rs` org; the de-facto standard Rust property-based testing crate, a QuickCheck-style port with shrinking | Standard `cargo`/`crates.io` build, no vendored binaries | High reverse-dependency count across the ecosystem for exactly this niche; no independent formal audit found, but long track record | No advisories against `proptest` or its resolved transitive tree (`rand`/`rand_chacha`/`rand_core`/`rand_xorshift`, `regex-syntax`, `bitflags`, `num-traits`, `unarray`, `zerocopy`, `getrandom`, `ppv-lite86`) in the RustSec database. `cargo audit`/`cargo deny check` run clean at time of adding (2026-08-28, T-58 batch); no new license-allowlist gap (all `MIT OR Apache-2.0` or `MIT`, already allowed) | **`[dev-dependencies]` only — never compiled into a shipped binary.** First real use in the workspace (named in CLAUDE.md's planned-stack table since Крок 0, never wired up until T-58's fuzz/property-test pass). `default-features = false, features = ["std"]` — deliberately excludes the default `fork`/`timeout` features, which pull in `rusty-fork` (process-spawning), `wait-timeout`, and `quick-error`; confirmed via `cargo tree -f "{p} {f}" -p proptest` that none of those appear with this feature selection. Property tests: `overrides::parse_pattern` (arbitrary strings never panic, a parsed domain never contains `*`), `dispatch::wire_bytes_from_get` (arbitrary query strings never panic), and `dispatch::serve` against arbitrary `/admin/config` POST bodies (the whole CSRF-gate→body-limit→JSON-decode→live-apply path never panics) |
| `maxminddb` 0.30.3 (+ `ipnetwork` 0.21.1) | `oschwald`; the long-maintained de-facto standard Rust `MaxMind` `DB`/`GeoIP2` reader — the crate `SPEC.md`'s own "Технічний стек" table names by name | Standard `cargo`/`crates.io` build, no vendored binaries | Narrow, single-purpose reader crate widely used across the Rust GeoIP-tooling ecosystem; no independent formal audit found | No advisories against `maxminddb`/`ipnetwork` in the RustSec database. `cargo audit`/`cargo deny check` run clean at time of adding (2026-08-29, T-74 batch); no new license-allowlist gap (`ISC` for `maxminddb`, already allowed for the `rustls`/`aws-lc-rs` stack; `MIT OR Apache-2.0` for `ipnetwork`, already allowed) | `default-features = false` — the `mmap`/`simdutf8`/`unsafe-str-decode` features are all opt-in and left off, so the crate reads a whole database file into an owned `Vec<u8>` rather than memory-mapping it; verified by reading the vendored source (`reader.rs`) before relying on it, per this project's own discipline — this is what keeps `#![forbid(unsafe_code)]` true for `dnsqb-service` without needing an exception for this dependency (the crate's own internals are outside that forbid's scope regardless, but the *reason* to prefer the non-`mmap` mode is this project's own no-`unsafe` posture, not merely "the default happens to work"). `geoip.rs`'s `GeoipReader` (T-74) is a pure `IpAddr → Option<country code>` lookup, no pipeline wiring yet (T-76) |
| `flate2` 1.1.9 | `rust-lang` org (the `flate2-rs` project); the de-facto standard Rust gzip/deflate/zlib crate | Standard `cargo`/`crates.io` build, no vendored binaries | Extremely high reverse-dependency count; `rust-lang`-org-maintained | No advisories against `flate2`/`miniz_oxide` in the RustSec database. `cargo audit`/`cargo deny check` run clean at time of adding (2026-08-29, T-75 batch); no new license-allowlist gap (`MIT OR Apache-2.0` for both, already allowed) | `default-features = false, features = ["miniz_oxide"]` — the crate's own default backend, a pure-Rust port of `miniz.c` with no `unsafe` and no C toolchain dependency (confirmed via the crate's own docs before choosing it, not assumed), keeping `#![forbid(unsafe_code)]` intact the same way `maxminddb`'s feature choice does above. `geoip_download::decompress_bounded` (T-75) decompresses a downloaded `.mmdb.gz` bounded to a fixed cap, reading the stream to its own real EOF so `flate2`'s trailer CRC32/ISIZE check always runs on anything not already rejected as oversized |
| `sha1` 0.10.7 (+ `digest`, `block-buffer`, `crypto-common`, `cpufeatures`, `generic-array`, `typenum`) | RustCrypto org; the standard Rust SHA-1 implementation, part of the same `hashes` workspace as `sha2`/`md-5`/etc. | Standard `cargo`/`crates.io` build, no vendored binaries | RustCrypto is a widely-used, actively maintained org for exactly this niche (cryptographic/hash primitives), high reverse-dependency count | No advisories against `sha1`/`digest`/`cpufeatures`/`generic-array`/`typenum`/`crypto-common`/`block-buffer` in the RustSec database. `cargo audit`/`cargo deny check` run clean at time of adding (2026-08-29, T-75 batch); no new license-allowlist gap (all `MIT OR Apache-2.0`, already allowed). Introduces a second `cpufeatures` version in the tree alongside the one `chacha20`/`rand` already pull in — `cargo deny`'s `multiple-versions` is `warn`, not `deny`, so this doesn't fail the gate | `geoip_updater::sha1_hex`/`checksum_matches` (T-75) — verifies an opportunistically-fetched `.sha1` sidecar against a downloaded `GeoIP` database before trusting it. SHA-1 chosen to match what `db-ip.com`'s own download page is confirmed (via live web search) to publish, not for cryptographic collision resistance — the threat model here is cross-host consistency (this project's dev sandbox couldn't confirm the sidecar even exists; see `geoip_updater.rs`'s own module doc comment), not defense against a deliberate, resourced adversary computing a SHA-1 collision |

| `sha2` 0.10.9 (shares `digest`/`block-buffer`/`crypto-common`/`cpufeatures`/`generic-array`/`typenum` with `sha1` above) | RustCrypto org; the standard Rust SHA-2 implementation, same `hashes` workspace as `sha1` (already a direct dependency) | Standard `cargo`/`crates.io` build, no vendored binaries | Same org/scrutiny as the `sha1` row above; very high reverse-dependency count | No advisories against `sha2` in the RustSec database. `cargo audit`/`cargo deny check` run clean at time of adding (2026-08-30, T-80 batch); no new license-allowlist gap (`MIT OR Apache-2.0`, already allowed). Adds no new shared sub-crate versions — the `digest`/`cpufeatures`/etc. crates are the exact ones `sha1` already pulls in | `geoip_updater::sha256_hex`/`checksum_matches_sha256` (T-80) — verifies MaxMind's `?suffix=tar.gz.sha256` sidecar against the downloaded GeoLite2 `.tar.gz` when it's present (opportunistic, a present mismatch hard-fails; absent falls back to the TLS + gzip-CRC + structural-parse gate, same resolution as the DB-IP `.sha1` path). SHA-256 (not SHA-1) because that's the digest MaxMind's endpoint is documented to publish |
| `tar` 0.4.46 (+ `filetime` 0.2.29) | `tar-rs` (originally Alex Crichton; repo now under the `composefs` org), the de-facto standard Rust tar reader/writer | Standard `cargo`/`crates.io` build, no vendored binaries | Extremely high reverse-dependency count; long-standing single-purpose archive crate | No advisories against `tar`/`filetime` in the RustSec database. `cargo audit`/`cargo deny check` run clean at time of adding (2026-08-30, T-80 batch); no new license-allowlist gap (`MIT OR Apache-2.0` for both, already allowed) | `default-features = false` — drops the unix-only `xattr` tail (already excluded anyway by `deny.toml`'s `targets = ["x86_64-pc-windows-msvc"]`). Used **read-only**, in memory, by `geoip_download::extract_mmdb_from_tar_gz` (T-80): decompress the GeoLite2 `.tar.gz` (bounded by `decompress_bounded` to `MAX_GEOIP_DECOMPRESSED_BYTES`, which transitively bounds the `.mmdb` member — it's always smaller than the whole tar), then select and read the single `.mmdb` member. No entry is ever written to a path derived from the archive — bytes go to memory and the caller's own fixed `geoip.mmdb` — so no path-traversal surface. Chosen over a hand-rolled 512-byte-header parser deliberately: untrusted network input into a hand-written archive parser is the code class `hickory-dns` was picked to avoid (SPEC.md §"Технічний стек") |
| `keyring` 4.2.0 (+ `keyring-core` 1.0.0, `windows-native-keyring-store` 1.1.0) | `keyring-rs` project (`hwchen` / `brotskydotcom`); the de-facto standard cross-platform Rust secret-store wrapper (macOS Keychain / Windows Credential Manager / Linux Secret Service·keyutils) | Standard `cargo`/`crates.io` build, no vendored binaries | Narrow, single-purpose wrapper; high reverse-dependency count across CLI tooling; no independent formal audit found | No advisories against `keyring`/`keyring-core`/`windows-native-keyring-store` (or their transitive `byteorder`/`regex` on the Windows target) in the RustSec database. `cargo audit`/`cargo deny check` run clean at time of adding (2026-08-31, T-67); no new license-allowlist gap (all `MIT OR Apache-2.0` / `Unlicense OR MIT` → resolves to already-allowed). The `v1` feature also lists the target-gated Unix/Apple store crates (`zbus-secret-service-keyring-store`, `apple-native-keyring-store`, …). They land in `Cargo.lock` and never **compile** for `x86_64-pc-windows-msvc`; `cargo deny` also never evaluates them (`deny.toml [graph] targets`, the same situation the removed `tauri` row created). **`cargo audit` is different** — it reads `Cargo.lock` wholesale with no graph-target filter, so a future advisory against `zbus`/`zvariant`/`secret-service`/`serde_repr`/etc. *would* turn CI red over crates this project never builds; that's a stated maintenance liability of the `v1` feature, to be handled with a scoped `audit` ignore if it ever fires, not a reason the row can claim it "can't happen". API shape (incl. the exact `keyring::Error::NoEntry` mapping `key_store.rs` relies on) confirmed via a throwaway scratch build+run before committing | `default-features = false, features = ["v1"]` (`v1` is required — `keyring` errors at compile time without `v1` or `cli`). `key_store.rs` is the single boundary: it stores/loads the TLS private-key PKCS#8 DER (T-67) and, since **T-163**, the MaxMind account-id+license-key JSON blob (~60 bytes) — both via `Entry::new("dns-quorum-filter", <path-derived entry>).set_secret`/`get_secret`, each far under Windows `CRED_MAX_CREDENTIAL_BLOB_SIZE` (2560). The `unsafe` Win32 FFI lives in `windows-native-keyring-store`, so `dnsqb-service`'s `#![forbid(unsafe_code)]` is untouched. Chosen over a point `unsafe` DPAPI call (user decision, T-67 plan) for exactly that reason and for the T-71 macOS/Linux forward-fit. **Credential-store tests across the crate are serialized** behind `key_store::STORE_TEST_GUARD` — the Windows backend intermittently returns a just-written secret as absent under concurrent access from one process, even on distinct entries (observed while adding T-163's tests) |

| `sysinfo` 0.39.6 (+ transitive `ntapi` 0.4.3 → `winapi` 0.3.9, and the `windows` 0.62 family — `windows-core`/`windows-collections`/`windows-future`/`windows-numerics`/`windows-strings`/`windows-result`, plus `windows-implement`/`windows-interface` proc-macros) | `GuillaumeGomez` / the `sysinfo` project; the de-facto standard cross-platform Rust system/process-info crate, named by SPEC.md §7.1 #3 | Standard `cargo`/`crates.io` build, no vendored binaries | Very high reverse-dependency count for exactly this niche (process/system enumeration); actively maintained; no independent formal audit found | No advisories against `sysinfo`/`ntapi`/`winapi` 0.3.9/`windows-*` in the RustSec database — `cargo audit`'s warning count is **12 before and 12 after** adding it (the pre-existing GTK-stack "unmaintained" set from `tray-icon`'s Linux deps + the `chacha20` 0.10.1 yank), i.e. zero new advisories. `cargo deny check` runs clean at time of adding (2026-09-02, T-89 batch) — no new license-allowlist gap (`sysinfo` `MIT`, `ntapi` `Apache-2.0 OR MIT`, `winapi` 0.3.9 `MIT/Apache-2.0`, `windows-*` `MIT OR Apache-2.0` — all already allowed), so **no `deny.toml` change**. It does add `multiple-versions` warnings for the `windows`/`windows-core`/`windows-result`/`windows-sys` families (a second major alongside what `keyring`/`tray-icon` already pull) — `multiple-versions = "warn"`, not `deny`, so the gate stays green. The transitive `winapi` 0.3.9 (via `ntapi`) is the old FFI stack SPEC.md §7.1 #2 rejected the `single-instance` **guard crate** for — but that rejection had a trivial 5-line safe alternative (`share_mode(0)` lockfile); a no-`unsafe` process-**identity** check has no such alternative, every Windows crate for it funnels through `ntapi` or `windows`/`windows-sys`, so the trade is accepted here rather than in §7.1 #2's context. API shape (`System::new()` + `refresh_processes(ProcessesToUpdate::Some(&[Pid]), true)` single-PID refresh; `Process::exe() -> Option<&Path>`; `Process::name() -> &OsStr`) confirmed via a throwaway scratch build+run before writing `pid_check.rs`, per this project's verify-empirically discipline | `default-features = false, features = ["system"]` — drops the `disk`/`network`/`component`/`user` sub-modules (there is no narrower "process-only" feature of `system`). `unsafe` Win32 FFI is contained in `ntapi`/`windows-*`, so `dnsqb-service`'s `#![forbid(unsafe_code)]` is untouched. Single boundary: `watchdog::pid_check::verify_pid_alive(pid, expected_exe) -> PidCheck` (T-89) — the recycled-PID guard §7 requires before a restart. **Links into `dnsqb-service` (the DoH binary) though only `dnsqb-watcher` calls it** — a consequence of §7.1 #6 (`dnsqb-watcher` depends on `dnsqb-service` as a lib and the whole `watchdog/` module tree lives there), accepted |

| `chacha20poly1305` 0.11.0 (+ `aead` 0.6.1, `poly1305` 0.9.1, `universal-hash` 0.6.1; the `chacha20` / `cipher` 0.5 / `crypto-common` 0.2 / `inout` / `hybrid-array` / `block-buffer` 0.12 / `ctutils` / `cmov` chain was already in `Cargo.lock` as a **dev-only** transitive of `proptest`'s `rand` 0.10, and moves to runtime here) | RustCrypto org — the same org as `sha1`/`sha2`/`zeroize` already vetted above; `chacha20poly1305` is RustCrypto's standard AEAD crate, part of the `AEADs` workspace | Standard `cargo`/`crates.io` build, no vendored binaries, no C toolchain (pure Rust — this is the reason it was chosen over promoting `aws-lc-rs`, user decision 2026-09-03) | RustCrypto is widely used and actively maintained for exactly this niche; the `AEADs` workspace has had independent review passes (e.g. the 2020 NCC-Group audit of the RustCrypto AEAD traits/impls) though not a fresh formal audit of 0.11 specifically; very high reverse-dependency count | No advisories against `chacha20poly1305`/`aead`/`poly1305`/`universal-hash`/`chacha20`/`cipher`/`crypto-common`/`inout`/`hybrid-array`/`ctutils`/`cmov` in the RustSec database. `cargo audit` warning count went **12 → 11** on adding this: `chacha20` was resolved at the **yanked** 0.10.1 (dev-only) before; this batch's `cargo update -p chacha20` pins the tree to the non-yanked **0.10.2** (2026-08-27), which fixes an SSE4.1 intrinsic wrongly emitted in the SSE2 backend of `chacha20`'s *RNG / legacy 64-bit-counter* variants — a codegen/portability bug, not a cryptographic one, and not on the standard-counter AEAD path this crate uses. `cargo deny check` runs clean (`advisories ok, bans ok, licenses ok, sources ok`) — **no `deny.toml` change**: every crate is `MIT OR Apache-2.0`, already allowed. It does add `multiple-versions` warnings (`crypto-common` 0.1 vs 0.2, `block-buffer` 0.10 vs 0.12 — the `sha1`/`sha2` hash stack vs the `cipher` 0.5 AEAD stack) — `multiple-versions = "warn"`, gate stays green. `chacha20poly1305` feature set confirmed against 0.11's own `Cargo.toml` (not carried over from 0.10) via `cargo tree -f "{p} {f}"` before/after | `default-features = false, features = ["alloc", "zeroize"]` — drops the default `getrandom` sub-feature (the nonce is drawn directly, see the `getrandom` note below), keeps `alloc` for the `Vec`-returning `encrypt`/`decrypt` and `zeroize` so the cipher's key material is wiped on drop. Single boundary: `encrypted_file::{seal,open}` (T-146) — `XChaCha20Poly1305` (192-bit random nonce) over the opt-in on-disk query log (`query-log.enc`) and, at T-97, the verdict cache. Key is the 32-byte secret in `key_store` (`persistence-key:<hash>` entry), never on disk. `unsafe` (SIMD in `chacha20`/`poly1305`, contained in those crates) leaves `dnsqb-service`'s `#![forbid(unsafe_code)]` intact, same as `keyring`/`sysinfo` |
| `getrandom` 0.3 (promoted from transitive to **direct**; already in `Cargo.lock` via `rand`/`reqwest`/others, multiple majors 0.2/0.3/0.4 pre-existing) | `rust-random` org; the de-facto standard OS-RNG shim, foundational across the Rust ecosystem | Standard `cargo`/`crates.io` build, no vendored binaries | Extremely high reverse-dependency count; `rust-random`-org-maintained; no fresh formal audit but under constant scrutiny | No advisories against `getrandom` 0.3 in the RustSec database. `cargo audit`/`cargo deny check` clean at time of promotion (2026-09-03, T-146 batch); no new license-allowlist gap (`MIT OR Apache-2.0`, already allowed) and no *new* crate in the tree — this only moves an existing transitive to a named direct dependency, the same move `url` made at T-72 | `encrypted_file::seal` (T-146) calls `getrandom::fill(&mut nonce)` for the 24-byte `XChaCha20Poly1305` nonce. A failure returns `EncryptedFileError::Rng` and the flush is **abandoned** — never a zero or predictable fallback nonce (nonce reuse under a fixed key breaks Poly1305 authentication and leaks the XOR of the plaintexts) |

`reqwest`'s row above (0.13.4) gained the `json` feature at T-52 (`admin::AdminClient`'s
`Response::json()`/`RequestBuilder::json()`) — `default-features = false, features = ["rustls",
"http2", "json"]`; `cargo deny check` clean, no new license-allowlist entries (the feature only
pulls in `serde_json`, already a direct dependency).

Planned crates and the reasoning for choosing each are listed in `SPEC.md` §"Технічний стек" and
`CLAUDE.md` §"Planned stack" — that's a design-time rationale, not a substitute for this vetting
table once a specific version is actually pinned.
