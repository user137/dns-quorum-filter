# Журнал рішень

Записи про архітектурні рішення, які не влізають у формат SPEC.md (зміна
вже ухваленого рішення заднім числом, рішення, ухвалене поза контекстом
конкретного розділу спека). Цей файл — over-ride щодо старіших міркувань
у SPEC.md, якщо виникає конфлікт: **дата запису тут вирішує, яке рішення
чинне**.

Формат запису:

```
## YYYY-MM-DD — Коротка назва рішення

**Контекст:** що спричинило потребу в рішенні.
**Рішення:** що саме вирішено.
**Причина:** чому саме так, а не інакше.
**Наслідки:** що це змінює в SPEC.md чи в коді (посилання на розділи).
```

---

## 2026-08-25 — VoterStatus: 6 варіантів, включно з Pending

**Контекст:** §6 (рядок 714) і §8 (рядок 826) SPEC.md обидва посилаються на §3.6
як джерело переліку `VoterStatus`, але наводять розбіжні списки — §6 називає
`BLOCK/ALLOW/TIMEOUT/ERROR/CANCELED` (5, без `Pending`), §8 називає
`Pending/Block/Allow{ip_count}/Error{message}/Canceled` (5, без `Timeout`). Сам
§3.6 (канонічне джерело для обох) описує лише 5 значень для `LogEntry.voters`
і не згадує `Pending` — там ідеться про фінальні, вже записані в лог статуси.

**Рішення:** `VoterStatus` як DTO-тип — 6 варіантів: `Pending, Block,
Allow{ip_count}, Timeout, Error{message}, Canceled`. `Pending` — легітимний
окремий варіант типу (§8), зарезервований під майбутнє live-відображення
голосів під час виконання запиту, а не помилка/друкарська неточність.

**Причина:** підтверджено користувачем після прямого запитання (варіант
"6 варіантів, з Pending" обрано свідомо, після того як було показано, що §3.6
сам по собі містить лише 5 значень) — Pending розглядається як навмисне
розширення DTO понад те, що §3.6 буквально описує для персистованого логу.

**Наслідки:**
- `LogEntry.voters` (персистований лог) фактично ніколи не міститиме
  `Pending` — запис пишеться лише після ухваленого рішення (§3.6). `Pending`
  стосується лише live/in-flight використання DTO, яке SPEC.md поки не
  описує як окрему фічу.
- `UI-SPEC.md` §4 п.2 і `diagrams/ui-dto-model.md` вже відображають 6-варіантну
  версію — зафіксовано тут, подальших правок цих файлів не потрібно.
- §6/§8 SPEC.md лишаються буквально розбіжними в тексті; цей запис — джерело
  істини для розбіжності за правилом файлу (дата тут переважає).

## 2026-08-25 — Платформа Фази 1: Windows

**Контекст:** SPEC.md §"Фазований план" фіксує Фазу 1 як "1 платформа: macOS
або Windows" — свідомо не вирішено на момент написання спека. T-1 (workspace
init) і T-18 (CI build matrix) потребують конкретної платформи для `runs-on:`
у CI та для локального ручного встановлення сертифіката (T-49).

**Рішення:** Windows.

**Причина:** підтверджено користувачем у сесії ініціалізації Кроку 0 —
робоча машина користувача Windows, що спрощує локальне ручне тестування
(встановлення self-signed сертифіката в trust store, T-49) без додаткового
обладнання чи VM.

**Наслідки:**
- `.github/workflows/ci.yml` — усі job'и `runs-on: windows-latest`.
- T-48/T-49/T-51 (сертифікат, CT-політика) виконуються й перевіряються на
  Windows first; macOS-гілка (Certificate Store аналог — Keychain) — Фаза 2,
  за SPEC.md "Фаза 2 — Автоматизація сертифіката + друга платформа".
- Не змінює жодного протокольного рішення — суто вибір цільової ОС для PoC.

## 2026-08-25 — T-20: жива перевірка сигнатур блоку Quad9 і AdGuard

**Контекст:** SPEC.md §3.1/§3.4 і "Відкриті питання" п.1 вимагали живої
перевірки формату сигнатури блоку Quad9 і AdGuard перед імплементацією
`is_blocked()` — маркетингові сторінки заявляли NXDOMAIN (Quad9) і
`0.0.0.0`/`::` (AdGuard), але це не підтверджено retrieved-джерелом.

**Рішення:** обидві сигнатури підтверджено живими DoH-запитами (A і AAAA)
через реальний wire-format канал (`https://dns.quad9.net/dns-query`,
`https://dns.adguard-dns.com/dns-query`, HTTP/2, генератор запитів —
одноразовий crate поза workspace, `hickory-proto` 0.26.1):

- **Quad9** (тест-домен `isitblocked.org` — офіційний test-домен Quad9,
  підтверджений з двох незалежних джерел: `docs.quad9.net/FAQs/` і
  офіційний твіт `@Quad9DNS`, 2018): `NXDOMAIN` з **порожньою authority-
  секцією** (0 записів) — на відміну від справжнього NXDOMAIN, де authority
  містить SOA (перевірено паралельно на свідомо неіснуючому домені
  `totally-nonexistent-dnsqb-probe-xyz123.com` — і Quad9, і Cloudflare дали
  NXDOMAIN з authority_count=1/SOA). Baseline (Cloudflare) підтвердив, що
  `isitblocked.org` реально резолвиться (A `74.208.236.124`,
  AAAA `2607:f1c0:100f:f000::2d1`) — тобто NXDOMAIN від Quad9 саме блок, не
  збіг з реально неіснуючим доменом.
- **AdGuard** (тест-домен `criteo.com` — обраний емпірично: спершу
  перевірено `scorecardresearch.com`, виявився НЕ заблокованим цим
  профілем; `criteo.com` підтверджено заблокованим і резолвиться на
  baseline з реальними A/AAAA): A → `0.0.0.0`, AAAA → `::`. Baseline
  (Cloudflare) повернув реальні адреси (`23.185.0.4` і дві реальні
  IPv6-адреси) для того самого домену.

**Причина:** SPEC.md прямо вимагає не покладатись на маркетингові
матеріали для Quad9/AdGuard (на відміну від Cloudflare, де формат
підтверджено офіційною документацією). Одноразовий тестовий генератор і
`.bin`-файли відповідей не потрапили в репозиторій (Відкрите питання п.2 —
ToS автоматизованих DoH-запитів до Quad9/AdGuard ще не звірене юридично;
регулярні CI-запити до живих апстрімів випереджали б це питання).

**Корoбороюче спостереження (не окремий доказ, доповнює вище):** у першому
проході генератор запитів не встановлював `RD` (Recursion Desired). З
`RD=0` Quad9 повернув `Refused` для свідомо неіснуючого домену — але
`NXDomain` для `isitblocked.org`. Це незалежний сигнал, що `isitblocked.org`
не проходить звичайний резолюційний шлях (де без RD нема з чого відповісти,
звідси `Refused`), а перехоплюється policy-рівнем Quad9 ще до перевірки
recursion — узгоджується з висновком "NXDOMAIN тут синтезований, не
результат резолюції", не спирається лише на authority-секцію.

**Апстрім-специфічна знахідка, окрема від сигнатур блоку:** `dns.quad9.net`
вимагає HTTP/2 — HTTP/1.1-запит (curl 8.21.0/Schannel без HTTP/2) отримав
HTML-тіло `"This server implements RFC 8484... and requires HTTP/2"` замість
DNS wire-format відповіді. Це не є RFC 8484-порушенням з боку Quad9 (RFC
дозволяє вимагати HTTP/2), але має прямий наслідок для T-24: якщо
апстрім-клієнт (`reqwest`) не форсує HTTP/2 до Quad9, `decode_wire_message`
впаде на HTML-тілі — а під дефолтним `fail-open` (SPEC.md §3.3) це виглядає
як "апстрім не блокує", тобто Quad9 **мовчки випадає з кворуму** замість
явної помилки. Зафіксовано в SPEC.md §3.4 як передумова, не деталь
оптимізації (на відміну від HTTP/2 keep-alive, §3.6).

**Наслідки:**
- SPEC.md §3.1, §3.4 і "Відкриті питання" п.1 оновлені — сигнатури
  підтверджені, не "заявлені"; HTTP/2-вимога Quad9 задокументована як
  передумова для T-24.
- `is_blocked()` (T-23) реалізується за підтвердженими тут сигнатурами з
  урахуванням n=1-застереження в SPEC.md §3.1: baseline-звірка лишається
  основним механізмом розрізнення, authority-секція Quad9 — лише
  корoбороюча деталь, не самодостатня умова.
- `criteo.com`/`isitblocked.org` — лише для одноразової верифікації в цій
  сесії, не для юніт-/conformance-тестів (ті лишаються на мокнутих
  апстрімах, T-61/T-62).

## 2026-08-25 — Навмисно червоні conformance-тести: `#[ignore]`, не blocking CI

**Контекст:** T-3–T-12 (Крок 0) вимагають падаючих тестів на RFC-вимоги,
написаних до того, як існує код резолвера (SPEC.md §"Фазований план", "Крок
0"). Якщо ці тести просто додати в звичайний `cargo test`, весь CI стає
перманентно червоним аж до завершення Фази 1 — суперечить власному правилу
проєкту "лінтер/CI має реально впливати на результат, не бути декоративним":
завжди-червоний job привчає команду ігнорувати статус CI загалом, а не лише
цей конкретний набір тестів. SPEC.md/TASKS.md не визначають, як саме
позначити "очікувано червоний" стан у CI — рішення нижче не мовчазне.

**Рішення:** кожен з 20 conformance-тестів позначено
`#[ignore = "Крок 0 red until T-NN (Фаза 1)"]` з конкретним ID задачі Фази 1,
яка знімає позначку. `.github/workflows/ci.yml`, job `conformance`, два кроки:
1. `cargo test --test conformance -p dnsqb-service` (без `--ignored`) —
   **звичайний blocking крок**: перевіряє компіляцію таргету й що жоден
   `#[ignore]` не був випадково знятий передчасно.
2. `cargo test --test conformance -p dnsqb-service -- --ignored` —
   **інформаційний, `continue-on-error: true`**: показує актуальний "red
   board" незакритих RFC-вимог, не блокує merge.

**Причина:** розділяє дві різні речі, які легко сплутати в одному job —
"тест ще не мав пройти" (очікувано, до Фази 1) і "щось справді зламалось"
(компіляція, помилково знятий `#[ignore]`) — перше не повинно ховати друге,
і навпаки.

**Наслідки:**
- У Фазі 1, коли конкретна вимога реалізується — прибрати `#[ignore]` з
  відповідного тесту; він переходить у звичайний blocking-прогін кроку 1
  автоматично, без зміни CI-конфігурації.
- Причина в кожному `#[ignore]`-рядку — це посилання на задачу з TASKS.md, не
  довільний текст; при розбіжності між рядком і фактичним власником логіки
  (напр. `min_rrset_ttl` спершу позначено як T-33, хоча T-33 — задача
  перевірки, не обов'язково нової імплементації) — рядок виправлено на
  T-33/T-34 з поясненням у коментарі над функцією-стабом
  (`crates/dnsqb-service/src/lib.rs`), а не залишено вводити в оману.

## 2026-08-26 — Формат конфіг-файлів: JSON → TOML

**Контекст:** SPEC.md §1 (рядок 564, до цього запису) і комунікаційна матриця
§0 (рядок 71) явно називали JSON форматом для `overrides` та (з T-144)
`resolver_config` — не просто "довільний текстовий файл". Користувач попросив
формат у стилі `ssh_config`/`my.cnf`, зручний для ручного редагування без
відкриття UI, і прямо запитав, чи є кращий за JSON варіант.

**Рішення:** TOML для обох конфіг-файлів (`resolver_config.toml`,
`overrides.toml`, T-145). Жорсткий перехід, без підтримки старого JSON —
`.json`-файли більше не читаються; якщо `.toml`-файл відсутній, а старий
`.json`-сусід є, сервіс лише логує попередження (шлях файлу, не вміст) і
запускається з дефолтами/порожніми списками, як і при взагалі відсутньому
файлі.

**Причина:** TOML підтримує коментарі та секції — саме те, чого бракує JSON
для ручного редагування в дусі `ssh_config`/`my.cnf`; на serde-структурах
(`ResolverConfigFile`/`OverrideListsFile`) заміна — це підміна парсера
(`toml::from_str` замість `serde_json::from_str`), без зміни публічного API
жодного модуля. INI-стиль розглядався й відхилений — менш зрілий крейт,
гірша підтримка вкладених структур, немає переваги над TOML для цього
проєкту. Без підтримки старого JSON: жодних відвантажених користувачів на
момент MVP, підтримка двох форматів паралельно — спекулятивна складність
проти проєктної дисципліни "без backward-compat-заглушок".

**Наслідки:**
- SPEC.md §1 (рядок 564) і комунікаційна матриця §0 (рядок 71) відредаговані
  напряму (JSON → TOML) — цей запис не перекриває їх мовчки, текст самого
  SPEC.md вже узгоджений.
- `overrides.rs`'s `OverrideError::Parse` — навмисно без payload (на відміну
  від `config.rs`'s `ConfigError::Toml(toml::de::Error)`, який зберігає
  payload): `overrides.toml` складається лише з доменних імен, а
  `toml::de::Error`'s `Display`/`Debug` виводять анотований фрагмент
  вхідного рядка з помилкою — підтверджено емпірично скретч-пробою під час
  T-145, не припущено. Обгортання цієї помилки в `overrides.rs` спричинило б
  новий витік домену в діагностичний лог сервісу (SPEC.md "Наскрізні
  вимоги"), той самий клас проблеми, що вже задокументований для
  `InvalidReason` (CLAUDE.md gotchas). `resolver_config.toml` доменів не
  містить, тому там багатий TOML-снепшот помилки — чистий виграш для UX без
  ціни приватності.
- Історичні записи CLAUDE.md про T-37/T-40/T-144 (де файли ще були JSON) не
  переписані заднім числом — вони коректно описують стан на момент тих
  зрізів; T-145 додає власний абзац, а не редагує історію.

## 2026-08-26 — LogEntry.decision: третій варіант `FAILED`

**Контекст:** SPEC.md §6 і `UI-SPEC.md` §3.2 обидва фіксували `decision` на
двох значеннях (`ALLOWED`/`BLOCKED`) з моменту першого визначення структури
логу (T-42/T-43) — до T-147 в `query_log::QueryLog` взагалі не було
продюсера, тож жоден реальний виклик `handle_query` ніколи не перевірявся
проти цього enum'а. Підключення продюсера (T-147) показало: `handle_query`
насправді породжує SERVFAIL на кількох гілках (помилка/таймаут baseline на
allowlist- і pass-through-шляхах, усі voters недоступні під fail-open,
недоступний `answer` після quorum-Allow) — жодна з них не є ні "дозволено", ні
"заблоковано" в сенсі фільтраційного рішення.

**Рішення (узгоджено з користувачем):** додати `Decision::Failed` —
"резолюція провалилась (SERVFAIL), жодного фільтраційного рішення фактично
не ухвалено". Альтернатива (не логувати SERVFAIL-шляхи цього зрізу) була
запропонована й відхилена користувачем.

**Причина:** позначати SERVFAIL як `ALLOWED` — це брехня, яку UI показав би
буквально; мовчки не логувати ці випадки означає, що лог не може пояснити
користувачу, чому домен не відкрився — саме той діагностичний сценарій,
заради якого лог взагалі існує (SPEC.md §6's "Конфлікт із вимогою
приватності").

**Наслідки:**
- SPEC.md §6 (таблиця структури запису) і `UI-SPEC.md` §3.2 (рядок `decision`
  таблиці 3.2) відредаговані напряму — цей запис не перекриває їх мовчки.
- `UI-SPEC.md` §3.2's facet-enum `{ALL,BLOCKED,ALLOWED}` (рядок 78) ще **не**
  розширений на четверте значення — позначено ⚠️-приміткою на місці,
  залишено для T-52, коли реальний UI-екран логу справді будується.
- `query_log::Decision` більше не покривається вичерпним `match` без
  wildcard-гілки ніде поза `pipeline.rs`'s власним `decision_from_response` —
  перевірено грепом по кодовій базі перед комітом T-147, нових точок
  розбіжності не знайдено.

## 2026-08-27 — UI-шар: Tauri-вікно (T-52) замінено треєм + вбудованим веб-UI (T-149)

**Контекст:** SPEC.md "Відкриті питання" п.13 (записаний тієї ж сесії, під час T-149) сформулював
це питання явно й сам зажадав окремого plan-mode + advisor-рев'ю перед імплементацією (той самий
клас реверсу вже поставленого дизайну, що й T-145's config-формат). Користувач підняв два реальних
приводи: (1) чи можна взагалі керувати додатком на ОС без графічної оболонки (headless Linux,
п.12 — окреме, не повністю закрите цим рішенням питання); (2) поточний `dnsqb-ui` (Tauri) не дає
жодного on-screen індикатора, що сервіс узагалі запущено, і тягне важкий WebView2/Tauri-хвіст
залежностей заради трьох простих контролів.

**Рішення:** `crates/dnsqb-ui` видалено повністю. Замість нього — два окремих компоненти:
(1) `dnsqb-tray` — легкий фоновий трей-індикатор (`tray-icon`/`tao`/`rfd`, не Tauri), живий
tooltip-статус (недоступний / фільтрація вимкнена / активна фільтрація з живими лічильниками),
п'ять пунктів меню, з яких лише "Зупинити фільтрацію" (`POST /admin/shutdown`, за нативним
confirm-діалогом) і "Перезапустити" (`POST /admin/reset`) чіпають сам резолвер; (2) повноцінне
налаштування (тумблери провайдерів, режим таймауту, статистика) — вбудований `GET /admin/ui`,
який `dnsqb-service` сам роздає звичайному браузеру на вже наявному адмін-каналі (той самий порт
і TLS-сертифікат, що й DoH-слухач). "Закрити" (лише трей) і "Зупинити фільтрацію" (сам процес
`dnsqb-service`) — навмисно окремі пункти меню, не один (advisor-caught під час планування): трей
ніколи не володіє `dnsqb-service` як дочірнім процесом (автозапуск лишається окремою, ще не
побудованою задачею — T-150), тож єдиний спільний пункт "закрити = усе зупинити" лишав би DNS
тихо нефільтрованим без жодного індикатора на екрані, щойно трей теж зникає.

**Причина:** підтверджено користувачем (AskUserQuestion) по трьох окремих осях — "Перезапустити" =
м'який reset через admin-канал, не OS-рестарт; меню трею розділяє "Закрити"/"Зупинити фільтрацію";
`dnsqb-ui` видаляється повністю, не лишається паралельним варіантом. Веб-UI на вбудованому
адмін-каналі — набагато дешевший приріст поверх уже готового `AdminClient`/DTO-шару (T-52), ніж
новий Tauri-екран, і єдиний з двох варіантів, що взагалі працює headless (SSH port-forward до
адмін-порту) — на відміну від трею, який так само вимагає графічної сесії, як і Tauri, і сам по
собі headless-кейс не закриває (п.12, окреме питання). Крок 0 (одноразовий, видалений до коміту
scratch-проб) підтвердив емпірично, що `tray-icon` 0.21.3 + `tao` 0.31.1 + `rfd` 0.15.4 реально
збираються й запускаються на Windows, і вся дерево залежностей резолвиться в уже дозволені
ліцензії — жодного нового запису `deny.toml` не знадобилось, на відміну від Tauri-онбордингу
(T-52), який додав дві нові ліцензії й п'ять RUSTSEC-ігнорів.

**Наслідки:**
- SPEC.md: рядок "UI-шар" у "Технічному стеку", комунікаційна матриця (рядок 4 — видалено,
  канал більше не існує; рядок 12 розщеплено на 12a трей↔сервіс/12b браузер↔сервіс), Відкрите
  питання п.13 позначено вирішеним.
- **Реальний UX-регрес, названий тут, а не відкритий постфактум**: колишній Tauri `AdminClient`
  програмно пінив `cert.pem`, тож користувач ніколи не бачив попередження браузера про сертифікат.
  Звичайний браузер, що відкриває `/admin/ui`, не може зробити це для себе — до T-49 (встановлення
  сертифіката в trust store) клік "Відкрити налаштування" в треї показує попередження про
  недовірений сертифікат перед самою сторінкою. Очікувано, задокументовано в CONFIGURATION.md/
  SERVICES.md, не помилка.
- UI-SPEC.md §5's чернетковий Tauri-command allowlist лишається чернеткою на майбутнє (T-53–T-60,
  більшість усе ще відкриті) — позначено заголовковою приміткою, не переписано рядок за рядком.
- SECURITY.md's "Tauri IPC boundary" загроза-пункт видалено разом із самим каналом; новий пункт
  "Admin HTTP channel" (T-52/T-149) покриває обидва нові учасники (трей і браузер) тим самим
  cert-пінінгом/CSRF-гейтом, що вже існував.
- `deny.toml`'s Tauri-специфічні записи (`MPL-2.0`, `Zlib` ліцензії, п'ять `unic-*`-ігнорів)
  прибрано по одному, з перевіркою `cargo deny check` після кожного — `[graph] targets` (Windows-
  only обмеження) лишено, самостійно правильне незалежно від Tauri.
- Автозапуск `dnsqb-service` (для миша-only користувача, що просто встановив пакет) — свідомо
  поза скоупом цього рішення, зафіксовано як окремий, ще не побудований T-150 (Startup-ярлик/
  Run-ключ, майбутній інсталятор).

---

## 2026-08-28 — Прибрано явне UI-повідомлення про приватність кворуму (реверс T-57)

**Контекст:** SPEC.md §"Відкриті питання" п.4 і §8 обидва явно вимагали, щоб компроміс
приватності кворуму (новий домен видимий одразу N стороннім провайдерам, не одному) був
**явно** показаний в UI, не прихована деталь архітектури. T-57 (TASKS-DONE.md) реалізував це
як інфо-картку на `/admin/ui` (`main.js`'s `privacyNoticeHtml`), показану щоразу, коли хоча б
один фільтрувальний провайдер увімкнено.

**Рішення:** цю картку прибрано повністю (`privacyNoticeHtml` та її виклик видалені з
`main.js`, невживаний `.notice.info` CSS-клас прибрано разом із нею). Окреме попередження
"обидва провайдери вимкнено — фільтрація не активна" (`.notice.warn`, інший смисл — активність
фільтрації, не приватнісний fan-out) **лишається**, воно не стосується цього рішення.

**Причина:** пряма вказівка користувача (2026-08-28), без додаткової аргументації, окрім самого
факту рішення — записано чесно, без вигаданого обґрунтування.

**Наслідки:**
- SPEC.md §"Відкриті питання" п.4 і §8's відповідний рядок анотовані приміткою про реверс і
  посиланням сюди, текст самої вимоги не видалено (документ ніколи не деградується мовчки).
- T-57's запис у TASKS-DONE.md лишається як є (історичний опис того, що й чому було зроблено
  на момент реалізації) — коротка примітка про реверс дописана в кінці, сам запис не
  переписаний заднім числом.
- Жодної зміни в бекенді — `admin::AdminStatusResponse.providers` і сам розрахунок кількості
  сторін (`filtering + baseline`) залишаються тими самими; змінилось лише те, чи UI це показує.
- Якщо ця вимога колись повернеться (наприклад, юридична необхідність), той самий розрахунок
  уже готовий до повторного використання в `main.js`'s git-історії (комутативна, не втрачена
  назавжди) чи в T-57's власному запису TASKS-DONE.md.

## 2026-08-29 — T-49: install/uninstall у trust store автоматизовано, не ручний крок

**Контекст:** T-49 і фазований план (SPEC.md §"Фазований план") спочатку описували встановлення
leaf-сертифіката в `CurrentUser\Root` як ручний, людський крок на Ф1, з повною автоматизацією
(install у trust store) відкладеною до T-68 у Фазі 2. Живий тест того ж дня підтвердив ціль
(`CurrentUser\Root`, без адмін-прав) і закрив мотивуючий сумнів користувача щодо самого механізму
довіри (SPEC.md §2). Користувач попросив реалізувати install/uninstall автоматизовано зараз, а не
чекати Фази 2.

**Рішення:** межа Ф1/Ф2 з фазованого плану переглядається для install-половини T-68 — вона
переноситься в Ф1 під номером T-49 (уже наявним). T-67's DPAPI-половина (приватний ключ у
platform secure storage) лишається окремою, незмінною Ф2-задачею — це рішення її не стосується.
Ціль — саме `CurrentUser\Root`, не `TrustedPeople` (не спрацював на живому тесті) і не
`LocalMachine` (потребує адмін-елевації, не потрібної тут).

**Причина:** пряме прохання користувача розширити T-49 за наперед підтверджену, безпечну ціль,
без додаткового чекання на Фазу 2 суто через нумерацію плану — сам механізм довіри вже емпірично
перевірений цього ж дня (SPEC.md §2).

**Наслідки:**
- `crates/dnsqb-service/src/trust_store.rs` (новий модуль) — `ensure_installed`/`uninstall` через
  `certutil.exe`, не `unsafe` `CryptoAPI` FFI.
- Ідентичність сертифіката розділена за напрямком: `ensure_installed` звіряє SHA-1 thumbprint
  поточного `cert.pem` (точна перевірка "чи саме цей сертифікат довірений"), `uninstall` звіряє
  `CommonName` (вичерпне видалення "усе, що цей проєкт колись довіряв", включно із застарілими
  записами після перевипуску сертифіката) — advisor-catch під час plan-review, не перший драфт;
  повний технічний запис — `TASKS-DONE.md`, T-49.
- `dnsqb-tray` отримує дві нові, симетричні, підтверджені нативним діалогом дії ("Встановити
  сертифікат"/"Видалити сертифікат") замість тихого автозапуску при старті `dnsqb-service` — чи
  сам `certutil.exe` показує ще й окремий діалог підтвердження від ОС, не підтверджено для цієї
  конкретної команди на момент цього рішення, залишено для живої перевірки користувачем.
- SPEC.md §2's "Встановлення в trust store" оновлено на місці (не переписано мовчки) для опису
  автоматизованого шляху замість ручної інструкції.

---

## 2026-09-02 — Порядок пріоритету умов індикатора стану (закриває ⚠️ GAP у `ui-status-indicator.md`)

**Контекст:** `diagrams/ui-status-indicator.md` з Батча 3.0 ніс ⚠️ GAP: SPEC.md не фіксує, яку
умову показувати, коли кілька виконані одночасно (напр. watchdog перезапускає сервіс **І**
активних voters 0). Діаграма пропонувала порядок `1→2→3→4` (браузер → 0 voters → watchdog →
деградація) як робочу рекомендацію, з явним записом «потребує підтвердження чи явного запису в
DECISIONS.md перед реалізацією T-56/T-95». Ф3 Батч 3.3 (T-95) реалізує watchdog-умову в
tray-tooltip і полі `GET /admin/status.watchdog`, тож рішення потрібне зараз.

**Рішення (прохання користувача 2026-09-02):** **watchdog вище за 0-voters.** Порядок умов
основного індикатора:

1. Браузер фактично не використовує локальний DoH (ВП №10) — не реалізовано (блоковано на T-134).
2. **Watchdog: сервіс перезапускається / зупинено (перевищено ліміт спроб).**
3. Активних voters = 0 (явний pass-through).
4. Деградація: N з M апстрімів не відповіли (режим `degraded`).
5. Жодна з 1–4 → «фільтрація активна».

**Причина:** інфраструктурний збій сервісу (`Restarting` / `GaveUp`) робить фільтрацію
недоступною незалежно від конфігурації, тож він показується раніше за конфігураційний вибір
користувача (0 voters — стан, у якому сам сервіс живий і відповідає). Це змінює порядок відносно
намальованого у flowchart `1→2→3→4` на `1→watchdog→voters→degraded`.

**Наслідки:**
- `diagrams/ui-status-indicator.md` — flowchart (`Check2`↔`Check3` місцями), таблиця умов
  перенумерована, секція «⚠️ GAP» → «Закрито».
- `crates/dnsqb-tray/src/status.rs` — `watchdog_override` перевіряється **перед** `/admin/status`
  у циклі поллінгу; `TrayStatus::{ServiceRestarting, ServiceGaveUp}` перебивають `NoActiveProvider`.
- `crates/dnsqb-service/src/admin.rs` — `AdminStatusResponse.watchdog: Option<WatchdogStatusView>`;
  `WatchdogStatusView` — 2-варіантна UI-проєкція (`Restarting` [вкл. `BackoffWait`] / `GaveUp`),
  не сирий 7-варіантний `WatchdogState` (§7.1 #7 описував ширшу форму — звужено свідомо: 4
  проміжні стани UI не показує, стале/відсутнє → `None`).

---

## 2026-09-03 — Офлайн-стан у порядку індикатора; T-155 fallback-перемикач за замовчуванням вимкнено (Ф3 Батч 3.4)

**Контекст:** T-152 вводить «немає інтернету» як окремий стан, відмінний від «апстрім
деградований». Він потребує місця в порядку пріоритету індикатора (закритому вчора, 2026-09-02).
Окремо, T-155 вводить керований перемикачем fallback на нефільтрований baseline, коли жоден
filtering-voter не відповів — задача в TASKS.md схилялася до «за замовчуванням увімкнено», але це
рішення користувача.

**Рішення (AskUserQuestion 2026-09-02 + 2026-09-03):**

1. **Офлайн = умова #3 індикатора**, одразу після watchdog-станів (#2), перед «0 активних
   провайдерів» (#3 → тепер #4). Новий порядок:
   1. Браузер не використовує локальний DoH (ВП №10) — не реалізовано.
   2. Watchdog: сервіс перезапускається / зупинено.
   3. **Немає інтернету (усі маркери досяжності впали).**
   4. Активних voters = 0 (явний pass-through).
   5. Деградація: N з M апстрімів не відповіли (режим `degraded`).
   6. Жодна з 1–5 → «фільтрація активна».

   **Причина:** офлайн — інфраструктурний/середовищний збій, та сама родина, що watchdog: його не
   полагодити тумблером провайдерів. Показувати «ви вимкнули провайдерів», коли насправді немає
   мережі, — оманливо. Вище за «деградацію» — офлайн є її повна версія.

2. **T-155 `serve_baseline_when_filters_unreachable` за замовчуванням `false`.** OFF = сьогоднішня
   поведінка, лише з чесною міткою в логу (`BASELINE_FALLBACK` замість схованого `QUORUM`): режим
   таймауту вирішує як раніше (`fail_open`/`degraded` → Allow через baseline; `fail_closed` →
   Block). ON = віддавати baseline-відповідь незалежно від режиму. **Розходиться з first-draft-
   нахилом задачі** («днсквоурум має продовжувати працювати»), але zero-регрес на дефолті: OFF
   через `fail_open` усе одно віддає робочу відповідь, тож жодна вкладка браузера не ламається під
   час Quad9+AdGuard блипу (і не спрацьовує тихий браузерний DoH-fallback, ВП №10).

**Наслідки:**
- `diagrams/ui-status-indicator.md` — нова умова #3, таблиця й flowchart перенумеровані.
- `crates/dnsqb-tray/src/status.rs` — `TrayStatus::Offline`, перевіряється в `from_response`
  **перед** `NoActiveProvider`; watchdog-стани (перевірені раніше в циклі) лишаються вище.
- `crates/dnsqb-service/src/admin.rs` — `AdminStatusResponse.network: NetworkStatusView`
  (`ONLINE`/`OFFLINE`) + `baseline_endpoint: BaselineEndpointView` (діагностика T-154).
- `crates/dnsqb-service/src/pipeline.rs` — офлайн → миттєвий `SERVFAIL` (без fan-out, без кешу,
  ігнор режиму таймауту); `DecisionSource::BaselineFallback` для «усі фільтри мовчать».
- `crates/dnsqb-service/src/config.rs` — нове поле `serve_baseline_when_filters_unreachable`
  (`resolver_config.toml`, дефолт `false`).

---

## 2026-09-03 — Батч 3.5: шифрована персистентність логу — шифр, спосіб вмикання, розбиття батча

**Контекст:** SPEC.md §6 / Відкриті питання п.5 давно передбачали opt-in шифровану персистенцію
логу й кешу, без вибору механізму. Батч 3.5 (T-146 + T-96) реалізує половину — лог; kickoff
plan-mode + AskUserQuestion + advisor.

**Рішення:**

1. **Шифр — RustCrypto `chacha20poly1305`, конкретно `XChaCha20Poly1305`** (192-бітний nonce).
   Відхилено: промоушн `aws-lc-rs` (уже крипто-бекенд проекту через rustls/rcgen — але користувач
   обрав pure-Rust, без C-тулчейну) і `age` (важкий формат із власним recipient/passphrase-
   механізмом, зайвим коли ключ із OS-сховища). `XChaCha` замість `ChaCha` — 24-байтний
   випадковий nonce знімає питання birthday-колізії для файлу, що перезаписується щофлаш, за
   12 зайвих байт. `cargo update -p chacha20` пінить дерево з yanked 0.10.1 (був dev-only) на
   0.10.2; `cargo audit` 12 → 11 warning'ів. Ключ — 32 байти у Credential Manager
   (`persistence-key:<hash теки>`), той самий `key_store`/`keyring`-механізм, що TLS-ключ T-67.
   Формат: 6-байтний cleartext-заголовок (`DQF1`/kind/version) як AAD — версійний cutover і
   підміна типу файлу стають гучними названими помилками, не auth-fail (дисципліна T-144/145/148).

2. **Вмикання — лише прапорцем `persist_query_log` у `resolver_config.toml`, без UI-тумблера**
   (T-146 так і формулює). `/admin/ui` показує пасивний рядок-попередження, поки прапорець
   активний (`AdminStatusResponse.query_log_persisted`). Зберігати історію переглядів на диску
   має бути свідома дія; завжди-видимий банер для стану, який користувач сам обрав, — не
   анти-патерн «always-on banner ≠ попередження» (нема per-event, який підтверджувати).
   Узгоджено з «backend before UI» і реверсом T-57 (прибрання always-on розкриття).

3. **Два незалежні прапорці** (`persist_query_log`, `persist_cache`) — не один спільний. Лог і
   кеш різняться чутливістю й ціннісною пропозицією (кеш = холодний старт). **Цей батч додає
   лише `persist_query_log`.**

4. **Батч розділено:** T-146 + T-96 (лог) — зараз; **T-97 (кеш) — окрема задача, окремий
   go-ahead + власний міні plan+advisor.** T-97 несе найбільше нового ризику (`Instant`→wall-clock
   реконструкція TTL, вікно виселення `moka`, `moka::iter()` семантика) при найнижчій цінності;
   advisor kickoff #4 радив зробити це рішенням, не контингенцією.

**Наслідки:**
- Нові модулі `encrypted_file` (чистий AEAD-кодек), `persist_dto` (serde-дзеркала `LogEntry`),
  `log_persist` (`persist_snapshot` / `load_persisted_query_log` / `run_query_log_persister`).
- `key_store` — третій секрет (`load_or_create_persistence_key`, `KeyStoreError::{Rng,MalformedKey}`).
- `config.rs` / `dispatch::PersistTarget` — `persist_query_log` через cross-field-read кожного
  запису `resolver_config.toml`.
- `paths::write_atomic` (temp + `sync_all` + `rename`; `fs::rename` поверх наявного файлу на
  Windows перевірено scratch-пробою).
- `main.rs` — сід логу до `AppState::new`, спавн персистера після; GeoIP-updater + reachability
  проб винесені в `spawn_public_http_tasks` (розплата за `too_many_lines`, структурно).
- 2 CodeQL `hard-coded-cryptographic-value` (critical) на тестових ключах у `encrypted_file.rs`
  — dismiss'нуто як «used in tests» (той самий клас, що T-165's тестові FP).

---

## 2026-09-03 — T-97: шифрована персистентність кешу — настінний дедлайн, лише Allow, вмикання

**Контекст:** винесена половина Батча 3.5 (див. запис вище). Kickoff plan-mode + AskUserQuestion +
advisor. Механізм (`encrypted_file` / `XChaCha20Poly1305` / `persistence-key` у OS-сховищі)
переиспользано з T-146 без змін; `FileKind::Cache` (0x02) уже був у коді.

**Рішення:**

1. **На диск пишеться абсолютний настінний дедлайн, не `Instant` і не відносний залишок.**
   `CacheEntry.expires_at` — монотонний `Instant`, який скидається при перезавантаженні, тож
   серіалізувати його безглуздо. Snapshot конвертує в `SystemTime`-дедлайн; restore обчислює
   залишок проти поточного настінного часу. **Запис, чий дедлайн сплив за час простою,
   відкидається** — не подається як stale: RFC 8767 stale-if-error не підключено
   (`should_serve_stale` — unconsumed predicate), тож споживача в прострочених записів нема.
   Оригінальний `ttl` (діагностичний, не в гарячому шляху) при цьому втрачається — на restore
   `ttl = remaining`.

2. **Персиститься лише `Verdict::Allow`; `Block` відкидається при знятті snapshot.** Рішення
   користувача (AskUserQuestion) проти альтернативи «персистити обидва + задокументувати».
   Причина (advisor kickoff): `fail_closed` + коротка мережева проблема кешують таймаут-`Block`
   на `block_verdict_ttl` (дефолт 24 год); з персистентним кешем такий «отруєний» блок пережив би
   рестарт — у т.ч. **автоматичний рестарт watchdog**, що є саме шляхом відновлення, який Батчі
   3.0–3.3 будували. `CacheEntry` не несе provenance, тож відфільтрувати саме timeout-блоки не
   можна — але Allow-only прибирає клас структурно, доказово з рядка фільтра. Ціна: свіжий
   quorum-`Block` треба перерахувати одним round-trip після старту (OR-logic → будь-який
   провайдер одразу переблокує); прогрів холодного старту домінований Allow-записами. DTO
   (`PCacheVerdict`) лишає `Block` представним, `to_json` його не емітить — зміна політики не
   потребує bump'у формату.

3. **`Verdict::Allow` IP лишаються `IpAddr` у DTO**, не дзеркаляться в рядки (на відміну від
   `SystemTime`/`RecordType`/`&'static str` у `persist_dto`) — `IpAddr` має власний serde-impl,
   round-trip lossless, дзеркало було б зайвим кодом.

4. **Вмикання — лише прапорцем `persist_cache` у `resolver_config.toml`, без адмін-маршруту**
   (як `persist_query_log`). Пасивний `/admin/ui`-рядок. Два прапорці об'єднано в один DTO-об'єкт
   `AdminStatusResponse.encrypted_persistence { query_log, cache }` — тримає `AdminStatusResponse`
   під `clippy::struct_excessive_bools` без `#[allow]`, і вони справді один набір.

**Наслідки:**
- Нові модулі `cache_persist_dto` (DTO + Instant→wall конвертація з інжектованим годинником) і
  `cache_persist` (`persist_cache_snapshot` / `load_persisted_cache` / `run_cache_persister`).
- `Cache::snapshot` (через синхронний `moka::future::Cache::iter()` → `(Arc<K>, V)`, best-effort)
  / `Cache::restore`; `CacheKey::domain()`/`qtype()` accessor'и; `AppState::cache_snapshot` /
  `restore_cache` (lock не тримається через `.await`).
- `log_persist::rename_orphan` → `pub(crate)`, переиспользано.
- `AdminStatusResponse.query_log_persisted` → `.encrypted_persistence.query_log`; `main.js` / tray
  / тести оновлено.
- CodeQL `hard-coded-cryptographic-value` на нових фіксованих тестових ключах — очікувано
  dismiss «used in tests» (той самий клас, що T-146/T-165).

---

## 2026-09-05 — T-170: дефолтний набір провайдерів першого запуску

**Контекст:** `DEFAULT_PROVIDER_IDS` (`upstream.rs`) — набір voter'ів, увімкнених при першому
запуску, коли в `resolver_config.toml` немає `[[providers]]`. З Ф1 це були `quad9` + `adguard`
(два PoC-провайдери). SPEC.md §3.4/§3.5 наказує дефолт «тільки Security-категорія» (Quad9
Filtered + Cloudflare Malware). Розбіжність несена як відкрите рішення без номера через
T-72/T-73 (не змінювали поведінку першого запуску всередині великого рефактора) аж до Ф1
closure-плану, де стала гейтом Батча 3.9. Kickoff-AskUserQuestion (Три Б: це зміна того, що
фільтрується при першому запуску).

**Рішення:** `DEFAULT_PROVIDER_IDS = ["quad9", "cloudflare-malware", "adguard"]` — **два
Security-tier voter'и §3.4 (Quad9 Filtered + Cloudflare Malware) плюс AdGuard для реклами**.
Не чистий SPEC-варіант «тільки Security» і не старий `quad9` + `adguard`.

**Причина:** SPEC-й аргумент проти змішування категорій (сайт блокується з несподіваної для
користувача причини) слушний для Adult, але блокування **реклами** — очікувана з коробки
поведінка DNS-фільтра; лишати ads суто opt-in суперечило б інтуїції користувача про те, що
робить продукт. Cloudflare Malware додано, щоб security-ядро було справжнім OR-кворумом двох
незалежних threat-feed'ів, а не одним Quad9 (прямо адресує Ф1-гейт #1 — T-66 показав AdGuard
0/38, n=1, коли security-сигнал фактично давав лише Quad9). Adult лишається окремим
category-перемикачем, вимкненим за замовчуванням.

**Наслідки:**
- `upstream.rs`: константа + doc-коментарі `DEFAULT_PROVIDER_IDS` / `default_active_set`; новий
  тест `the_fresh_install_default_is_two_security_voters_plus_adguard` пінить склад (2 Security +
  1 ads), щоб тиха зміна набору була гучною.
- `dispatch.rs`: тестові асерти, що хардкодили `["quad9", "adguard"]` / `third_party_count` /
  `health.active_providers`, оновлено на набір із трьох (фікстури будуються з
  `default_active_set()`, тож продакшн-шлях слідує автоматично).
- `pipeline.rs` / `quorum.rs`: тестові хелпери `default_voters()` **відв'язано** від
  `DEFAULT_PROVIDER_IDS` — це тести механіки `handle_query`/`resolve` з фіксованою парою
  `quad9`+`adguard` (їхні `MockClient`/`MockDohClient` мокають рівно цих двох + baseline), не
  тести «що постачається». Реальний трипровайдерний дефолт покрито на рівні `dispatch.rs` +
  новий `upstream.rs`-тест.
- SPEC.md §3.4 (абзац «Дефолтний активний набір») і §3.5 (абзац «Дефолтний preset при першому
  запуску») — цей запис їх override.
- CONFIGURATION.md (`[[providers]]`-дефолт + приклад TOML), README (опис «з коробки»),
  UI-SPEC.md §Провайдери, CLAUDE.md — узгоджено.
- **Не** торкається `phase1_metrics.rs` (T-66/T-171 harness усе ще міряє baseline/quad9/adguard) —
  його оновлення на фінальний набір — робота T-171.

---

## 2026-09-05 — T-171: кворум-гіпотезу не підтверджено (n = 122)

**Контекст:** Ф1-гейт #1 (SPEC.md `## Фаза 1` closing-advisor / `## Фаза 2` «Перенесено з Ф2»):
T-66 виміряв detection-rate OR-кворуму проти окремих провайдерів на n = 38 живих URLhaus-доменів
і **не** показав приросту над найкращим одиночним провайдером (AdGuard тоді дав 0/38). Ф2/Ф3
стартували рішенням користувача *попри* цей відкритий гейт. T-171 — переміряти більшим зразком
уже фінальним T-170-набором і закрити гейт чесним записом (будь-яким).

**Прогін (2026-09-05, `examples/phase1_metrics.rs`, `SAMPLE_CAP = 150`, набір
`quad9` + `cloudflare-malware` + `adguard`).** Після baseline-`NoError`-фільтра + 4 транзієнтні
збої запиту до Quad9 → **n = 122**:

| voter | blocked | rate |
|---|---|---|
| Quad9 Filtered | 73/122 | 59.8 % (NeedsBaseline path — верхня межа) |
| Cloudflare Malware | 43/122 | 35.2 % |
| AdGuard Default | 0/122 | 0.0 % |
| Кворум (OR трьох) | 74/122 | 60.7 % |
| **дельта над найкращим одиночним (Quad9)** | **+1/122** | **+0.8 pp** |

**Рішення:** зафіксувати **«гіпотезу не підтверджено на n = 122»** (вже ~3× зразок T-66) і
**закрити Ф1-гейт #1** — не подальшим переміром. **Один прогін — один запис** (правило T-171:
не переганяти з іншим зразком/набором, доки не «підтвердиться»). Цифри — у PERFORMANCE.md
«Quorum coverage (T-66 / T-171)».

**Причина / що це означає:**
- OR трьох провайдерів зловив **рівно один** malware-домен, якого не зловив би сам Quad9. +0.8 pp
  — у межах шуму вибірки.
- Cloudflare Malware-блоки — майже підмножина Quad9-блоків (трейс: `quad9=true cloudflare=true`
  майже всюди; `quad9=false cloudflare=true` — 1/122). Два Security-tier-фіди тут сильно
  корельовані, не незалежні — саме те, що знецінює OR-кворум.
- AdGuard 0/122 — очікувано (у дефолті для **реклами**, T-170; ads-блоклист не перетинається з
  живим malware-URL-фідом), не дефект детекції (T-66 окремо звірив raw-відповіді).

**Наслідки:**
- **Не блокує Ф3.** Гейт закривався чесним числом, не гейтив на результаті.
- Цінність OR-кворуму над одним добрим Security-провайдером **не продемонстровано** жодним із
  двох вимірів → **відкрите дизайн-питання для Ф4+** (SPEC.md «Відкриті питання», новий пункт):
  ширший threat-корпус ніж один фід; вага на *незалежність* провайдерів (різні джерела
  intelligence), не на їх *кількість*; або переформулювати цінність як стійкість/резервування
  (будь-який провайдер лежить чи помиляється), а не адитивне покриття.
- `## Фаза 1` / `## Фаза 2` closure-абзаци SPEC.md — гейт #1 позначено закритим (посилання на цей
  запис). CLAUDE.md «Фаза 1 closure — two open gaps» — оновлено.
- Harness `phase1_metrics.rs` — тепер міряє трипровайдерний набір + дельту над найкращим
  одиночним (не «exactly one»).

**Доповнення 2026-09-05 (той самий день, за запитом користувача «додай усі DNS-фільтри й
прожени знову»):** harness розширено на всі 10 вбудованих пресетів, перепрогнано (n = 124).
Прогін виявив **баг у `BUILTIN_PRESETS`**: `cleanbrowsing-security` і `cleanbrowsing-adult`
оголошені `BlockSignature::NullIp`, але з цієї точки огляду **блокують через NXDOMAIN** (кожен
повернув NXDOMAIN на 66/124 доменів, з яких **18 не зловив Quad9**) — `is_blocked(NullIp, …)`
цього не бачить, тож усі їхні блоки **не рахуються** в `quorum::resolve`. Заведено **T-174**
(live-verify + фікс сигнатур CleanBrowsing). **Вердикт T-171 стає провізорним:** з правильно
працюючим `cleanbrowsing-security` (~53 %, +18 доменів понад поточний кворум) Security-tier OR
був би помітно вищим за Quad9 самотужки, не +0.8 pp. Чи підтверджується гіпотеза — не вирішено,
доки T-174 не полагодить сигнатури й вимір не повторять. Не переганяли ще раз тут (one-run
дисципліна; фікс — у T-174). Дані прогону: PERFORMANCE.md «Follow-up run — all 10 built-in
presets».

---

## 2026-09-05 — T-174: сигнатура CleanBrowsing виправлена → кворум-гіпотезу **підтверджено**

**Контекст:** T-171-доповнення (запис вище) виявило, що `cleanbrowsing-security` /
`cleanbrowsing-adult` блокують через NXDOMAIN, а `BUILTIN_PRESETS` каже `NullIp` → їхні блоки не
рахуються. Вердикт T-171 («гіпотезу не підтверджено») був явно позначений провізорним. Запит
користувача: T-174 + додати до `phase1_metrics.rs` рекламні й adult-домени.

**Рішення:**

1. **`cleanbrowsing-security` і `cleanbrowsing-adult`: `BlockSignature::NullIp` →
   `NullIpOrNxdomain`.** `NullIpOrNxdomain` (не `NxdomainVsBaseline`) — щоб регіон/qtype, який усе
   ж віддає `0.0.0.0`, лишався покритим (їхня опублікована поведінка — `0.0.0.0`; спостережена —
   NXDOMAIN на 67/126 malware і 10/10 adult, baseline зарезолвив усі). CleanBrowsing тепер
   позначено live-звіреним (через `examples/phase1_metrics.rs`, як Quad9/AdGuard у 2026-08-25).

2. **`phase1_metrics.rs` розширено на 3 корпуси** — `malware` (живий URLhaus, як раніше),
   `ads` (18 фіксованих well-known ad/tracker-хостів), `adult` (10 фіксованих
   найтрафіковіших adult-сайтів). Списки хардкоджені свідомо (стабільні, нема чистого
   публічного фіда); інструмент робить лише DNS-резолв, ніколи HTTP. Per-corpus-таблиця + NXDOMAIN-
   лічильник = вбудований signature-sanity-чек.

3. **Вердикт T-171 переглянуто: гіпотезу ПІДТВЕРДЖЕНО.** Перемір із виправленими сигнатурами
   (n = 126 malware):

   | | blocked | rate |
   |---|---|---|
   | Quad9 | 73/126 | 57.9 % |
   | Cloudflare Malware | 43/126 | 34.1 % |
   | **CleanBrowsing Security** | **67/126** | **53.2 %** |
   | Cloudflare Family (Adult, теж malware) | 45/126 | 35.7 % |
   | **Кворум OR Security-tier (4)** | **93/126** | **73.8 %** |
   | **дельта над найкращим одиночним (Quad9)** | **+20/126** | **+15.9 pp** |
   | Кворум OR усіх 10 | 94/126 | +21 / +16.7 pp |

   **19 malware-доменів заблокував _лише_ CleanBrowsing** (не Quad9, не Cloudflare Malware) — це
   і є приріст OR-логіки. Фіди **не** такі корельовані, як робив висновок T-171; той висновок був
   артефактом сигнатурного бага, що ховав CleanBrowsing цілком.

**Причина / що це означає:**
- T-66 (n=38) і T-171 (n=122) обидва недооцінювали кворум, бо 2 з 4 Security-провайдерів мали
  недетектовані блоки. З усіма 4 робочими OR-кворум дає **+15.9 pp** над найкращим одиночним — це
  саме той приріст, який виправдовує архітектуру, і якого раніше не було видно.
- **Ф1-метрик-гейт (гейт #1) тепер закрито підтвердженням**, не лише «чесним записом».
- Приватнісна ціна кворуму (N третіх сторін бачать історію) тепер має продемонстровану
  контр-цінність — SPEC.md «Відкриті питання» п.4 оновлено (з «не продемонстровано» на
  «+15.9 pp на n=126, T-174»).

**Наслідки:**
- `upstream.rs` — 2 сигнатури + `BlockSignature` doc-коментар; SPEC.md §3.4 (`NullIp`-список
  без CleanBrowsing; CleanBrowsing live-звірений). `## Фаза 1` / `## Фаза 2` closure-абзаци —
  гейт #1 з «не підтверджено» на «підтверджено (T-174, n=126)».
- `phase1_metrics.rs` — 3 корпуси + `Corpus` enum + `ADS_DOMAINS`/`ADULT_DOMAINS` + per-corpus
  звіт. PERFORMANCE.md «Quorum coverage» — переписано під фінальний результат.
- CLAUDE.md — Ф1-closure секція, Known-limitations (CleanBrowsing рядок → «виправлено T-174»).
- **Нове: T-175** — `adguard-family` / `opendns-familyshield` / `dns4eu-*` схоже блокують через
  провайдер-специфічний sinkhole-IP (не `0.0.0.0`, не NXDOMAIN), який жодна поточна
  `BlockSignature` не ловить (adult corpus: 0/10 при тому, що Cloudflare Family і CleanBrowsing
  Adult дали 10/10). Потребує нового варіанта `BlockSignature::SinkholeIp` — окрема задача,
  нижчий пріоритет (вторинні пресети, не shipped-дефолт).
- Raw-виходи прогонів: scratchpad `t174_3corpus_run_prefix_2026-09-05.txt` (до фіксу),
  `t174_remeasure_fixed_2026-09-05.txt` (після).

**Closing-advisor (T-174) — confound перевірено й відкинуто.** Advisor вказав: для
NXDOMAIN-сигнатурного voter'а «blocked» == «повернув NXDOMAIN» за побудовою (harness лишає лише
домени з baseline=NoError), тож приріст живе в зоні, де NXDOMAIN CleanBrowsing може бути
блоклист-хітом **або** різницею у видимості між рекурсивними резолверами на свіжих
URLhaus-доменах. **Перевірка:** у `phase1_metrics.rs` додано **другий незалежний нефільтрований
резолвер** (Quad9 unsecured `dns10`, `SECOND_BASELINE_URL` = `BASELINE_CHAIN[1]`) — домен
лишається, лише якщо **обидва** дають NoError. Перемір із двобейзлайновим гейтом (n = 106):
**0 доменів** відкинуто через розбіжність нефільтрованих резолверів (Cloudflare NoError, а
Quad9-unsec — ні) — confound не матеріалізувався взагалі. Кожен NXDOMAIN фільтруючого voter'а —
проти домену, який два нефільтровані резолвери погоджуються, що резолвиться. Дельта не впала, а
**трохи зросла: кворум OR Security-tier 76/106 (71.7 %) — +18 доменів / +17.0 pp над Quad9**
(проти +15.9 pp на першому проході з одним baseline), 17 malware-доменів зловив лише CleanBrowsing.
**Вердикт «підтверджено» стоїть, тепер із явно протестованим і відкинутим confound'ом.**
Raw-вихід: scratchpad `t174_advisor_twobaseline_2026-09-05.txt`. Побічно закрито
непослідовність, яку advisor теж зауважив: PERFORMANCE.md давав caveat «upper bound» лише
Quad9, а не CleanBrowsing (та сама сигнатура) — тепер обидва під тим самим двобейзлайновим
гейтом і з тим самим формулюванням.

## 2026-09-06 — T-175: sinkhole-IP-детекція через network-префікс (не новий `BlockSignature`)

**Контекст:** T-174-прогін по всіх 10 пресетах показав `adguard-family` / `opendns-familyshield`
/ `dns4eu-*` як «0 блоків» там, де мали б блокувати. Орієнтаційна проба (`examples/sinkhole_probe.rs`,
живі запити) з'ясувала: **жоден пресет не «нічого не фільтрує»** — вони блокують, підмінюючи
відповідь сталим провайдер-специфічним sinkhole/block-page IP (не `0.0.0.0`, не NXDOMAIN), який
`quorum::evaluate` не декодував. Це стосувалось і **shipped-дефолтного `adguard`** для частини
malware. Дві AskUserQuestion: (1) детектувати гібридно (хардкод-набір + baseline-guard), (2) на
тиху ротацію IP — не покладатись на точні `/32`, а на network-префікс власника + рекалібратор.

**Рішення:**

1. **Адитивний параметр `sinkhole_nets: &[SinkholeNet]` на `evaluate` / `is_blocked` / `known_signal`,
   НЕ новий `BlockSignature`-варіант.** Sinkhole композується з наявною сигнатурою: `adguard`
   лишається `NullIp` (для `0.0.0.0`-реклами) **і** отримує sinkhole-набір (для malware). Новий
   enum-варіант зробив би це взаємовиключним. `evaluate` перевіряє sinkhole **перед** `match
   signature`, але тільки якщо `!has_null_ip` (`0.0.0.0` — безумовний блок, виграє).

2. **`Signal::NeedsBaseline`, не `Signal::Blocked`.** Sinkhole IP — підмінена відповідь, але, на
   відміну від `0.0.0.0`, невідрізненна від реального резолву без baseline (домен *міг би*
   легітимно жити на тій адресі). Той самий baseline-guard, що й для голого NXDOMAIN: block лише
   коли baseline сам віддав `NoError`. **Наслідок під `fail_closed`:** якщо baseline сам
   `TimedOut`/`Errored`, `resolve_needs_baseline` → `unresponsive_signal(FailClosed)` → `Blocked`
   — новий сигнал у старій mode-залежній гілці, безпечний бік (консистентно з voter-таймаутом),
   задокументовано навмисно.

3. **Матч за network-префіксом власника (RDAP-звірено 2026-09-06), не за точним IP:**
   `adguard`/`adguard-family` → `94.140.14.0/24` (у зареєстрованому AdGuard `94.140.14.0/23`,
   містить їхні резолвери `.14/.15`; фільтр-інфра, не хостинг); `opendns-familyshield` →
   `146.112.61.104/29` (у Cisco OpenDNS `146.112.0.0/16`, історичний block-page-діапазон);
   `dns4eu-*` → `51.15.69.11/32` (**exact only** — IP у Scaleway `51.15.0.0/17`, загальний
   хостинг, `/24` там небезпечний). Матч: `(ip ^ net).leading_zeros() >= prefix` — без зсуву,
   без `<< 32` overflow-шляху (паніка на serve-шляху = watchdog restart loop). `SinkholeNet` без
   `Default` (`0.0.0.0/0` = match-everything), інваріант `prefix ∈ 1..=32`.

4. **Стійкість до ротації — 2 шари** (пасивний аларм відхилено): (1) префікс поглинає ротацію
   хост-бітів у мережі провайдера; (2) `examples/sinkhole_probe.rs` перетворено на **рекалібратор**
   — звірка канарковим доменом (`internetbadguys.com` — офіційний Cisco/OpenDNS тест-домен;
   стабільний adult-домен для `*-family`; свіжий URLhaus-хост для pure-malware) + власним доменом
   провайдера як контролем, друкує `IN`/`OUT` префікса. Не-CI, ганяти перед релізом. **Пасивний
   аларм «0-in-prefix за вікно» — відхилено** (advisor): предикат «одиничний *незнайомий* A»
   необчислюваний (CDN-варіативність домінує знаменник), справжній сигнал потребує персистованого
   історичного rate; окрема майбутня задача, якщо взагалі.

5. **Per-user фоновий рекалібратор** («раз на кілька годин») і **чиста структурна евристика**
   («той самий IP для ≥K доменів») — відхилено: перше — privacy/traffic-ціна на кожного
   користувача; друге — крос-запитовий стан біля decision-core + CDN false-positive.

**Перемір (`phase1_metrics.rs`, sinkhole-детекція увімкнена, n = 111 malware, гейт: два
нефільтровані резолвери, 0 розбіжностей):**

| | blocked | rate |
|---|---|---|
| Quad9 | 66/111 | 59.5 % |
| **DNS4EU Protective** (у T-174 = 0, block через sinkhole) | **92/111** | **82.9 %** |
| CleanBrowsing Security | 59/111 | 53.2 % |
| Cloudflare Malware | 39/111 | 35.1 % |
| AdGuard (shipped-дефолт) | 45/111 | 40.5 % |
| **Кворум OR (усі 10 / Security-tier 4)** | **99/111** | **89.2 %** |
| **дельта над найкращим одиночним (`dns4eu-protective`)** | **+7/111** | **+6.3 pp** |

**Причина / що це означає:**
- Гіпотезу **лишено підтвердженою** (дельта додатна: кворум ловить 7 доменів, яких не ловить
  жоден одиночний). Але маржа — **+6.3 pp**, не T-174-ві **+17.0 pp**: sinkhole-детекція зробила
  `dns4eu-protective` видимим як найсильніший одиночний (82.9 %), тож планка «найкращий одиночний»
  піднялась. Це **чесніша** цифра, не регрес вердикту.
- SPEC.md §"Відкриті питання" / Ф1/Ф2 closure-абзаци — T-175-уточнення дельти.
- `adguard` shipped-дефолт: sinkhole-детекція підняла його malware-rate (частина через
  `94.140.14.x`); кворум тепер рахує ці блоки.

**`representative_allow_answer` — латентний баг заодно закрито** (closing-advisor). До T-175
`NullIp`-сигнатурний voter, що віддав sinkhole-IP (напр. `94.140.14.33`), класифікувався як
`Signal::NotBlocked` — тобто цей block-page IP **міг піти клієнту як «справжня» відповідь**
(браузер відкрив би сторінку-заглушку провайдера, вважаючи її сайтом). Тепер sinkhole-відповідь
→ `NeedsBaseline`, тож не є кандидатом у `representative_allow_answer`; випадок «sinkhole-only +
baseline SERVFAIL → Allow» маршрутизується в наявний `answer: None`-шлях (той самий, що
deeply-degraded fail-open), не нову необроблену гілку. Звірено читанням fallback'а.

**AAAA-доповнення** (closing-advisor катч — `is_sinkhole_ip` спершу читав лише `RData::A`, а
`requires_quorum` пускає й AAAA). Проба AAAA (2026-09-06): `opendns-familyshield` sinkholить AAAA
на `::ffff:146.112.61.108` (IPv4-mapped — розгортається в v4-префікс, окремий запис не потрібен);
`dns4eu-protective` **і** `dns4eu-child` — на спільний native-v6 `2001:bc8:1640:3ffd:dc00:ff:fe4a:3ec9`
(Scaleway, стабільний по ≥3 доменах кожен, `joindns4.eu`-контроль чистий) → доданий `/128`, та
сама «no widening»-логіка. `adguard`/`adguard-family` — NODATA на AAAA, v6-sinkhole не
спостережено. `SinkholeNet.addr` став `IpAddr` (v4 `1..=32`, v6 `1..=128`).

**Наслідки:**
- `upstream.rs` — `SinkholeNet { addr: IpAddr, prefix: u8 }` (`v4()`/`v6()` конструктори, без
  `Default`) + `SINKHOLE_NETS` + `DNS4EU_SINKHOLE_NETS` + `sinkhole_nets_for` + тести (маска обох
  сімейств, межі, invariant, negative-control на власних доменах провайдерів, v6-точність).
- `quorum.rs` — `evaluate`/`is_blocked`/`known_signal` +param; 3 внутрішні виклики
  (`voter_record`/`combine`/`resolve`-early-block) + `representative_allow_answer`; `is_sinkhole_ip`
  читає A + AAAA (IPv4-mapped розгортається); ~10 нових тестів. 3 `resolve`-тести: фікстура
  `94.140.14.14` (реальний AdGuard-резолвер, тепер у sinkhole `/24`) → TEST-NET-3 `203.0.113.14`.
- `phase1_metrics.rs` — `sinkhole_nets_for(&spec.id)` для **кожного** пресета (ніколи `&[]`);
  opt-in `oisd:<n>` корпус (детермінований stride-семпл `big.oisd.nl`; **окрема секція, НЕ у
  кворум-вердикт** — oisd це блоклист, не known-bad, Security-резолвер коректно не блокує ad/tracker).
- `sinkhole_probe.rs` — з orientation-проби на постійний рекалібратор; питає A + AAAA.
- SPEC.md §3.4, CONFIGURATION.md, PERFORMANCE.md, CLAUDE.md, TASKS.md, TASKS-DONE.md.
- Raw-виходи: scratchpad `t175_sinkhole_probe_2026-09-05.txt` (орієнтація),
  `t175_phase1_metrics_2026-09-06.txt` (фінальний перемір, `+6.3 pp`; перший прогін того ж дня
  дав `+5.4 pp` на іншому URLhaus-семплі — дельта стабільна).

---

## 2026-09-06 — T-176: категорійний перемикач `/admin/ui` = атомарний маршрут; adult-дефолт `opendns-familyshield`

**Контекст:** T-176 (Батч 3.10) переробляє `/admin/ui` під нетехнічного користувача — basic-вигляд
із трьома категорійними перемикачами (malware / ads / adult) замість поштучних тумблерів
провайдерів. У бекенді категорійного перемикача не було: `Category` — лише групування
`ProviderView[]`, а `POST /admin/providers/set-enabled` міняє **один** voter і **перезаписує весь**
`resolver_config.toml`. Kickoff-plan+advisor (2026-09-06) виявив два питання, винесені на
AskUserQuestion.

**Рішення 1 — механізм: новий атомарний маршрут `POST /admin/providers/set-category-enabled`
`{ category, enabled }`.** Один `persist_lock`, один запис, все-або-нічого, echo `ProvidersResponse`
(та сама форма, що решта `/admin/providers/*`). Не клієнтський fan-out по `set-enabled`.

**Причина:** N послідовних `set-enabled` на категорію з N voter'ів = N повних перезаписів
`resolver_config.toml`; збій на 2-му з 3 лишає половинчастий стан на диску, а basic-вигляд читає
«ads: off», поки один ads-voter ще живий. Це рівно recurring user-safety-баг цього проєкту
(`persisted:false` мовчазно втрачається — T-57 / T-139 / T-149 / T-47 / T-77), і причина, чому
«Backend before UI» у CLAUDE.md recurring-patterns. Клієнт атомарності дати не може.

**Рішення 2 — adult-дефолт: порожня `ADULT_CONTENT` при `enabled=true` авто-додає
`opendns-familyshield`** (Cisco OpenDNS `FamilyShield`) у тій самій транзакції. Не
`cleanbrowsing-adult`, не `adguard-family`.

**Причина:** `DEFAULT_PROVIDER_IDS` (T-170) = `quad9` + `cloudflare-malware` + `adguard` — **нуль
voter'ів у ADULT_CONTENT**; SECURITY/ADS завжди мають свої дефолти й built-in'и видалити не можна,
тож adult — єдина категорія, що буває порожня на свіжій інсталяції. Без авто-`add` basic-перемикач
«дорослий контент» був би мертвий (найімовірніша річ, яку нетех-користувач хоче ввімкнути).
`opendns-familyshield` блокує через провайдер-специфічний sinkhole-префікс `146.112.61.104/29`
(+ IPv4-mapped AAAA) — детекцію додано в T-175, тож quorum його блоки **рахує**. Вибір із трьох
кандидатів — рішення користувача.

**Наслідки:**
- `upstream.rs`: `pub const EMPTY_ADULT_CATEGORY_DEFAULT_PRESET = "opendns-familyshield"` поряд із
  `DEFAULT_PROVIDER_IDS` (той самий клас — first-run policy); re-export у `lib.rs`.
- `admin.rs`: `SetCategoryEnabledRequest { category: Category, enabled: bool }` +
  `AdminClient::set_category_enabled`.
- `dispatch.rs`: `serve_admin_providers_set_category_enabled` (через наявний `apply_provider_change`
  — той самий `persist_lock` + cross-field-read); `ROUTES` +1 рядок + `EXPECTED_ADMIN_ROUTES`
  snapshot; 5 тестів (disable-цілу-категорію-один-запис, add-adult-дефолт-у-порожню, on/off/on
  без дублювання, off-порожньої = no-op, unknown-category + non-JSON → 400/415). Порожня категорія
  `enabled=false` — ідемпотентний no-op. `500` лише якщо `opendns-familyshield` зник із
  `BUILTIN_PRESETS` (внутрішній інваріант, не клієнтська помилка).
- **Не** чіпає `AdminConfigUpdate` (категорії редагуються тут, як `/admin/providers/*`, не в
  `/admin/config`). Головний перемикач «фільтрація увімк/вимк» — клієнт робить 3 послідовні
  виклики цього маршруту (без нового config-поля/міграції); фінальне рішення — closing-advisor T-176.
- UI-SPEC.md §3.1/§3.4, `diagrams/ui-navigation.md`, CONFIGURATION.md — синхронізуються в
  doc-кроці T-176.

**Closing-advisor T-176 (2026-09-06) — головний перемикач НЕ створює voter'а.** Перша реалізація
`flipAllCategories(true)` викликала `set-category-enabled` для всіх трьох категорій безумовно. На
свіжій інсталяції `ADULT_CONTENT` порожня → цей виклик потрапляв у авто-`add`-гілку й вмикав
`opendns-familyshield`. Тобто: вимкнути «Фільтрацію» → знову ввімкнути = увімкнена фільтрація
дорослого вмісту + 4-й провайдер, якого користувач не обирав. Суперечить Рішенню 2 вище (авто-`add`
— тільки для *явного* adult-перемикача) і T-170 («adult лишається окремим перемикачем, вимкненим
за замовчуванням»). **Фікс — клієнтський:** `flipAllCategories(enabled, providers)` пропускає
категорію без жодного сконфігурованого voter'а (`continue`), тож головний перемикач лише *фліпає*
наявних voter'ів, ніколи не *створює*. Бекенд `set-category-enabled` без змін. Тест
`main_js_master_switch_only_flips_categories_that_already_have_a_voter` (T-59-стиль — guard як дані,
не поведінка). **Асиметрія, лишена свідомо:** OFF→ON відновлює *всі* непорожні категорії (свідомо
вимкнена «реклама» повертається), а не попередній per-category вибір — «головний = все ввімкнено»,
а не «відновити знімок».

**Closing-advisor T-176 — супутні дрібниці того ж проходу:** `flipCategory` показував помилку
*до* `refreshProviders()`, який одразу перебудовує `#filter-controls-error` порожнім → повідомлення
зникало (порядок вирівняно з `flipAllCategories`); `#status-pill` («Сервіс доступний») прибрано —
дублював hero, якого немає в затвердженому макеті; `role="switch"`+`aria-checked="mixed"` на
частковому перемикачі → `role="checkbox"` (тільки checkbox приймає `mixed`), мертве CSS-правило
`:indeterminate` видалено.

## 2026-09-06 — тести UI/API: серверна межа, без JS-раннера

**Контекст:** скрупульозний аудит кожного маршрута vs його тестів (запит користувача). Виявлено:
(1) CSRF-гейт (`content_type_is_json`) скопійований у кожен обробник окремо, не централізований у
`serve()` — per-route список тестів не бачить майбутній маршрут, що забув гейт; (2) сигнал
`persisted: false` (recurring user-safety клас T-57/T-139/T-149/T-47/T-77) не перевірявся для
`/admin/providers/{add,set-enabled,set-category-enabled}`; (3) кілька мутаційних маршрутів мали
лише happy-тест (`set-enabled` невідомий id, `overrides/remove` відсутній запис,
`geoip/maxmind/clear` на порожньому стані); (4) **клієнтський `main.js` не має жодного
виконуваного тесту** — усі 17 тестів `admin_ui.rs` це `INDEX_HTML.contains(…)` /
`MAIN_JS.contains(…)` (присутність рядка у файлі, слабше за смоук).

**Рішення — зміцнюємо серверний бік межі, JS-раннер НЕ додаємо.** Причина: межа безпеки UI↔бекенд
(чотири категорії SPEC §8.1) за задумом лежить на сервері — webview у моделі загроз недовірений,
тож тести належать туди, де вони переважно й є. Node-тулчейн (`package.json`, CI-job, dep-audit
поверхня) у чисто-Rust репо заради presentation-шару суперечить дисципліні мінімальних залежностей
(`~/.claude/rules/cross-language-style.md` п.11). Структурний sweep по `ROUTES` (стиль T-59 —
властивість як дані) — найдешевша у підтримці форма: новий маршрут покривається в день додавання.

**Наслідки:** `dispatch.rs` +7 тестів — `every_json_post_route_rejects_a_missing_or_wrong_content_type`
(структурний sweep, покриває всі 15 JSON-POST + майбутні), `serve_admin_providers_{add,set_enabled,
set_category_enabled}_reports_not_persisted_when_no_config_path_is_set` (через спільний
`assert_not_persisted`), `serve_admin_providers_set_enabled_rejects_an_unknown_id` (400),
`serve_admin_overrides_remove_is_a_safe_no_op_for_an_absent_entry`,
`serve_admin_geoip_maxmind_clear_is_idempotent_on_a_fresh_state`. Клієнтська presentation-логіка
(`computeProtectionState`, `categoryState`, two-step confirm, `renderError` «не фейкові 0/0»,
`textContent`-не-`innerHTML`) лишається на: (а) структурних тестах-присутності в `admin_ui.rs`,
(б) задокументованому ручному chrome-devtools smoke (README «Перевірка: браузер → локальний DoH»,
T-172/T-176). Якщо presentation-баг колись пройде обидва — тоді окрема задача на JS-harness, не
раніше.

## 2026-09-07 — §5.1 (voter-scope виключення топ-сайтів) прибрано; злито в рейтинговий фільтр §5.3

**Контекст:** kickoff Фази 4. §5.1 описувала окремий always-on механізм: топ-N домени країни
виключаються з Ads/Adult-voters (Security лишається живим). Kickoff-гейти й уточнення задуму з
користувачем показали, що цей механізм у поточному вигляді не тримається.

**Рішення — «Варіант 1» (рішення користувача):**
- **§5.1 прибрано** (номер лишено із superseded-нотаткою — не депрекуємо розділ мовчки).
- Механізм топ-сайтів = **єдина opt-in «бульбашка»** — рейтинговий фільтр §5.3: домен поза
  зведеними зонами доступності → BLOCK; у зоні → звичайний конвеєр **без жодного звуження
  voter-набору**. Позиція — останній локальний крок перед кворумом (уже там і був).
- **Рейтинговий фільтр (§5.3) перенесено з Фази 5 у Фазу 4.** Фаза 5 = ccTLD-блок (§5.2) +
  i18n (T-151). Фаза 4 несе інфраструктуру per-country топ-N списку, яку раніше мала будувати §5.1.
- **§5.1.1 переформульовано** (T-138): не «персональний voter-scope-виняток», а **четверте,
  опційне (default-off) персональне джерело зон бульбашки** — лише додає домени, джерело сигналу
  незмінне (тільки вже-`ALLOW`-вердикти кворуму).
- **`voter_scope` / `VoterScopeView` DTO прибрано** (`admin.rs`, `query_log.rs` doc,
  `UI-SPEC.md`, `diagrams/ui-dto-model.md`) — жоден шлях більше не звужує voter-набір, тож
  `SECURITY_ONLY` непродукований. Поле ніколи не споживалось клієнтом (`ui/main.js`/`index.html`
  його не рендерять) — на практиці не breaking-зміна wire-форми `GET /admin/log`.

**Причина — у порядку ваги:**
1. **Уточнення задуму (2026-09-07).** Механізм топ-сайтів **завжди був однією фічею** — «не в
   списку → блок; у списку → далі конвеєром» — а не окремим always-on звуженням voter-набору.
   Поточний текст §5.1 (редакція 24 серпня, commit `31bd138`) — дрейф від цього задуму, внесений
   у попередній сесії. Тобто це **не** «знайшли проблеми → змінили дизайн», а «дизайн було
   неправильно транскрибовано».
2. **T-106 + T-104 — незалежне підтвердження, що транскрибована версія не спрацювала б.**
   T-106: Cloudflare Radar (назване в §5.1 джерело) — CC BY-NC 4.0; редистрибуція похідного
   per-country списку всередині Apache-2.0-продукту шипить компонент, чия ліцензія забороняє
   клас використання, який решта продукту дозволяє. Чистий fallback CrUX — CC BY 4.0, але
   origin-keyed і magnitude-bucket top-1000, не ординал top-50, під який писався risk-аргумент
   §5.1. T-104 (`examples/topn_fp_probe.rs`, n=318, топ-75/країну × 5 країн): Ads-tier FP ≈ 0 %
   на легітимних топ-сайтах (для дефолту чистити нема що); Adult-tier «FP» переважно **коректні**
   блоки реально-adult популярних сайтів (CrUX ранжує за трафіком, не темою) — blanket-виняток
   топ-N з Adult-voters **розблокував би** ці сайти, протилежне задуму.

**Наслідки в SPEC.md:** §5.1 → superseded-нотатка; §5.1.1 переписано; §5.3 → ядро Фази 4, чотири
джерела зон, крос-посилання на §5.1 прибрано; конвеєри §5 і §5.3 — крок «Voter scope» прибрано,
кроки перенумеровано (GeoIP лишається після кворуму — data dependency, §5.2); §6 таблиця логу —
рядок `voter_scope` прибрано; Відкриті питання п.7 і п.11 → закрито; Фазований план Ф4/Ф5 —
переписано.
**Наслідки в коді:** `VoterScopeView` + `LogEntryView.voter_scope` прибрано (T-179).
**Наслідки в TASKS.md:** блок «План виконання Ф4» переписано; T-109/T-110/T-112/T-113/T-114
знято; T-120–T-131 перенесено у Фазу 4; T-111/T-138 переформульовано; T-104/T-105/T-106 закрито.

---

## 2026-09-07 — T-185: навмисна зупинка/пауза застосунку через `stop.flag` / `quit.flag`

**Контекст:** Батч 3.12, живий прогін `v0.3.0` MSIX. `/admin/shutdown` термінує процес
`dnsqb-service`, але watchdog респавнить його за ~40 с — тож пункт трея «Зупинити фільтрацію»
на практиці = «пауза на 40 с», і немає жодного способу вимкнути застосунок повністю з трея
(«хто з ним сидітиме»). Перевірено: раніше навмисний shutdown-сигнал не проєктувався й не
відхилявся (SPEC рядок ~2213 лише каже, що `/admin/shutdown` термінує весь процес служби).

**Рішення:** два флаг-файли в теці app-data, чия **присутність** — увесь сигнал (вміст не
читається):
- `stop.flag` — пауза. Поки існує, watchdog **не респавнить** мертвий `dnsqb-service`.
- `quit.flag` — вихід. Watcher на наступному тіку зупиняє службу й виходить сам.

**Хто чистить:** **entry-point процес на старті** — `dnsqb-watcher::main` чистить обидва (свіжий
запуск = чистий стан). Heartbeat-луп watcher'а `stop.flag` лише **читає**, ніколи не чистить
там (інакше headless-запуск `dnsqb-watcher.exe` не зміг би лишатись на паузі). Повторний клік
плитки, що влучає в уже-живий watcher, чистить `quit.flag` (користувач перезапустив → скасувати
відкладений вихід), але лишає `stop.flag` (не роз-паузити мовчки).

Меню трея (Варіант B, узгоджено з користувачем): «Призупинити ↔ Відновити фільтрацію» (тогл за
`stop.flag`), «Відновити нагляд» (`ensure_sibling_running(Watcher)` — єдиний ручний шлях
підняти мертвий watcher), «Вийти з DNS Quorum Filter» (пише обидва флаги + вихід трея; за
нативним confirm-діалогом), «Сховати іконку» (лише трей). «Перезапустити» → «Скинути кеш і лог»
(без зміни поведінки — це `/admin/reset`).

**Причина:** флаг-файли, а не новий `/admin/*`-маршрут — щоб `dispatch::ROUTES` (і T-59 snapshot)
не мінялись, менший blast radius для хотфіксу; `dnsqb-service` не чіпається взагалі (флаги пише
трей). Окремі файли, не `watchdog-state.json` — §7.1 #7 (єдиний письменник) лишається. Правило
«clear on startup, honor in loop» — інакше deadlock: «Вийти» → клік плитки → застосунок виглядає
запущеним, а фільтрація не піднімається.

**Наслідки в коді:** новий `crates/dnsqb-service/src/lifecycle.rs` (`set/clear/…_flag`, re-export);
`dnsqb-watcher/main.rs` (чистить на старті + в `AlreadyRunning`-гілці лише `quit.flag`; loop
поважає `stop.flag` перед `Effect::Spawn` і виходить на `quit.flag`); `dnsqb-tray/main.rs`
(перебудова меню, `confirm_pause`/`confirm_quit`, live-тогл лейбла).

**Уточнення (closing-advisor Батча 3.12):**
- `stop.flag` **повністю заморожує** heartbeat-луп watcher'а — `LoopDriver::tick` не викликається
  взагалі, а не лише глушиться `Effect::Spawn`. Причина: pure-`tick` сам викликає
  `RestartBudget::register_attempt`, щойно бачить `Restarting`, і `transition` веде автомат до
  термінального `GaveUp` (§7 — з нього виходить лише перезапуск процесу). Глушіння самого ефекту
  (impure shell) відбувається запізно: ~5 спроб на паузі → `GaveUp` durable → після resume трей
  показує `ServiceGaveUp` на живій службі. Заморожений луп також перестає переписувати
  `watchdog-state.json` (протухає за `WATCHDOG_STATE_STALE_AFTER`) — чесний сигнал «нагляд є, але
  свідомо не супроводжує».
- Трей отримує окремий `TrayStatus::Paused` («фільтрацію призупинено»), який читається прямо з
  `stop.flag` у поллінг-потоці й ранжується **вище** за `watchdog_override`/admin-канал — інакше
  на паузі тултип був би оманливий «служба недоступна».
- Пауза **не переживає перезапуск застосунку** (плитка / логін → `dnsqb-watcher::main` чистить
  `stop.flag`). Це відображено в тексті `confirm_pause` — попередня редакція обіцяла, що пауза
  тримається до явного «Відновити», що неправда після рестарту.
**Наслідки в SPEC:** §7 — блок «Навмисна зупинка (T-185)»; §8 — меню трея.
**Наслідки в діаграмах:** `diagrams/process-lifecycle.md` — секція пауза/вихід стає реальною
(знято ⚠️ «заплановано»).

---

## 2026-09-08 — Батч 3.14: колір трей-іконки за станом; `is_trusted` наперед; порядок тегів

**Контекст:** T-183 (Батч 3.12) дав одну статичну білу гексагон-іконку трея. Користувач попросив
індикацію станом за патерном Google Drive / Dropbox: зелений — фільтрує ОК, жовтий — деградація /
перезапуск / офлайн, сірий — свідомо вимкнено, червоний — недосяжно / здалося / сертифікат не
встановлено. Окремо: `AdminClient` трея пінить власний сертифікат, тож на чистій машині без
довіреного cert трей чесно поллить «захищає», поки браузерний DoH до нас узагалі не
встановлюється й DNS тихо йде нефільтрованим (**Три Б**). Живий прогін показав, що застосунок не
вміє навіть *перевірити* довіру. Замовлення користувача — окремий Батч 3.14, «не критично».

**Рішення:**
- **Кольорова мапа** над наявними 7 варіантами `TrayStatus` + `cert_trusted: bool`, як чиста
  тотальна `status::icon_colour(TrayStatus, bool) -> IconColour` з вичерпним `match` без
  wildcard-гілки (майбутній 8-й варіант має не компілюватись, а не тихо стати зеленим):
  🟢 `Filtering{degraded==0}` · 🟡 `Filtering{degraded>0}` / `ServiceRestarting` / `Offline` ·
  ⚪ `Paused` / `NoActiveProvider` · 🔴 `Unreachable` / `ServiceGaveUp`.
- **Override cert-not-trusted → red фліпає ЛИШЕ `Filtering`.** Не `NoActiveProvider`, не `Paused`,
  не `Offline`/watchdog. `cert_warning`-суфікс у tooltip (`status::compose_tooltip`, патерн
  T-56 degraded-суфікса) натомість спрацьовує для `Filtering` **і** `NoActiveProvider` — червона
  іконка не може стояти поряд із tooltip «захищає» (той самий провал, що doc-comment `status.rs`
  «Honestly distinguished states» існує запобігти). Tooltip писати на зміну `(observed, trusted)`,
  не лише `observed`.
- **`is_trusted()` витягнуто наперед у 3.14** (вибір користувача, замість відкладання до T-188):
  read-only `pub fn trust_store::is_trusted(cert_path) -> Result<bool, TrustStoreError>`, спільне
  ядро `trusted_state` з `ensure_installed`, re-export з `lib.rs`. **Без HTTP-маршруту** —
  `GET /admin/cert-status` + `POST /admin/install-cert` лишаються в Батчі 3.13 / T-188; трей кличе
  lib-функцію напряму, як уже кличе `ensure_installed`. Окремий `std::thread`
  (`status::spawn_trust_watch`) тримає `Arc<AtomicBool>`; показуваний прапор seed `true` (червоний
  лише коли `certutil` *довів* недовіру; `cert.pem` відсутній = «невідомо», не «недовірений»);
  **каденс** (`next_delay`, closing-advisor Батча 3.14) ключиться на підтверджений `Ok(true)`, не
  на кеш `trusted` — `Ok(false)` **і** `Err` (перший полл до появи `cert.pem`, бо трей стартує
  раніше служби) обидва беруть драбину `2→5→15→60→300` с, `300` с лише після доведеної довіри;
  cert-пункти меню викликають `request_recheck()` після `certutil`-мутації.
- **Повноколірний гліф** (крапки-вершини несуть колір), палітра GitHub Primer
  (`green #3FB950` / `amber #F5A623` / `grey #8B949E` / `red #F85149`). **Без анімації вершин.**
  `assets/gen-icon.py` `make_tray_glyph(size, colour)` → 4 блоби
  `crates/dnsqb-tray/icons/tray-32-{green,amber,grey,red}-rgba.bin`; старий `tray-32-rgba.bin`
  видалено.
- **Порядок тегів:** `v0.3.1` тегається **після 3.14** (T-192) і покриває Батч 3.12 **+ 3.14**,
  закриваючи давно відкритий T-186. Майстер онбордингу (Батч 3.13) стає окремим **`v0.3.2`**
  (потребує власного бампу `0.3.1`→`0.3.2`).

**Причина:** override лише `Filtering` — SPEC §3/§8.1 вимагає показувати порожній voter-набір
**як окремий стан, не як помилку** (`ui-status-indicator.md` умова 4 — «явний pass-through, не
помилка»), а червоний — колір помилки; `Paused` захищений регресійним тестом
`paused_tooltip_names_the_state_plainly_and_not_as_a_failure` (T-185). `Filtering`+untrusted —
єдиний стан, де тихий збій браузерного DoH є проблемою, яку користувач мусить лагодити.
Окремий тред (не `spawn_blocking` на 2-с poll-лупі): `is_trusted` = два блокувальні
`certutil`-сабпроцеси; seed `true` — «невідомо» ≠ «зламано». `is_trusted` напряму з lib —
дзеркалить наявний `INSTALL_CERT_ID`-хендлер.

**Наслідки в коді:** `crates/dnsqb-service/src/{trust_store.rs,lib.rs}` (`trusted_state` +
`is_trusted` + 2 тести); `crates/dnsqb-tray/src/status.rs` (`IconColour`, `icon_colour`,
`cert_warning`, `compose_tooltip`, `TrustState`, `spawn_trust_watch` + тести);
`crates/dnsqb-tray/src/main.rs` (`TrayIcons`, `refresh_tray`, `spawn_cert_action`,
`handle_menu_event` дістає `&TrustState`); `assets/gen-icon.py` + 4 нові `.bin` (старий видалено).

**Наслідки в SPEC:** немає (SPEC §8 hero-стани не чіпано — це Батч 3.13 / T-188).
**Наслідки в діаграмах:** `diagrams/ui-status-indicator.md` — новий підрозділ «Колір трей-іконки»
(рендер наявної драбини + `cert_trusted` як ортогональний вхід, **без** нової умови й **без**
зміни порядку пріоритетів 2026-09-02 / 09-03); `diagrams/README.md` індекс.
**Наслідки в інших доках:** `SERVICES.md` §dnsqb-tray «Іконка»; `CLAUDE.md` (module-table,
tray-абзац, gen-icon рядок, `trust_store` surface); `UI-SPEC.md` (одне речення); `TASKS.md`
(секція Батч 3.14, T-191/T-192); T-186 закривається в T-192.

---

## 2026-09-08 — Батч 3.13 / T-188: майстер першого запуску пропонує встановити сертифікат; `cert-status` три-стан

**Контекст:** живий прогін `v0.3.1` MSIX на чистій машині без сертифіката: застосунок нічого не
пропонує нетехнічному користувачеві. `dnsqb-service` генерує `cert.pem`, але встановлення в
довірене сховище — **тільки** ручний пункт трея «Встановити сертифікат» (T-49), і застосунок
навіть не вміє *перевірити*, чи сертифікат уже довірений. T-99 / T-134 раніше закрили
**автоматичне** прописування DoH/довіри як несумісне з «без постійних підвищених прав» і Три Б
(тихий фолбек на системний резолвер). Потрібен середній шлях.

**Рішення:**
- **Майстер першого запуску = `rfd`-діалог [Так]/[Пізніше]** (0 нових залежностей; модалка —
  правильний патерн онбордингу за консенсусом UX-джерел; трей-нотифікація — для статусу після
  того, як застосунок прижився, не для онбордингу). На [Так] → наявний шлях `ensure_installed`
  → на успіх відкрити `/admin/ui` у браузері (Tailscale-стиль «клік → браузер»). Стартує лише
  коли `cert.pem` **підтверджено не довірений** (не оптимістичний seed — на чистій MSIX-інсталяції
  служба ще не написала `cert.pem`, коли трей стартує, T-187) **і** немає marker-файлу
  `onboarding.seen`. Повторний вхід — пункт меню «Майстер налаштування».
- **Автоматичне прописування DoH у браузер лишається поза обсягом** (T-99 / T-134 не
  переглянуто) — майстер лише встановлює серт і відкриває сторінку з інструкцією; вставлення
  адреси в налаштування браузера користувач робить сам (норма Fiddler / AdGuard / NextDNS —
  копіювати рядок).
- **`GET /admin/cert-status` — три-стан `CertTrustView` (`TRUSTED` / `NOT_TRUSTED` / `UNKNOWN`)**,
  не bool. `certutil` може не відповісти (свіжа інсталяція до генерації `cert.pem`, зламаний
  `certutil`); контракт `trust_store::is_trusted` каже трактувати це як «невідомо», ніколи як
  «недовірений». Згортання в bool сказало б користувачу зі зламаною перевіркою (re)встановити
  серт, що вже довірений, кнопкою, яка нічого не робить. Та сама закрита-проєкція форма, що
  `WatchdogStatusView` / `ArtifactOutcomeView`.
- **`POST /admin/install-cert`** — `ensure_installed` через `tokio::task::spawn_blocking`
  (синхронний `certutil` за crypt32-модалкою не має стопорити лістенер, що ще й обслуговує
  `/health`, watchdog-канал 3). Прецедент мутації trust store з маршруту — `POST
  /admin/uninstall-local-state` (T-70). Обидва маршрути в `FUZZ_EXCLUDED_ROUTES` (`cert-status`
  GET = 2 `certutil`-спавни/кейс).
- **`onboarding.seen` — marker-файл у app-data, НЕ в `lifecycle.rs`.** `lifecycle`-інваріант —
  «entry-point-процес чистить обидва прапори на старті»; цей marker мусить пережити кожен запуск.
  Пишеться на [Пізніше] і на **успішний** install, ніколи на невдалий (невдача на машині без
  довіреного серта має повторити пропозицію наступного запуску).

**Причина:** prompted-install на першому запуску — середній шлях між «нічого» (статус-кво,
Три Б) і auto-install (відхилено T-49 module-doc — fire-and-forget із `main.rs`). Три-стан
`cert-status` — прямий наслідок «unknown ≠ untrusted» контракту, підтверджений pre-impl advisor
Батча 3.13.

**Наслідки в коді:** `crates/dnsqb-service/src/{admin.rs,dispatch.rs}` (2 DTO-групи, 2 маршрути,
`cert_pem_path`, `FUZZ_EXCLUDED_ROUTES` +2, `EXPECTED_ADMIN_ROUTES` +2, 5 тестів);
`crates/dnsqb-service/ui/{main.js,style.css}` (hero cert-гілка + `.hero-action`);
`crates/dnsqb-tray/src/{onboarding.rs(новий),status.rs,main.rs}` (`TrustState.confirmed` +
`is_confirmed`, `maybe_offer_onboarding`, `run_setup_wizard`, пункт меню).
**Наслідки в SPEC:** §8 — абзац онбординг-флоу.
**Наслідки в діаграмах:** нова `diagrams/onboarding.md`; `ui-navigation.md` (пункт меню +
hero-стан), `ui-status-indicator.md` (hero cert-гілка), `ui-dto-model.md` (2 DTO + 2 маршрути) —
звірка; `diagrams/README.md` індекс.
**Наслідки в інших доках:** `UI-SPEC.md` (маршрути + hero-стан); `SERVICES.md` (майстер трея +
пункт меню + `onboarding.seen`); `CLAUDE.md` (module-table, tray-абзац). T-189 (per-браузер
картка + Firefox) і T-190 (`v0.3.2`) — попереду.

---

## 2026-09-08 — T-193: пауза фільтрації тримає службу живою й віддає нефільтрований baseline (перегляд T-185)

**Контекст:** живий прогін `v0.3.1` MSIX. Трей-пункт «Призупинити фільтрацію» (T-185, Батч 3.12)
писав `stop.flag` **і слав `POST /admin/shutdown`** → процес `dnsqb-service` виходив, watcher
бачив `stop.flag` і заморожував heartbeat-луп (не респавнив) → на `127.0.0.1:<port>` ніхто не
слухав → браузер **не резолвив нічого**. Діалог `confirm_pause` обіцяв «DNS піде нефільтрованим»
— обіцянка хибна: DNS іде не нефільтрованим, а мертвим (Три Б — користувач після паузи гірше,
ніж без фільтра, і діалог бреше). Первісне рішення T-185 (нижче, 2026-09-07) явно вибрало «не
чіпати `dnsqb-service` зовсім» заради меншого blast radius хотфіксу — цей запис його переглядає.

**Рішення:**
- **`stop.flag` стає сигналом, який читає сама служба.** Новий detached-полер
  `pause_watch::run_pause_watcher` (дзеркало `run_reachability_prober`) раз на секунду публікує
  наявність `stop.flag` в `AppState::filtering_paused` (`RwLock<bool>`, `Copy`, без `Arc` — як
  `reachability`). `pipeline::handle_query` знімає його раз на запит у `UpstreamContext.filtering_paused`
  і **зливає в наявну гілку `!ProviderEntry::any_enabled(voters)`** — пауза й «0 провайдерів»
  резолвляться ідентично: baseline pass-through, **ніколи не кешується**, лог `DecisionSource::Quorum`
  + порожні voters. Провайдери лишаються **enabled** (resume = нуль змін конфігу), просто не
  опитуються.
- **Порядок fast-path'ів:** overrides (blocklist/allowlist далі виграють) → offline (далі виграє —
  baseline теж недосяжний) → **paused** → cache → quorum. Заблокований домен під час паузи далі
  повертає `0.0.0.0`; обходяться лише quorum + GeoIP.
- **Трей:** «Призупинити» = **лише `set_stop_flag`**, без `/admin/shutdown`. Resume = `clear_stop_flag`
  + `ensure_sibling_running(Service)` (ідемпотентний no-op, крім випадку «і служба, і watcher
  померли під час паузи»). `confirm_pause` переписано на правду — **без** over-claim «усе
  нефільтроване» (blocklist блокує).
- **Watcher:** блок заморозки `stop.flag` + латч `supervision_frozen` + `Effect::Spawn if
  stop_flag_is_set` guard **прибрано**. Поки служба жива й здорова під час паузи, звичайний tick
  — no-op (3 канали відповідають, vote `Alive`, `RestartBudget` не витрачається, `GaveUp`
  недосяжний). Реальний краш служби під час паузи тепер **респавниться** (нова служба читає
  `stop.flag` на старті й повертається в bypass-режимі) — строго краще за старе «DNS мертвий до
  resume». Занепокоєння T-185 closing-advisor «guarding Effect у impure shell запізно → durable
  `GaveUp`» повністю розчиняється, щойно пауза не вбиває службу.
- **`/admin/status.paused: bool`** — нове поле. Без нього після цієї зміни hero `/admin/ui`
  показав би зелене «Захищено» під час трей-паузи (служба жива, провайдери ввімкнені, мережа
  Online) — хибне зелене, яке **вводить саме цей коміт**. `computeProtectionState` дістав гілку
  «Фільтрацію призупинено» (сірий `is-warn`), ранг offline > paused > 0-voters (=порядок конвеєра).
- **Трей `TrayStatus::Paused` — код деривації без змін**, `stop_flag_is_set` досі перша перевірка.
  Трей ранжує `Paused` **вище** за `Offline`; конвеєр і hero ставлять offline першим. Обидва
  читання коректні, трейове — дієве; навмисно, не «фіксити».
- **Вікно виходу ~5 с:** `QUIT_APP_ID` пише `stop.flag` тоді `quit.flag`; tick watcher'а 5 с →
  до ~5 с перед виходом застосунку служба віддає нефільтрований baseline замість фільтрованого.
  Прийнятно (застосунок усе одно зараз стане повністю нефільтрованим). Задокументовано, не
  інженерено навколо.

**Причина:** середній шлях між «пауза = мертвий DNS» (статус-кво, Три Б + брехливий діалог) і
«пауза = вимкнути всіх провайдерів через `/admin/config`» (потребує backup/restore стану
провайдерів — гірше). Дзеркалення offline-fast-path T-152 — той самий `Copy`-прапорець, знятий
раз на запит, один ранній `if`, без рестарту, без кешу.

**Наслідки в коді:** `crates/dnsqb-service/src/pause_watch.rs` (новий) · `dispatch.rs`
(`AppState.filtering_paused` + акцесори + 2 літерали `AdminStatusResponse` + `UpstreamContext`
знімок) · `pipeline.rs` (`UpstreamContext.filtering_paused` + гілка `handle_query` + 5 тестів) ·
`lib.rs` (`mod`/re-export) · `main.rs` (спавн полера) · `admin.rs` (`AdminStatusResponse.paused`) ·
`ui/main.js` (hero-гілка) · `crates/dnsqb-tray/src/main.rs` (`PAUSE_RESUME_ID`, `confirm_pause`) ·
`crates/dnsqb-tray/src/status.rs` (doc-коментарі) · `crates/dnsqb-watcher/src/main.rs` (прибрано
заморозку).
**Наслідки в SPEC:** §7 «Навмисна зупинка (T-185)» — пауза = нефільтровано-але-живо, не мертво.
**Наслідки в діаграмах:** `diagrams/process-lifecycle.md` (секція паузи, `stateDiagram`,
bullet); `diagrams/ui-status-indicator.md` + `diagrams/ui-dto-model.md` (поле `paused` / hero-стан).
**Наслідки в інших доках:** `crates/dnsqb-service/src/lifecycle.rs` module-doc; `CLAUDE.md`
(трей-абзац, gotcha «Guarding an Effect … → GaveUp» розчинилась, module-table, watcher-абзац,
Project state); `SERVICES.md`. Ships у `v0.3.2` (Батч 3.13, перед тегом T-190).

## 2026-09-08 — «Повністю видалити» = стоп застосунку + wipe теки app-data; `certutil` без вікна (T-194/T-195)

**Контекст:** живий прогін `dist\dns-quorum-filter-0.3.1.msix` (Батч 3.14) на чистій машині
виявив два дефекти.

**Рішення T-194 — `certutil` завжди спавниться з `CREATE_NO_WINDOW`.** До T-191 `trust_store`
викликав `certutil.exe` лише на явний клік у меню (рідко → блимання консольного вікна прийнятне).
T-191 додав фоновий `status::spawn_trust_watch`, що поллить `is_trusted` (**2 спавни `certutil` на
полл**) на драбині `2→5→15→60→300` с — на свіжій інсталяції без довіреного серта вікно блимало
кожні кілька секунд, ~5 разів. Новий приватний хелпер `trust_store::certutil_command(&Path) ->
Command` ставить `CREATE_NO_WINDOW` (`0x0800_0000`) під `#[cfg(windows)]`; усі 4 сайти
(`local_cert_thumbprint`, `store_lookup_output`, `ensure_installed`, `uninstall`) йдуть через
нього. `stdout`/`stderr` далі захоплюються через pipe.

**Рішення T-195 — «Повністю видалити» зупиняє застосунок і стирає всю теку app-data.** Перегляд
T-70 (Батч 3.8). Раніше пункт чистив довірений серт + 3 секрети Credential Manager й **лишав усе
запущеним** — `dnsqb-service` на наступному циклі регенерував `cert.pem` і мінтив нові ключі,
частково скасовуючи очищення; `query-log.enc`/`cache.enc`/config/logs у `%LOCALAPPDATA%\dns-quorum-filter`
не чіпались узагалі (видалення MSIX через Windows їх теж не чистить). Обсяг обрав користувач
(питання 2026-09-08): **уся тека**. Новий порядок у треї (`spawn_remove_all_and_quit`):
`remove_all_local_state` (серт + секрети — не файли в app-data) → звіт-діалог → `stop.flag` +
`quit.flag` (watcher наступним tick зупиняє службу й виходить) → **detached прихований
`powershell.exe`** (`self_uninstall.rs`, `DETACHED_PROCESS | CREATE_BREAKAWAY_FROM_JOB` +
raw-OS-error-5 fallback, дзеркалить `watchdog::spawn::spawn_detached` — MSIX-дерево job-contained,
без breakaway job убив би прибиральника разом із треєм) → відкрити `ms-settings:appsfeatures` через
`explorer.exe` → `QUIT_REQUESTED` (трей виходить на наступному tick). Прибиральник **спершу чекає**
на зникнення `dnsqb-service`/`dnsqb-watcher`/`dnsqb-tray` (кеп ~20 с); якщо котрийсь вижив —
`exit` **без** видалення (інакше стер би `stop.flag`/`quit.flag` з-під живого watcher'а); інакше
`Remove-Item -Recurse -Force` у retry-циклі.

**Причина (самозамкнення):** трей тримає `tray.lock` відкритим (`share_mode(0)`), watcher/служба
— свої; `tao` `event_loop.run` не повертається (розходиться в `process::exit`), тож post-run
cleanup немає й `_guard` не дропнути. Жоден процес DNS-QF не може `remove_dir_all` власну теку →
зовнішній прибиральник. Wait-loop **обов'язковий**: `stop.flag`/`quit.flag` — звичайні файли в
тій самій теці, і якби `Remove-Item` спрацював до 5-с tick'а watcher'а, той не побачив би
`quit.flag`, не зупинив би службу, а `ensure_sibling_running(Service)` респавнив би службу в теку,
яку саме стирають. Це також розчиняє орфан-`.enc` після видалення ключа (advisor) — повне
стирання теки прибирає будь-який `.enc` незалежно від того, що persister-flush записав.
`build_wipe_script` фенсить ціль: шлях під `%LOCALAPPDATA%` **і** остання компонента = точно
`dns-quorum-filter`. `explorer.exe <shell-URI>` (не `rundll32 … FileProtocolHandler`, чия
підтримка app-протоколів не підтверджена). HTTP-роут `/admin/uninstall-local-state` лишається
«лише секрети» — крутиться *в* службі, не може видалити власну відкриту теку.

**Наслідки в коді:** `crates/dnsqb-service/src/trust_store.rs` (`certutil_command`); нові
`crates/dnsqb-tray/src/self_uninstall.rs`; `crates/dnsqb-tray/src/browser.rs`
(`open_windows_apps_settings`, `system_root_exe`); `crates/dnsqb-tray/src/main.rs` (`QUIT_REQUESTED`,
`spawn_remove_all_and_quit`, `confirm_remove_all_local_state`, event-loop); `crates/dnsqb-service/src/local_state.rs`
(module-doc).
**Наслідки в доках:** `SERVICES.md`, `CLAUDE.md` (трей-абзац, `trust_store` row, gotcha, Project
state), `diagrams/process-lifecycle.md` (S13). Заходять у `v0.3.1` перед тегом (Батч 3.14);
`dist`-MSIX перезбирається.

**Рішення T-196 — трей-іконка не «кричить» на відновлений блип.** Живий прогін: іконка лишалась
жовтою при робочій фільтрації. Причина — `icon_colour` фарбував `Filtering` у amber на будь-який
`degraded_events > 0` (T-191), а це трейлінговий лічильник за останні 20 quorum-запитів: один
тайм-аут апстріма застрягав жовтим на весь таскбар, поки 20 чистих запитів не виштовхнуть його з
вікна. Виправлено: amber для `Filtering` **лише** коли `degraded_events == degraded_window > 0`
(усі останні quorum-запити деградували → фільтрація фактично не відбувається — не хибний
all-clear, який дав би `degraded_events > 0` → drop; `degraded_counts` покриває `BASELINE_FALLBACK`,
тобто «усі фільтри мовчать»). Частковий лічильник → зелена іконка + наявний суфікс тултіпа «N/M
останніх». `degraded_window == 0` (свіжий старт, ще нема quorum-записів) → зелена.
**Наслідки в коді:** `crates/dnsqb-tray/src/status.rs` (`icon_colour` — одна гілка + тести).
**Наслідки в доках:** `SERVICES.md`, `CLAUDE.md`, `diagrams/ui-status-indicator.md`. Заходить у
`v0.3.1` перед тегом.

## 2026-09-09 — T-108: гігієна рейтинг-фільтра — лінива під час роботи, не bulk-скан при курації

**Контекст:** перший підхід до T-108 (Батч 4.1) фільтрував опублікований топ-N на етапі курації —
`examples/curate_topn.rs` для кожного з ~1000 registrable-доменів робив two-baseline-gate + запит
до кожного вбудованого Security/Adult-пресета й відкидав ті, що блокуються. Живий прогін показав
(а) Quad9 з dev-адреси починає віддавати `error sending request` після сотень швидких запитів —
класичний рейтлімітинг/бан адреси; (б) навіть на GitHub-раннерах частка помилок стрибала 0 %
(`gb`) → 45 % (`global`), а пропущений через помилку домен у BLOCK-only бульбашці = **заблокований**
домен, і файл цього не показував; (в) ~20 % «adult» drop — переважно реальний porn/gambling-хвіст
CrUX (T-104), але тулза не виводила список відкинутого, тож рев'ю було наосліп.

**Рішення:** курація **не робить жодного DNS**. `curate_topn` лише тягне CrUX-список, нормалізує
origin→registrable (PSL) і пише сирий популярний набір. Гігієна переїжджає в клієнт (Батч 4.3) як
**лінива, під час роботи**: домен у зоні йде звичайним конвеєром, кворум його як завжди перевіряє;
якщо кворум блокує (Security/Adult voter) — клієнт прибирає домен з локального набору зони, і
наступний lookup трактує його як поза-зони. Зона самоочищається на тих ~1 тис. сайтів, які
користувач реально відвідує.

**Причина:** (1) 1000 DoH-запитів до кожного публічного резолвера з однієї адреси на кожну
регенерацію — прямий шлях до бану/рейтліміту цієї адреси. (2) Кворум усе одно стоїть далі по
конвеєру — подвійна перевірка того самого на етапі курації не додає захисту, лише коштує запити й
надійність. (3) Сирий список простіший, детермінований, рев'ю — це «чи правильний це популярний
набір», не «чи вгадала евристика гігієни». (4) Контентна політика лишається у кворумі, де нею
керує користувач (увімкнув adult-voters → adult блокується; ні → ні) — курація не «зашиває»
думку конкретного набору adult-фільтрів у список.

**Наслідки в коді:** `crates/dnsqb-service/examples/curate_topn.rs` — прибрано `screen_registrable`,
`query_with_retry`, DoH-клієнт, baseline-константи, запити до пресетів; лишились fetch + PSL +
`build_list` (чиста) + запис. `.github/workflows/topn-curate.yml` — без live-DNS, один job, швидко.
Ліниве прибирання на клієнті — задача Батча 4.3 (з T-124–T-126). **Для 4.3:** прибрані домени
**не** можна тримати у завантаженому `<list>.txt` (atomic-swap на кожен refresh їх зітре) —
потрібен окремий per-list локальний overlay у app-data; зона на момент матчу =
`downloaded_list − removed_overlay`. Прибраний домен лишається прибраним і після вимкнення/
ввімкнення фільтра (інакше зона щоразу «перевчає» ті самі домени).
**Наслідки в доках:** SPEC.md §5.3 п.1 (гігієна = лінива рантайм), `data/topn/README.md` («NO
content filtering here»), CONFIGURATION.md, CLAUDE.md (Commands). Перекриває опис T-108 у
TASKS.md §"Фаза 4" від 2026-09-07.

---

## 2026-09-09 — T-108/T-124: overlay лінивої гігієни — in-memory, окремий lock, exact-match

**Контекст:** запис вище (той самий день) намітив «окремий per-list локальний overlay у
app-data; зона = `downloaded_list − removed_overlay`; прибране лишається прибраним і після
вимкнення/ввімкнення фільтра». При реалізації в Батчі 4.3 (T-124) три деталі уточнено —
closing-advisor kickoff.

**Рішення:**
- **Єдиний overlay, не per-list.** «Per-list» додавало б сенсу лише для granularity «пере-додати
  домен, якщо нова версія списку його все одно викинула» — маргінально. Вимоги, що справді
  важать (пережити refresh списку; пережити тумблер), задовольняє один набір registrable-рядків.
- **In-memory, не на диску (цей батч).** `AppState::rating_filter_removed:
  RwLock<Arc<HashSet<String>>>`, не персистується. Після рестарту відновлюється ліниво: перший
  запит раніше-прибраного домену знову йде в кворум → блокується → знову прибрано (один
  round-trip, той самий прийнятний tradeoff, що T-97 «свіжий Block після рестарту»). Так уникнуто
  нового споживача `persistence-key` і нового on-disk сховища browsing-derived даних —
  **персистенція overlay відкладена в Батч 4.5** (його шифроване персональне сховище — природне
  місце).
- **Окремий lock від зони.** `rating_filter_zone` (`RwLock<Arc<ZoneLists>>`) і
  `rating_filter_removed` — окремі поля з окремими єдиними писарями (updater vs
  `record_zone_removal`). Той самий `geoip` / `geoip_countries` split — щоб refresh списку не
  зітер прибирання, і навпаки. **Не** `Arc::make_mut` на спільному `ZoneLists` (гонка + deep-copy
  кожного `HashSet` на кожне прибирання).
- **Exact-match only.** Прибирати домен лише коли кворум заблокував **сам registrable**
  (`log_domain == matched_registrable`). Блок субдомену (`sub.example.co.uk` — один malware-хост)
  → нічого не прибирати: кворум і так блокує його щоразу за нуль ціни, а виселення цілого
  `example.co.uk` (легітимний топ-сайт) — чистий мінус.

**Причина:** мінімальна поверхня для батча без UI; жодного нового крипто/on-disk-сховища
browsing-даних до Батча 4.5, який його й проєктує; lock-модель — уже благословлений у CLAUDE.md
патерн; exact-match прибирає хибне over-blocking.

**Наслідки:** `AppState` (три поля + accessors), `pipeline::handle_query` крок 5 +
`QueryLogMeta::zone_removal` (action-сигнал, не лог-поле), `dispatch::resolve_doh_request`
(`record_zone_removal`). SPEC.md §5.3 п.1, CONFIGURATION.md, CLAUDE.md. Кеш (крок 4) вище
фільтра, тож свіжий кворумний Block маскує ефект прибирання до спливу `block_verdict_ttl` — це
правильно за SPEC §5.3, зафіксовано як known limitation.

## 2026-09-10 — T-127: `validate_rating_filter_lists` звужено до членства в `AVAILABLE_TOPN_LISTS`

**Контекст:** Батч 4.3 (`c64e1db`) валідував `[rating_filter] lists` лише за
**формою** (два малих латинських літери або `"global"`). Код правильної форми, але
без датасету (`"fr"`), проходив і на завантаженні конфігу, і в
`POST /admin/rating-filter`. `topn_updater` качає з фіксованого URL
(`raw.githubusercontent.com/<репо>/main/data/topn/<код>.txt`) — такий код 404-иться
щоцикл, дає 0 доменів, і `lists = ["fr"]` осідає у вічному Fork B
(«увімкнено, списки завантажуються») без жодної підказки чому. UI до `6289903`
взагалі ховав такий код (перебирав тільки `available_lists`) — зона в конфізі,
а в картці її не видно й не прибрати.

**Рішення:** `validate_rating_filter_lists` тепер робить дві впорядковані перевірки —
(1) форма → `ConfigError::InvalidRatingFilterList` (друкарська помилка `"uka"`),
(2) членство в `topn_download::AVAILABLE_TOPN_LISTS` → новий
`ConfigError::UnknownRatingFilterList` (форма правильна, датасету нема). Обидва —
`400` на маршруті (як уже сьогодні з `"ukr"`) / гучна помилка старту на завантаженні.
Після цього `lists ⊆ available_lists` — гарантований інваріант для всіх споживачів.
Обʼєднання в `main.js` (`displayCodes`) **лишається навмисно** — оборонний код для
конфіга, відредагованого до цієї перевірки: без нього такий файл втрачає єдиний
видимий спосіб прибрати погану зону.

**Причина:** URL завантаження фіксований, тож код поза `AVAILABLE_TOPN_LISTS` не
може запрацювати ніколи — тільки постійний інертний Fork B. Конфіг, що гучно не
завантажується, строго кращий за той, що завантажується й мовчки не фільтрує (той
самий принцип «hard TOML cutover, no shim», T-144/T-145). Фіча default-OFF і їй
кілька днів — реальний конфіг із таким кодом майже гіпотетичний. Дві перевірки, а
не одна: `"uka"` (не та довжина) і `"fr"` (нема датасету) — різні помилки оператора
з різними виправленнями, тож різні повідомлення.

**Наслідки:**
- `config.rs`: `+ConfigError::UnknownRatingFilterList`, `validate_rating_filter_lists`
  двоетапна, `use crate::topn_download::AVAILABLE_TOPN_LISTS`. Тести:
  `load_rejects_a_rating_filter_list_code_with_no_dataset`; `dispatch` —
  `serve_admin_rating_filter_rejects_a_code_with_no_dataset` (був
  `..._accepts_a_well_formed_code_outside_the_available_set`).
- `AVAILABLE_TOPN_LISTS` тепер гейтить **завантаження конфігу**, не лише вибір у UI:
  **додати** код — сумісно назад, **прибрати** — ламна зміна (конфіг, що досі обрав
  прибраний код, не завантажиться). Зафіксовано в doc-коментарі константи.
- `ResolverConfig::save` пише `self.rating_filter.lists` без ревалідації — безпечно:
  жоден шлях не сіє `.lists` повз `validate_rating_filter_lists` (load `config.rs:490`,
  маршрут `dispatch.rs:2157`, reset перечитує з диска).
- CLAUDE.md «Known limitations» / CONFIGURATION.md — оновити в Коміті 6.

---

## 2026-09-10 — T-204 (знахідка 3-B): вирішальна логіка hero `/admin/ui` — на сервер; обсяг «Максимум»

**Контекст:** пріоритетні сходи hero-статусу `/admin/ui` (watchdog > offline >
paused > 0-voters > cert), guard масового перемикача фільтрації (не вмикати
дорослу категорію без воутера) і fold стану категорій жили в `main.js` і
верифікувалися **лише** `assert!(MAIN_JS.contains("…"))`. Жоден рядок не
виконувався в тестах (свідомо немає JS-тулчейну) — форма «тест проходить, не
доводячи властивість» (урок T-59) на поверхні, що вмикає/вимикає фільтрацію.
Питання обсягу: чи тягнути сюди cert-гілку (потребує кешу `is_trusted`, інакше
+2 `certutil` на кожен 2 с-poll `/admin/status`).

**Рішення:** перенести вирішальну логіку на сервер як обчислені поля DTO —
`AdminStatusResponse.hero_state: HeroStateView` (чиста `admin::compute_hero_state`),
`ProvidersResponse.{category_states, master_switch_targets}` (чисті
`category_filter_views` / `master_switch_targets`). `main.js` → чистий рендер
(`HERO_PRESENTATION` мапа). **Обсяг «Максимум»** (рішення користувача через
AskUserQuestion): cert-гілка теж на сервер → окремим комітом наперед зроблено
**T-211** (кеш `trust_store::is_trusted` у `AppState.cert_trust:
RwLock<Option<CertTrustView>>`, фоновий 60 с-poll `cert_watch`). `hero_state` —
влада **лише** для hero `/admin/ui`: трей (`status.rs::from_response`) тримає
власний, свідомо інший ранкінг (Paused вище watchdog — див. запис 2026-09-07 /
2026-09-08 T-193).

**Причина:** патерн у репо вже двічі застосовано (`rating_filter_is_active` «єдина
влада» на сервері; `dnsqb-tray/status.rs` виокремлення чистих функцій) — це
рефактор за наявним патерном, не нова архітектура. «Максимум» бо T-204 і так
ставить cert-trust на 2 с-poll `/admin/status` — передумова кешування T-211 вже
виконана, тож半-міра лишила б клієнтську cert-гілку єдиним нетестованим шматком.
`cert: None` («фоновий poll ще не дав відповіді») → `Protected`, **не**
`CertUnknown` (advisor — трей зробив цю саму помилку до `TrustState::is_confirmed`,
T-188: свіжа інсталяція не має читатися як «зламано»). `SERVICE_UNREACHABLE`
лишається клієнтським — сервер не може повідомити про власну недоступність.

**Наслідки:**
- `admin.rs`: `+HeroStateView` (8 варіантів, `SCREAMING_SNAKE`), `+CategoryToggleState`
  / `CategoryFilterView`, `+CATEGORY_ORDER`, чисті `compute_hero_state` /
  `category_filter_views` / `master_switch_targets`. `AdminStatusResponse.hero_state`
  **без `#[serde(default)]`** — консистентно з рештою полів; версіонування DTO —
  окремо T-205 / 3-A (ризик прийнято як низький: MSIX атомарний, path-deps +
  `Cargo.lock`).
- `dispatch.rs`: обидва білдери `AdminStatusResponse` кличуть `compute_hero_state`;
  `providers_view` заповнює 2 нові поля.
- `main.js`: `computeProtectionState` → `HERO_PRESENTATION` + `heroPresentation()`;
  `renderError` → `SERVICE_UNREACHABLE`; видалено `certTrust` / `refreshCertStatus`
  / fetch `/admin/cert-status` / visibilitychange; `categoryState()` →
  `categoryStateFrom(data)`; `flipAllCategories` ітерує `data.master_switch_targets`.
- `admin_ui.rs`: 4 `contains`-тести перенацілено на рендер-звʼязку; вирішальні
  ассерти → `admin::hero_and_category_tests` (15 нових Rust-тестів).
- SPEC.md §8/§8.1, UI-SPEC.md §3.1/§4, `diagrams/ui-status-indicator.md` —
  індикатор обчислюється сервером; DTO-таблиці += 3 поля.
- T-210 (демоут внутрішніх re-exports) лишається опційним хвостом, не зроблено тут.

---

## 2026-09-11 — T-205 (знахідка 3-A): версіонування адмін-DTO — `#[serde(default)]` на additive-полях + `schema_version`

**Контекст:** `AdminStatusResponse` (+ вкладені `*View`) — єдиний крос-процесний
контракт репо без явного версіонування. Кожен інший (`FRAME_VERSION`,
`STATE_SCHEMA_VERSION`, `PersistedFileV1`, header-байт `encrypted_file`) має
версійне поле/байт або `#[serde(default)]`-additive-дисципліну. Новий `dnsqb-tray`
проти старого `dnsqb-service` → `serde_json` падає «missing field» (напр.
`rating_filter` з Батч 4.4, `hero_state` з T-204). Вплив низький (MSIX атомарний,
`pack-msix.ps1` кросс-звіряє версії, path-deps), але це невідповідність власній
нормі проєкту.

**Рішення:** Варіант 1 знахідки. `pub const ADMIN_DTO_SCHEMA_VERSION: u32 = 1` +
`AdminStatusResponse.schema_version` (`#[serde(default)]` → відсутнє = `0`).
`#[serde(default)]` на **additive** полях (усе, додане після Ф1-контракту T-52:
`serve_baseline_when_filters_unreachable`, `network`, `paused`, `baseline_endpoint`,
`watchdog`, `encrypted_persistence`, `rating_filter`, `hero_state`) + `Default` /
`#[default]` на їх типах із чесним нулем (`NetworkStatusView`→`Online` = «вважати
доступним», як seed `NetworkReachability::default`; `BaselineEndpointView`→`Primary`;
`HeroStateView`→`Protected` = «без тривоги»; `EncryptedPersistenceView`/
`RatingFilterStatusView` → все off). **Load-bearing Ф1-поля лишаються строгими**
(`active_providers`/`timeout_mode`/`timeout_ms`/`port`/`stats`/`persisted`): їх
відсутність — реальна помилка «це не той DTO», не defaulted-нуль (пом'якшення
тредофу «`default` маскує missing-field баг», який знахідка називає).
`ProvidersResponse` — `#[serde(default)]` на T-204-полях (Vec, без derive).
`AdminClient::{status,apply,reset}` після декоду кличе `warn_on_schema_mismatch` —
`tracing::warn!` на розбіжність, повертає `Ok` (best-effort).

**Причина:** робить контракт консистентним із рештою репо за ~derive-атрибути;
new-consumer-old-service graceful; версійне поле дає оператору сигнал перезібрати.
`hero_state` теж `#[serde(default)]` попри те, що трей його не читає, а `/admin/ui`
— той самий процес: консистентність дисципліни дешевша за розбір, який саме
підмножині полів вона потрібна, а `Protected` як `#[default]` — безпечний нуль
(та сама «unknown ≠ not-yet-known» логіка, що `cert: None` у T-204). `u32` (не
`u8`) — простір для схем; `= 1` бо це перша версіонована ітерація.

**Наслідки:**
- `admin.rs`: `+ADMIN_DTO_SCHEMA_VERSION`, `+warn_on_schema_mismatch`, `#[serde(default)]`
  × 9 полів, `Default` × 5 типів. `admin::dto_versioning_tests` (4). `lib.rs` re-export.
- `dispatch.rs`: обидва білдери стемплять `schema_version: ADMIN_DTO_SCHEMA_VERSION`;
  `serve_admin_status_returns_the_default_live_settings` += перевірка стемпа.
- `dnsqb-tray/status.rs` тест-фікстура += `schema_version`.
- CLAUDE.md `admin` рядок, UI-SPEC.md §4 — правило «додав поле → бампни const +
  `#[serde(default)]`».
- Не робилось: `GET /admin/version` (Варіант 3 — зайве для PET);
  `deny_unknown_fields` на request-DTO (окремо, якщо колись).

## 2026-09-11 — T-210 (знахідка 4-B): оркестрація `main.rs` → lib `orchestrate::run`, звуження публічної поверхні

**Контекст:** `lib.rs` реекспортував ~50 `pub use`-рядків (майже всю внутрішню
машинерію), бо `dnsqb-service/src/main.rs` — окремий крейт ([[bin]]-таргет лінкує
lib як зовнішню залежність), тож усе, що потрібно самому оркестраційному коду
(`handle_query`, `wire::*`, десяток `run_*`/`load_*` персистенс/апдейтер-хелперів),
мусило бути `pub`, не `pub(crate)`.

**Рішення:** Варіант 1 ревʼю (єдиний серйозно розглянутий — варіант 2, окремий
`dnsqb-core` крейт, дорожчий і ревʼю сам не рекомендує). Перенести тіло `main.rs`
дослівно в новий lib-модуль `orchestrate.rs` (`pub async fn run()`); `main.rs` →
3-рядковий шим (`dnsqb_service::run().await`). Звуження — **компілятор-кероване, не
ручний grep і не `pub(crate) use`**: видалити реекспортну групу цілком → зібрати →
`E0432` називає точний символ і зовнішнього споживача, який його ще тримає → повернути
лише той символ. `pub(crate) use` була б неправильним проміжним станом — жоден
внутрішній модуль не імпортує сам себе через `lib.rs`'s реекспортний шлях, тож
`pub(crate) use` без жодного внутрішнього вжитку = гарантований `unused_imports`
під `-D warnings`.

**Скоригована оцінка обсягу (advisor-catch до реалізації):** перший чорновик плану
оцінював «~60 груп → ~25», порахувавши лише 4 класи зовнішніх споживачів
(`dnsqb-tray`, `dnsqb-watcher`, `tests/{admin_client,conformance}`, `lib.rs`'s
doctests). `examples/*.rs` — пʼятий клас, який CI реально компілює й запускає
(`cargo test --workspace --examples`), пропущений спочатку — виявилось, що він
тягне майже весь `upstream` і частину `quorum`/`wire` незалежно від T-210. Чесний
результат: **49 → 32 `pub use`-рядки** (виміряно, не оцінка) — реальне звуження, але
скромніше, і зосереджене на тому, що було виключно оркестраційним: `cache_persist`,
`log_persist`, `encrypted_file`, `tls`, `listener`, `key_store`, `reachability`,
`pause_watch`, `cert_watch`, `pipeline::*`, `quorum::resolve`, і частини
`topn_updater`/`geoip_updater`/`geoip_credentials`/`cert`/`cert_rotation`.

**Побічний ефект — звуження оголило 2 pre-existing gap'и**, раніше замасковані
широкою поверхнею (публічний елемент не варнить dead_code, навіть без реального
викликача): `BaselineSelector::on_primary()` (нуль production-викликів, лише
тести) — видалено, тести переписані на `active_index() == 0`;
`watchdog::transition::transition`'s `&TransitionInput`-параметр — clippy
`trivially_copy_pass_by_ref` раніше мовчав, бо функція вважалась «частиною
публічного API» (`effective_visibilities::is_exported`); тепер internal-only →
лінт більше не пригнічений → змінено на by-value (`TransitionInput` вже `Copy`).

**§7.1 #7 межа названа явно, не залишена неявною.** `run_service_to_watcher_watchdog`
тепер живе в lib-коді, який лінкує і `dnsqb-watcher`, а не в окремому `main.rs`, як
раніше — фізична межа зникла. Doc-коментар `orchestrate::run` називає інваріант
прямо: `dnsqb-watcher` не повинен викликати `run()` чи цю функцію напряму.

**Верифіковано:** повна тест-матриця без регресій (748+6 lib / 37 tray / 3 watcher /
8 examples / 9 doctests — точне число до і після, advisor-catch: `--doc` не бере
`--document-private-items`, тож демоутнутий doctest-символ мовчки губить свій тест
/ 18+2 conformance / 7 admin_client), `clippy --all-targets -D warnings`, `fmt`,
`rustdoc -D warnings`. Ручний smoke: реальний `dnsqb-service.exe` на скретч
app-data — `/health` 200, `/admin/ui` 200, реальний `GET /dns-query` для
`example.com` A → 200 `application/dns-message`.

**Наслідки:**
- Новий `crates/dnsqb-service/src/orchestrate.rs`; `main.rs` — 3 рядки.
- `lib.rs`: 49 → 32 `pub use`-рядки.
- `admin.rs`/`dispatch.rs`: 2 внутрішні місця, що самі йшли через `crate::`-реекспорт
  замість модульного шляху, переведено на прямі шляхи (`crate::reachability::
  NetworkReachability` тощо) — виявлено тим самим компілятор-керованим проходом.
- `baseline_selector.rs`: видалено `on_primary()`.
- `watchdog/transition.rs`, `watchdog/loop_driver.rs`: `transition` бере
  `TransitionInput` за значенням.
- CLAUDE.md `dnsqb-service`-параграф + новий рядок `orchestrate` у таблиці модулів.
- Два окремі коміти (механічний рух, потім звуження) — прецедент T-211/T-204, той
  самий advisor-catch: перший коміт самодостатньо тестовний і доводить «нуль змін
  видимості» власним дифом.

---

## 2026-09-11 — Батч 4.2 (T-122/T-123): держ./науково-освітні зони — спрощено проти SPEC.md
§5.3 пп.2–3

**Що казав SPEC.md (пп.2–3, план ще з початкового формулювання §5.3):** для держ. доменів —
гібридний підхід: евристичне кандидування за TLD-патерном (`*.gov`, `*.gov.ua`, `*.gob.*` і
подібні) + **обов'язкове ручне рев'ю перед публікацією**, дефолт N=10/країну, «постійний
редакційний процес, не одноразовий скрипт». Для науково-освітнього списку — ручна курація з
власним процесом рев'ю, версіонуванням і changelog. TASKS.md дублював це в план Батчу 4.2:
окремий інструмент кандидування (аналог `curate_topn`) + розвилка PR-шаблон/CODEOWNERS/
`ZONES-CHANGELOG.md`.

**Що зроблено натомість (рішення користувача на kickoff-плані батчу):** користувач відхилив
запланований обсяг як зайву складність для одноосібного репозиторію — «просто витягати топ
відповідних доменів спираючись на старі або відомі домени які використовують науковці інженери
держ службовці. Там відносно вузький список доменів». Реалізовано:

- **Без окремого інструмента кандидування.** Замість евристичного TLD-сканування широкого пулу
  кандидатів — прямий підбір: кожен держ.-список тримає **один запис** — сам домен/суфікс, чия
  реєстрація обмежена реєстратором лише легітимним держ. органам (`gov.ua`, `gov.pl`, `gov.uk`,
  голий `gov` для США — DotGov Act 2021, CISA). Через те, що `ZoneLists::zone_match`
  (`rating_filter.rs`) — чистий walk по суфіксах без PSL, один такий запис автоматично покриває
  весь простір піддоменів — не потрібне ані кандидування, ані перелік конкретних міністерств.
- **Без формального PR-шаблону/CODEOWNERS.** Прозорість курації (SPEC.md вимагає «публічний і
  версійований список») закрита легким `data/topn/ZONES-CHANGELOG.md` — таблиця «запис / критерій
  / джерело перевірки» без жодного GitHub-механізму.
- **Науково-освітній список (`edu.txt`, T-123)** — той самий blanket-suffix трюк для `edu`/
  `ac.uk`/`edu.ua`/`int` + коротка жменька індивідуально названих globally-визнаних тіл (SPEC.md
  §5.3's власні приклади PubMed/NASA + arXiv/DOI/ORCID/IETF/W3C/IEEE/ООН), без формального
  процесу рев'ю понад те, що вже фіксує changelog.

**Причина, чому це не просто «спростили, бо лінь», а об'єктивно краще рішення:** ризик, який
SPEC.md називав («хибне зарахування домену до "державного" має реальні наслідки для довіри
користувача») звужується до перевірки самої **політики реєстратора** (об'єктивний, публічно
задокументований факт — «чи gov.ua дійсно обмежений реєстратором», перевірено пошуком цього
сеансу проти офіційних джерел реєстратора/реєстру), а не до суб'єктивного редакційного судження
про конкретний сайт («чи цей саме сайт "справді" урядовий»). Легітимність вже гарантована на рівні
реєстрації домену — повторна ручна перевірка кожного окремого сайту нічого не додає, лише коштує
процесу. Це той самий клас аргументу, що T-108 (гігієна → лінива, не bulk-скан) і T-127 (гейт по
`AVAILABLE_TOPN_LISTS`): перевірити raз на структурному рівні дешевше й надійніше, ніж перевіряти
щоразу вручну.

**Прогалини, свідомо не закриті цим батчем:** `gov-de` (Німеччина не має єдиної, реєстратором
обмеженої конвенції для держ.-доменів — SPEC.md сам називає цю варіативність); аналогічний
обмежений академічний SLD для `de`/`pl` у `edu.txt` (не перевірено з достатньою впевненістю цієї
сесії). Обидві — задокументовані в `data/topn/README.md`/`ZONES-CHANGELOG.md`, не в TASKS.md
backlog (немає інструмента, який їх колись закриє автоматично — наступний крок, якщо колись
знадобиться, це ручна курація за тим самим процесом).

**Структурний ризик, знайдений advisor-ревʼю kickoff-плану (не в оригінальному SPEC.md-плані
взагалі — новий клас ризику, який виник саме через blanket-suffix дизайн):** один запис = цілий
простір піддоменів, тож хибно-позитивний quorum-блок голого суфікса (буквально `gov.ua`) міг би
через T-108 ліниву гігієну евіктити всю державну зону мовчки. Закрито `ZoneSourceKind::
hygiene_eligible` (`false` для `GovernmentTopN`/`SciEdu`) на двох рівнях — `zone_match` сам
ігнорує `removed` для non-eligible джерела (структурно, не лише дисципліна виклику викликача), і
`pipeline::rating_filter_step`'s `exact` додатково вимагає eligibility. Деталі — TASKS-DONE.md
«Батч 4.2».

**Наслідки в доках:** SPEC.md §5.3 (пп.2–3 лишено як історичний план + новий pointer-абзац на цей
запис, не видалено — «ніколи не депрекейтити мовчки»), `CONFIGURATION.md` (`[rating_filter]
lists` — нові коди + приклади помилок), `data/topn/README.md` (новий розділ), новий
`data/topn/ZONES-CHANGELOG.md`, `TASKS.md`/`TASKS-DONE.md`/`CLAUDE.md`, `diagrams/rating-filter.md`
(ground-truth ritual).

---

## 2026-09-11 — Батч 4.5 (T-138): персональне навчене джерело зон + персистенція
T-108-overlay

**Контекст:** останнє, четверте джерело зон «бульбашки» (SPEC.md §5.1.1) — локально навчений
список часто/регулярно відвідуваних доменів самого користувача, похідний виключно з уже-ALLOW-і-
вже-пройшов-Quorum трафіку. Kickoff plan+advisor (два раунди рев'ю).

**(а) Навігація vs subresource — прийнято як чесну структурну прогалину DNS-шару.** SPEC.md §5.1.1
вимагає рахувати лише «користувач-ініційовані навігації», не кожен subresource-домен (реклама/
аналітика/CDN), що тягнеться з кожним відвіданим сайтом — інакше частотний критерій штучно роздує
такі домени. DNS-резолвер технічно не бачить різниці: кожен A/AAAA-запит виглядає однаково, немає
браузерного сигналу (Sec-Fetch-Dest тощо) на цьому шарі. **Рішення користувача цього kickoff'у:**
прийняти як прогалину, той самий клас чесності, що вже є в проєкті (ccTLD-евристика §5.2, GeoIP
anycast §3.5) — не намагатись імітувати точність, якої шар не має. Наслідок: домени на кшталт
`google-analytics.com` природно потраплять у персональний список як «часто дозволені» — це не
помилка навчання, а точне відображення того, що резолвер справді бачить.

**(б) Архітектура: окремий `Arc<ZoneLists>`, не третій елемент існуючого `Vec<ZoneSource>`.**
`rating_filter.rs`'s module doc (Батч 4.3) заздалегідь формулював N-арність саме заради цього
батчу — «Батч 4.5 adds the personal learned list» — але буквальне додавання персональної зони в
той самий `Vec<ZoneSource>`, який публікує `AppState.rating_filter_zone`, зламало б інваріант
одного писача: `topn_updater::refresh_all_lists` (24-годинний цикл, повна заміна) і персональний
щоденний rollover — два незалежні писачі до одного `Arc`, неминучий втрачений запис (той самий
клас проблеми, задля якої T-108's `removed`-множина вже винесена в окремий lock). **Рішення:**
`AppState.rating_filter_personal_zone: RwLock<Arc<ZoneLists>>` — повністю незалежний від
`rating_filter_zone`, власний писач (`personal_zone_persist::run_personal_zone_task`).
`pipeline::rating_filter_step` викликає `ZoneLists::zone_match` **двічі** (спершу завантажений
список, тоді персональний) — жодних змін сигнатури вже протестованого `zone_match`. `removed`-
overlay (T-108) застосовується до обох викликів однаково — `record_zone_removal` вже
індиферентний до джерела.

**(в) `rating_filter_is_active` НЕ змінюється — advisor-знахідка другого раунду рев'ю.** Перша
версія kickoff-плану розширювала предикат до `enabled && !(zone.is_empty() && personal.is_empty())`
— це відкривало рівно ту несподіванку, яку SPEC.md §5.3 п.8 забороняє явно: `enabled=true`,
`lists=[]`, персональна зона навчила N хостів → бульбашка стає *активною* і блокує весь інтернет
поза тими N, без жодного обраного списку користувачем. SPEC.md §5.1.1 сам каже, що персональне
джерело «має сенс лише коли рейтинг-фільтр §5.3 увімкнений» — **додає** до вже змістовної
бульбашки, ніколи не активує її самотужки. Виправлено до злиття: `rating_filter_is_active`
лишається дволичильниковою (0 змінених serve-side викликів), тест
`a_personal_zone_alone_never_activates_the_bubble` фіксує властивість явно.

**(г) Записи — hostname, не registrable.** Клієнт свідомо не має PSL (T-105) — навчена множина
покриває hostname і його піддомени через той самий suffix-walk, що й завантажені списки (вужчий,
безпечний випадок), але `www.`/`api.`/`cdn.` того самого сайту рахуються як окремі записи, ділять
частотний сигнал і швидше вичерпують `MAX_TRACKED_DOMAINS` (2000, провний bound).

**(д) Персональний список за конструкцією навчається лише на вже-в-зоні доменах, поки бульбашка
активна.** Домен поза зоною блокується на кроці 5 й ніколи не доходить до quorum — тобто сигнал
навчання не додає нового покриття «наживо». Цінність — утримувати вже дозволені домени проти
майбутнього дрейфу публічних списків (SPEC.md's власне формулювання) плюс покриття для періодів,
коли бульбашка була вимкнена/порожня. Не баг; підтверджує вибір гейтити навчання на
`personal_zone.enabled`, не на `rating_filter.enabled`.

**(е) T-108's обіцянка також закрита цим батчем** (TASKS.md's рядок 4.5 цього не називав — пропуск
у першому чернетковому проході плану, спіймано advisor-рев'ю): `AppState.rating_filter_removed`
(лінива гігієна) тепер переживає рестарт через `zone_removal_persist.rs`, окремий, малий модуль.
Гейт — `[rating_filter].enabled`, не `[personal_zone].enabled` (overlay важливий, доки увімкнений
сам рейтинг-фільтр). Переюзано наявний `persistence-key` (T-96/97) — overlay є операційним станом
уже-опт-ін фічі (множина вже заблокованих quorum'ом registrable, той самий приватнісний рівень, що
`cache.enc`), не новою розкриттям персональної історії, тому не потребує окремого секрету.

**(є) Знайдено, не виправлено в цьому батчі: `PersistTarget.rating_filter` — застарілий знімок.**
`dispatch.rs`'s `apply_admin_config`/`apply_cache_config`/`apply_geoip_change`/
`apply_provider_change` читають `state.persist.rating_filter.clone()` — статичний знімок, взятий
рівно один раз при старті `AppState::new` і ніколи не оновлюваний після. Якщо користувач змінює
`[rating_filter]` через власний `POST /admin/rating-filter` (T-127), а тоді викликає будь-який
**інший** із цих чотирьох маршрутів, той маршрут перезаписує `resolver_config.toml` застарілим
значенням `rating_filter`, мовчки скасовуючи зміну на диску (не в пам'яті — `state.
rating_filter_config` лишається коректним до наступного рестарту). Той самий клас багу, що T-57/
T-139/T-149/T-47/T-77. Не виправлено тут — поза межами цього батчу, і `[rating_filter]` вже мав
власний робочий шлях до дня, коли він набув живого RwLock (T-127, той самий Батч 4.4, де
з'явився `POST /admin/rating-filter`). **Для нового поля `[personal_zone]` цей самий клас багу
свідомо не відтворено:** усі 5 місць побудови `ResolverConfig` у цьому батчі читають живий
`state.personal_zone_config_snapshot()`, а не статичний `PersistTarget`-знімок — `[personal_zone]`
взагалі не додано до `PersistTarget`. TASKS.md backlog отримує окремий пункт на виправлення
`rating_filter`-версії того самого класу бага.

**Наслідки в коді:** `rating_filter.rs` (`ZoneSourceKind::Personal`, `hygiene_eligible() == true`),
`key_store.rs` (4-й секрет `personal-zone-key`, рефакторинг спільного mint-хелпера),
`encrypted_file.rs` (`FileKind::PersonalZone`/`ZoneRemovals`), нові `personal_zone_stats.rs`
(чисте ядро) / `personal_zone_persist.rs` / `zone_removal_persist.rs`, `config.rs` (`[personal_zone]`
таблиця + `validate_personal_zone`), `dispatch.rs`/`pipeline.rs`/`orchestrate.rs` (проводка),
`admin.rs` (`RatingFilterStatusView.personal_zone_enabled` + `loaded` розширено; `ADMIN_DTO_
SCHEMA_VERSION` 1→2), `local_state.rs` (5-й артефакт видалення).
**Наслідки в доках:** `CONFIGURATION.md` (`[personal_zone]` таблиця + 3-тє шифроване сховище),
`SECURITY.md` (4-й секрет + новий рядок загроз), `SERVICES.md` (`personal-zone.enc` у переліку),
`CLAUDE.md` (Батч 4.5 project-state абзац + таблиця модулів + known-limitations), `TASKS.md`/
`TASKS-DONE.md`, `diagrams/rating-filter.md` (ground-truth ritual).
