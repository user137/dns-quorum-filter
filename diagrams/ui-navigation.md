SOURCES: SPEC.md §6, §8, §8.1, §3.3, §3.4, §3.5, §5, §5.1.1, §5.2, §5.3, "Наскрізні вимоги";
DECISIONS.md 2026-09-07 (T-179 — §5.1 прибрано, рейтинг-фільтр Ф5→Ф4); `diagrams/rating-filter.md`
(DB-IP Lite атрибуція), `diagrams/onboarding.md` (майстер першого запуску, T-188), "Відкриті
питання" №4, №10, №11; TASKS.md T-47, T-52, T-56, T-57, T-72, T-73, T-77, T-78, T-81, T-111,
T-118, T-127, T-128, T-138, T-139, T-140, T-176, T-188; UI-SPEC.md §2.1;
DECISIONS.md 2026-09-06 (T-176), 2026-09-08 (T-188 — онбординг); mockups/gui-dashboard.html.

# Навігація / інформаційна архітектура GUI

Структура вкладок і те, що на кожній обов'язково є — не порядок кліків користувача
(це не user-flow діаграма), а інвентар екранів з прив'язкою до розділів SPEC.md,
звідки взята кожна вимога.

```mermaid
flowchart TD
    Header["Персистентний header (на ВСІХ екранах)<br/>Індикатор стану — обов'язковий, не опційний (§8)<br/>Посилання «Приватність кворуму» — не дрібним шрифтом (§8, ВП№4)<br/>Бейдж рейтингового фільтра, якщо увімкнено (§5.3, T-128) — окремий, завжди видимий"]

    Header --> Dashboard
    Header --> Log
    Header --> Lists
    Header --> Providers
    Header --> GeoIP
    Header --> Advanced
    Header --> About

    Dashboard["Dashboard / Статус — Ф1 база<br/>T-176: базовий вигляд = hero-статус «захищено/не захищено» (обчислюваний, §8/§3.3/§7) + головний перемикач + 3 категорійні перемикачі шкідливе/реклама/дорослий (POST /admin/providers/set-category-enabled, §3.4)<br/>T-188: hero дістав cert-гілку — GET /admin/cert-status: NOT_TRUSTED → «Сертифікат не встановлено» + кнопка «Встановити сертифікат» (POST /admin/install-cert), UNKNOWN → «Сертифікат не перевірено» (окремо)<br/>рядок «N сторін бачать запити» + попередження pass-through — у базовому вигляді, НЕ в розкритті (§3.4, §8.1)<br/>секція «Підключення браузера» (copy DoH-URL + перевірка, T-176; per-браузер кроки — T-189)<br/>швидка статистика заблокованого (§8) + прев'ю логу<br/>режим таймауту, поштучні провайдери — переїхали в &lt;details&gt; «Розширені» (§3.4, T-72/T-73/T-176)<br/>Поза фазами/некритично: кількість оброблених доменів + % заблокованих, live з логу (§8, T-139)<br/>Поза фазами/некритично: топ відвідуваних сайтів поточного log-вікна (§8, §6, T-140) — не 5.1.1's персональний список"]

    Log["Лог запитів — Ф1<br/>таблиця + пошук по domain + фасети (§6)<br/>колонка voters (§6)<br/>дія «в allowlist / в blocklist» в один клік з рядка (§6) — основний UX-цикл продукту<br/>кнопка «очистити лог» (§6)"]

    Lists["Списки виключень — Ф1<br/>редактор allowlist/blocklist<br/>підсвітка конфлікту домен-в-обох-списках (§5, §8)"]

    Providers["Провайдери — картка #providers-body (T-72/T-73); T-176: усередині &lt;details&gt; «Розширені»<br/>режим таймауту fail_open/fail_closed/degraded лишився на картці #app-body (§3.3)<br/>список voter'ів згруповано за категорією SECURITY/ADS_TRACKERS/ADULT_CONTENT, тумблер на рядок → POST /admin/providers/set-enabled (§3.4)<br/>T-176: категорійний перемикач у базовому вигляді → POST /admin/providers/set-category-enabled (атомарно, один запис конфігу; порожня ADULT_CONTENT → додає opendns-familyshield, DECISIONS.md 2026-09-06)<br/>«Додати пресет» з available_presets; всі 10 вбудованих presets §3.4 (§3.4, T-73)<br/>суб-форма «додати власний DoH-провайдер»: id + URL + назва + категорія + block_signature → POST /admin/providers/add; SSRF-хост відхиляється (§3.4, T-72)<br/>рядок «N третіх сторін бачать запити» + filtering_active-попередження — T-176 підняв у базовий вигляд Dashboard (CLAUDE.md «не ховати»)<br/>GAP: бейдж DNS4EU «provider run by EU institution» ще не реалізовано; baseline-резолвер поза scope"]

    GeoIP["GeoIP — Ф2<br/>список заблокованих країн, порожній за замовчуванням (§3.5, T-77)<br/>попередження про over-blocking через CDN при додаванні країни (§3.5, T-77)<br/>дата останнього оновлення бази (§8, T-78)"]

    Advanced["Розширені — Ф4 / Ф5, нішеве використання<br/>Ф4: рейтинговий фільтр «бульбашка» — окремий візуально виділений перемикач, НЕ звичайний checkbox поруч з іншими, явне попередження «буде недоступна переважна більшість інтернету» при увімкненні, дефолт OFF (§5.3, T-127)<br/>Ф4: зони — N (розмір топ-N) + список країн + тумблер персонального списку §5.1.1 (§5.3/§5.1.1, T-111/T-138)<br/>Ф5: ccTLD-блок — список заборонених TLD, попередження про грубість евристики (§5.2, T-118)"]

    About["Про застосунок — Ф1 база / Ф2 доповнення<br/>РЕАЛІЗОВАНО T-81 як футер сторінки #credits, НЕ окремий екран (UI однобічний)<br/>Ф1: ліцензія Apache 2.0 (посилання на apache.org)<br/>Ф2: атрибуція DB-IP Lite — CC BY 4.0 (не -SA), сніпет «IP Geolocation by DB-IP» + посилання назад, юридично обов'язково (Наскрізні вимоги, T-81)<br/>Ф2: атрибуція MaxMind GeoLite2 «у розширеному режимі» (T-80)<br/>Версія застосунку — ще не показана ніде в UI"]
```

## Примітка щодо пріоритету розміщення

Лог запитів (§6: «основний UX-цикл продукту») і Dashboard — найвищий пріоритет
видимості, не глибша вкладка налаштувань. Advanced-вкладка (Ф4/Ф5) — свідомо
останньою в списку вкладок: SPEC.md сам характеризує ці фічі як нішеві
(«рідковживана функція», §5.3) і пізніші фази.

## ⚠️ GAP — вкладок немає, це інвентар вимог, не карта реалізації

Реальний `/admin/ui` (T-149) — **одна сторінка**, без вкладкового шелу. Вузли
діаграми вище — інвентар вимог SPEC.md до вмісту, не окремі екрани.

**Фактична розкладка після T-176** (макет: `mockups/gui-dashboard.html`,
UI-SPEC.md §2.1): два рівні на одній сторінці —
- **базовий вигляд** — hero-статус, головний + 3 категорійні перемикачі,
  privacy-рядок, pass-through-попередження, статистика, прев'ю логу, секція
  «Підключення браузера», футер `#credits` (T-81);
- **`<details>` «Розширені налаштування»** (згорнуто) — картки `providers`,
  `cache`, `geoip`, `geoip-maxmind`, режим таймауту, `danger-zone`. ID карток
  і їхня `main.js`-логіка без змін; T-176 їх лише загорнув.

Картка `providers` реалізована T-72/T-73; `About` — частково (футер `#credits`,
не екран). Вузли `Advanced` (Ф4: рейтинг-фільтр «бульбашка» + зони; Ф5:
ccTLD-блок) — усе ще вимоги SPEC.md, не існують; не плутати з `<details>`
«Розширені» T-176, який лише переховує наявні технічні картки.

## ⚠️ GAP

SPEC.md не визначає порядок вкладок явно (це UI-компонування, не протокольна
поведінка) — послідовність вище є інтерпретацією за принципом «найчастіше
вживане — найвище» (§6, §8), а не фактом, вичитаним із джерела. Позначено тут
чесно замість видавання за задокументоване рішення.

## Примітка — §5.1 прибрано (T-179, 2026-09-07)

Попередня редакція вузла `Advanced` мала окремий тумблер «виключення топ-сайтів
з Ads/Adult-voters» (§5.1) + ⚠️ GAP про те, що §5.1.1 (T-138) не має власного
контролу. §5.1 знято (DECISIONS.md 2026-09-07): механізм топ-сайтів злито в
рейтинговий фільтр §5.3, а §5.1.1 переформульовано як персональне джерело зон
цього фільтра. Вузол `Advanced` вище оновлено відповідно: рейтинг-фільтр +
зони (N, країни, персональний список §5.1.1 — тумблер, дефолт OFF). Детальна
поведінка фільтра — `diagrams/rating-filter.md`.
