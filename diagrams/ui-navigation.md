SOURCES: SPEC.md §6, §8, §8.1, §3.3, §3.4, §3.5, §5, §5.1, §5.1.1, §5.2, §5.3, "Наскрізні вимоги"
(DB-IP Lite атрибуція), "Відкриті питання" №4, №10, №11; TASKS.md T-47, T-52, T-56, T-57, T-72,
T-73, T-77, T-78, T-81, T-111, T-118, T-127, T-128, T-138, T-139, T-140.

# Навігація / інформаційна архітектура GUI

Структура вкладок і те, що на кожній обов'язково є — не порядок кліків користувача
(це не user-flow діаграма), а інвентар екранів з прив'язкою до розділів SPEC.md,
звідки взята кожна вимога.

```mermaid
flowchart TD
    Header["Персистентний header (на ВСІХ екранах)<br/>Індикатор стану — обов'язковий, не опційний (§8)<br/>Посилання «Приватність кворуму» — не дрібним шрифтом (§8, ВП№4)<br/>Бейдж рейтингового фільтра, якщо увімкнено (§5.3, T-128) — окремий, завжди видимий"]

    Header --> Dashboard
    Header --> Log
    Header --> Lists
    Header --> Providers
    Header --> GeoIP
    Header --> Advanced
    Header --> About

    Dashboard["Dashboard / Статус — Ф1 база<br/>тумблери провайдерів перенесено на картку #providers-body (§3.4, T-72/T-73)<br/>режим таймауту fail_open/fail_closed/degraded (§3.3)<br/>швидка статистика заблокованого (§8)<br/>прев'ю останніх записів логу<br/>Поза фазами/некритично: кількість оброблених доменів + % заблокованих, live з логу (§8, T-139)<br/>Поза фазами/некритично: топ відвідуваних сайтів поточного log-вікна (§8, §6, T-140) — не 5.1.1's персональний список"]

    Log["Лог запитів — Ф1<br/>таблиця + пошук по domain + фасети (§6)<br/>колонка voters (§6)<br/>дія «в allowlist / в blocklist» в один клік з рядка (§6) — основний UX-цикл продукту<br/>кнопка «очистити лог» (§6)"]

    Lists["Списки виключень — Ф1<br/>редактор allowlist/blocklist<br/>підсвітка конфлікту домен-в-обох-списках (§5, §8)"]

    Providers["Провайдери — РЕАЛІЗОВАНО T-72/T-73 як картка #providers-body<br/>режим таймауту fail_open/fail_closed/degraded лишився на картці #app-body (§3.3)<br/>список voter'ів згруповано за категорією SECURITY/ADS_TRACKERS/ADULT_CONTENT, тумблер на рядок → POST /admin/providers/set-enabled (§3.4)<br/>«Додати пресет» з available_presets; всі 10 вбудованих presets §3.4 (§3.4, T-73)<br/>суб-форма «додати власний DoH-провайдер»: id + URL + назва + категорія + block_signature → POST /admin/providers/add; SSRF-хост відхиляється (§3.4, T-72)<br/>рядок «N третіх сторін бачать запити» + filtering_active-попередження (CLAUDE.md «не ховати»)<br/>GAP: бейдж DNS4EU «provider run by EU institution» ще не реалізовано; baseline-резолвер поза scope"]

    GeoIP["GeoIP — Ф2<br/>список заблокованих країн, порожній за замовчуванням (§3.5, T-77)<br/>попередження про over-blocking через CDN при додаванні країни (§3.5, T-77)<br/>дата останнього оновлення бази (§8, T-78)"]

    Advanced["Розширені — Ф4 / Ф5, нішеве використання<br/>Ф4: виключення топ-сайтів з Ads/Adult-voters — тумблер + N + список країн (§5.1, T-111)<br/>Ф5: ccTLD-блок — список заборонених TLD, попередження про грубість евристики (§5.2, T-118)<br/>Ф5: рейтинговий фільтр — окремий візуально виділений перемикач, НЕ звичайний checkbox поруч з іншими, явне попередження при увімкненні (§5.3, T-127)"]

    About["Про застосунок — Ф1 база / Ф2 доповнення<br/>РЕАЛІЗОВАНО T-81 як футер сторінки #credits, НЕ окремий екран (UI однобічний)<br/>Ф1: ліцензія Apache 2.0 (посилання на apache.org)<br/>Ф2: атрибуція DB-IP Lite — CC BY 4.0 (не -SA), сніпет «IP Geolocation by DB-IP» + посилання назад, юридично обов'язково (Наскрізні вимоги, T-81)<br/>Ф2: атрибуція MaxMind GeoLite2 «у розширеному режимі» (T-80)<br/>Версія застосунку — ще не показана ніде в UI"]
```

## Примітка щодо пріоритету розміщення

Лог запитів (§6: «основний UX-цикл продукту») і Dashboard — найвищий пріоритет
видимості, не глибша вкладка налаштувань. Advanced-вкладка (Ф4/Ф5) — свідомо
останньою в списку вкладок: SPEC.md сам характеризує ці фічі як нішеві
(«рідковживана функція», §5.3) і пізніші фази.

## ⚠️ GAP — вкладок немає, це інвентар вимог, не карта реалізації

Реальний `/admin/ui` (T-149) — одна сторінка з картками (status, providers,
overrides, cache, geoip, geoip-maxmind, log) плюс футер `#credits` (T-81), без
вкладкового шелу. Картка `providers` реалізована T-72/T-73; вузли
`Advanced`/`About` вище описують **вимоги SPEC.md до вмісту**, а не окремі
екрани, що існують сьогодні. `About` реалізовано частково (як футер, не екран).

## ⚠️ GAP

SPEC.md не визначає порядок вкладок явно (це UI-компонування, не протокольна
поведінка) — послідовність вище є інтерпретацією за принципом «найчастіше
вживане — найвище» (§6, §8), а не фактом, вичитаним із джерела. Позначено тут
чесно замість видавання за задокументоване рішення.

## ⚠️ GAP — 5.1.1 (T-138) не додано до вузла Advanced

SPEC.md §5.1.1 описує другий, персональний варіант топ-N-виключення (той
самий механізм, що §5.1's тумблер у вузлі `Advanced` вище, T-111) — але
T-138 не визначає власного UI-контролу (тумблер? окрема секція? частина
того самого §5.1-тумблера?), і `UI-SPEC.md` не має рядка для нього. Додавати
вузол/підпис тут означало б вигадати форму контролу, якої джерело ще не
називає — за правилом ritual'у вище це порушення, не акуратність. Оновити
цей вузол, коли T-138 (чи його UI-задача) зафіксує конкретний контрол.
